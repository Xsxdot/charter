# C1.5 契约落地台账

- 2026-08-24：`git status --short --branch` 实读为 `## cards/C1.5-charter`，工作树初始无改动。
- 2026-08-24：`docs/specs/2026-08-23-codegraph-context-assembler-spec.md:3` 实读为「已批准」（2026-08-23）并含「2026-08-24 复核修订已批准」状态位。
- 2026-08-24：`find . -maxdepth 3 -type f \( -name 'target.json' -o -name 'best.json' \)` 未找到项目根级目标图；仅 `graph/codegraph/testdata/repo/codegraph/target.json` 与 `best.json` 位于 fixture，按存量无图项目规则冻结物采用契约增量文档，不创建项目级目标图/视图 diff。
- 2026-08-24：实读 spec `:69`、`:111`，发现 `--collapse-util` 的高扇入阈值 N、外部领域折叠行的代表节点数量、`context` 主调用链的前 N 级均未给出可冻结数值；按契约纪律未自行假定，暂停出稿等待裁决。
- 2026-08-25：协调者裁决三个数均为 3：`--collapse-util` 按「被 >=3 个不同领域调用」判定；外部领域折叠保留按本次结果被调用次数降序的前 3 个代表且 `count` 保留全量；`context` 主链默认 3 级。三者均写死包内常量，不进配置或 flag；依据分别为 handoff 基线 K>=3 命中 28 节点、3 个代表足够辨认且避免清单复现、与 C1.10 级联深度对齐并让 30000 token 截断生效。
- 2026-08-25：现状签名核对：`graph/codegraph/query.go#Neighborhood(v *View, foci []string, down, up int) (*Result, error)` 保持多源 BFS；`graph/codegraph/sym.go#ReAnchor(repoRoot string, n Node) (int, string)` 读文件并返回行号/状态；`graph/codegraph/stale.go#CheckStale(repoRoot string, g *Graph) []StaleNode` 按全图工作。
- 2026-08-25：现状签名核对：`graph/codegraph/domains.go#DomainTree(v *View) []DomainStat` 与 `#DomainTreeWithBest(v *View, best *Best) []DomainStat` 负责领域树；`graph/codegraph/decls.go#LoadDomainDecls(repoRoot string) (map[string]DomainDecl, error)` 读取声明；`graph/codegraph/entity.go#EntityLookup(v *View, repoRoot, arg string) (*EntityResult, error)` 负责单实体投影链。
- 2026-08-25：现状 wire 核对：`graph/codegraph/types.go#Graph` 有 `Packages map[string]Package`，但 `graph/codegraph/merge.go#View` 当前不携带 `Packages`；context 组合器必须从基线 `Graph` 取包摘要，不新增加载路径。
- 2026-08-25：现状 CLI 核对：`graph/cli/cli.go#graphQueryRunE` 当前调用 `Neighborhood` 后直接输出 `graphQueryOutput`，`--stale` 调用 `CheckStale(graphRepo, g)`；`chain`/`who-calls` 使用 `cobra.MinimumNArgs(1)`。Cobra v1.10.2 源码 `args.go:73-100` 证实该校验分别为至少 1 个参数/精确 N 个参数。
- 2026-08-25：现状依赖核对：`graph/go.mod` 与 `graph/cli/deps_test.go:12-49` 将 CLI 直接依赖冻结为 `github.com/spf13/cobra v1.10.2`，其余仅 pflag/mousetrap 传递依赖；契约不新增依赖。
- 2026-08-25：契约翻译再审发现 spec 只冻结 `context` 主链深度 3，未冻结主链焦点集合；现状可复用的候选是 `DomainTree(v)` 产出的 `DomainStat.Interfaces` 与图节点 `kind=="entry"`，二者语义不同，未自行选择，等待裁决。
- 2026-08-25：协调者裁决 context 焦点取未删除 `kind=="entry"` 节点与 `DomainStat.Interfaces` 的并集并去重；父领域按整棵子树边界重算入缝；接口清单全量输出，主链焦点配额为 5，按跨域入边数降序、entry 优先、id 字典序排序，超额输出 `fociTruncated:{total,shown,reason:"focus-quota"}`；两类焦点均过滤 `status=="deleted"`，主链深度仍固定 3。
- 2026-08-25：执行 `mkdir -p docs/superpowers/specs` 成功，为法定契约路径创建缺失目录；未改动其他路径。
- 2026-08-25：契约文档当前为 301 行；`git diff --check` 退出码 0，`gofmt -d cli/cli.go codegraph/query.go codegraph/sym.go codegraph/stale.go codegraph/domains.go codegraph/decls.go codegraph/entity.go` 无输出且退出码 0。
- 2026-08-25：在 `graph/` 执行 `go build ./...`；原始 stdout/stderr 为空，退出码 `0`。
- 2026-08-25：在 `graph/` 执行 `go test ./...`；原始输出为 `ok github.com/Xsxdot/charter/graph/cli 0.368s`、`? github.com/Xsxdot/charter/graph/cmd/codegraph [no test files]`、`ok github.com/Xsxdot/charter/graph/codegraph 0.027s`、`ok github.com/Xsxdot/charter/graph/webui 0.002s`，退出码 `0`。
- 2026-08-25：`find . -maxdepth 2 -type f -path './codegraph/*' -print` 原始输出为空；当前项目无根级 baseline，故未运行 `codegraph resolve --doc`，符号锚按无图项目的文件/符号锚人工核对。
- 2026-08-25：契约补入 `ContextDomain`、context `--stale` 语义及四个行为常量的包内逐行注释要求；`wc -l` 实读契约 316 行；再次 `git diff --check` 原始输出为空，退出码 `0`。
- 2026-08-25：执行 `git add -N docs/superpowers/specs/c1.5-contract.md docs/ledgers/2026-08-24-codegraph-context-assembler-ledger.md` 失败；原始报错：`fatal: Unable to create '/root/.handoff/repos/charter/.git/worktrees/2f32445f/index.lock': Read-only file system`，退出码 `128`；未据此推断代码状态。
- 2026-08-25：在获准写 Git 元数据后执行 `git add docs/superpowers/specs/c1.5-contract.md docs/ledgers/2026-08-24-codegraph-context-assembler-ledger.md` 成功，退出码 `0`。
- 2026-08-25：首次 `git diff --cached --check` 发现契约头部第 3~5 行尾随空格；已用 `apply_patch` 删除并重新暂存。复跑 `git diff --cached --check` 原始输出为空、退出码 `0`；暂存统计为 2 个新增文件、338 行。
- 2026-08-25：收尾前复跑 `git diff --cached --check` 原始输出为空、退出码 `0`；`git diff --cached --stat` 显示 2 个新增文件、339 行；`git status --short --branch` 显示两文件均已暂存于 `cards/C1.5-charter`。
- 2026-08-25：执行 `git commit -m 'docs(contract): freeze C1.5 context assembler'` 成功；原始输出为 `[cards/C1.5-charter 5dafd0b] docs(contract): freeze C1.5 context assembler`、`2 files changed, 340 insertions(+)`，当前提交已冻结契约文档与台账。
- 2026-08-25：提交后的 `git diff --check` 原始输出为空，退出码 `0`；仅台账追加本提交记录，契约文档未再修改。
- 2026-08-25：最终审计：`git status --short --branch` 原始输出为 `## cards/C1.5-charter`；`git log -2 --oneline --decorate` 原始输出为 `99b0661 (HEAD -> cards/C1.5-charter) docs(ledger): record C1.5 freeze` 与 `5dafd0b docs(contract): freeze C1.5 context assembler`；`git diff --check HEAD~2..HEAD` 与 `git show --check --oneline --summary HEAD` 均无检查输出且退出码 `0`。
- 2026-08-25：本轮实读 `/root/.codex/skills/handoff/SKILL.md` 全部 374 行；遵守平台不变量，未调用 handoff CLI、未派发或启动子任务。
- 2026-08-25：`git status --short --branch` 原始输出为 `## cards/C1.5-charter-3`，工作树无已报告改动；本轮只在当前分支工作。
- 2026-08-25：本轮实读 C1.5 上游 spec/contract/breakdown；`wc -l` 原始输出为 `259`、`322`、`292` 行。契约冻结新增签名、默认值、wire 形状、context 八步、CLI 参数、Snippet 收缩与文档同步边界。
- 2026-08-25：按协调者补充裁决，本计划将 A～E 作为单轮 implement 内的有序 task，不扇出子卡；A 落 `assemble.go`/`source.go`，`query.go` 仅保留 BFS 与 deleted-edge 修正；C 必须包含穿过 `graphPrintJSON` 的真实 stdout JSON 回归；D 因 vitest+tsc 验收继续独立。
- 2026-08-25：计划出稿前基线命令实跑：`git diff --check` 退出码 `0`、原始输出为空；`cd graph && go test ./...` 退出码 `0`，原始输出为 `ok github.com/Xsxdot/charter/graph/cli (cached)`、`? github.com/Xsxdot/charter/graph/cmd/codegraph [no test files]`、`ok github.com/Xsxdot/charter/graph/codegraph (cached)`、`ok github.com/Xsxdot/charter/graph/webui (cached)`；`cd graph && go build ./...` 退出码 `0`、原始输出为空。
- 2026-08-25：webui 基线命令均未通过：`cd graph/webui && npm test -- --run` 退出码 `127`，原始报错 `sh: 1: vitest: not found`；`npm run typecheck` 退出码 `127`，原始报错 `sh: 1: tsc: not found`；`npm run build` 退出码 `127`，原始报错 `sh: 1: tsc: not found`。计划将其标为基线未验证，不替失败命令归因。
- 2026-08-25：现状源码核对：`graph/codegraph/merge.go` 的 `View` 不带 `Packages`；`Graph.Packages` 位于 `types.go`；`ReAnchor` 在 `sym.go:118-154` 读全文件后仅返回行号/状态；`CheckStale` 在 `stale.go:30-78` 接收 `*Graph`；CLI `graphPrintJSON` 在 `cli.go:82-88` 是唯一 JSON stdout 编码器，`graphQueryRunE` 在 `cli.go:453-490` 当前把 stale 错传全图；命令计数基线为 14（`cli_test.go:657-675`）。
- 2026-08-25：现状日志核对 `rg -n 'slog|log\\.|zerolog|zap|logger|fmt\\.Print|console\\.log' graph/codegraph graph/cli graph/webui skills/spec/SKILL.md skills/plan/SKILL.md` 仅命中计划纪律文字与 webui 依赖 lockfile，未找到项目结构化 logger；计划将关键节点日志明确落为 Go 标准库 `log/slog`，webui 本 task 不新增日志。
- 2026-08-25：计划判据对应的 Go 包基线实跑：`cd graph && go test ./codegraph -run 'Test(Neighborhood|ReAnchor|DomainTree|Entity|LoadDomainDecls)' -count=1` 退出码 `0`，原始输出 `ok github.com/Xsxdot/charter/graph/codegraph 0.007s`；`cd graph && go test ./cli -run 'TestGraph(Chain|WhoCalls|Domains|Entity|CommandCount)' -count=1` 退出码 `0`，原始输出 `ok github.com/Xsxdot/charter/graph/cli 0.014s`。
- 2026-08-25：法定计划初稿写入 `docs/superpowers/plans/c1.5-plan.md`；`wc -l` 原始输出为 `514`；`git diff --check -- docs/superpowers/plans/c1.5-plan.md` 退出码 `0`、原始输出为空；计划覆盖 A～E、接口签名、基线、日志/注释、缺陷族、序列化边界、接缝双向检查与四种协调者真机形态。
- 2026-08-25：计划占位符扫描 `awk 'NR < 495' docs/superpowers/plans/c1.5-plan.md | rg -n 'TBD|TODO|待定|同上|适当处理|无风险'` 退出码 `0`、原始输出为空；自我声明仅位于计划第 495 行后的“占位符扫描”段，属于规则允许的例外声明。skill 文档基线扫描原始命中仅为 `skills/plan/SKILL.md:49` 的纪律示例与 `skills/spec/SKILL.md:15` 的旧菜单。
- 2026-08-25：首次 `git add docs/superpowers/plans/c1.5-plan.md docs/ledgers/2026-08-24-codegraph-context-assembler-ledger.md` 失败；原始报错为 `fatal: Unable to create '/root/.handoff/repos/charter/.git/worktrees/a121535a/index.lock': Read-only file system`，退出码 `128`。
- 2026-08-25：在获准写 Git 元数据后重试同一 `git add` 成功，退出码 `0`、原始输出为空；仅暂存法定计划与台账。
- 2026-08-25：暂存审计：`git diff --cached --check` 退出码 `0`、原始输出为空；`git diff --cached --stat` 原始输出为台账 13 行新增、计划 513 行新增，共 2 文件/526 行；`git status --short --branch` 原始输出为 `## cards/C1.5-charter-3`、台账 M 和计划 A 均已暂存；`git diff --cached --name-only` 仅列法定台账与 `docs/superpowers/plans/c1.5-plan.md`。
- 2026-08-25：计划结构扫描 `rg -n '^## (1|2|3|4|5|6|7)|^Task [A-E]|^### Interfaces|^### 实现步骤|^### 文件集' docs/superpowers/plans/c1.5-plan.md` 命中 A～E 五个 task、各自文件集/Interfaces/实现步骤，以及协调者真机与自审段；未发现任务缺失。
- 2026-08-25：提交前复跑暂存门禁：`git diff --cached --check` 退出码 `0`、原始输出为空；`git diff --cached --stat` 原始输出为台账 15 行新增、计划 513 行新增，共 2 文件/528 行；`git status --short --branch` 原始输出为 `## cards/C1.5-charter-3`，仅法定台账和计划已暂存。
- 2026-08-25：执行 `git commit -m 'docs(plan): C1.5 CLI context assembler implementation plan'` 成功；原始输出为 `[cards/C1.5-charter-3 d95d53b] docs(plan): C1.5 CLI context assembler implementation plan`、`2 files changed, 529 insertions(+)`，法定计划已入库。
- 2026-08-25：收尾审计：`git status --short --branch` 原始输出为 `## cards/C1.5-charter-3`（工作树干净）；`git log -3 --oneline --decorate` 原始输出为 `9f6b6a4 (HEAD -> cards/C1.5-charter-3) docs(ledger): record C1.5 implementation plan`、`d95d53b docs(plan): C1.5 CLI context assembler implementation plan`、`46b9539 (cards/C1.5-charter-2) docs(ledger): finalize C1.5 breakdown audit`；`git diff --check HEAD~2..HEAD && git show --check --oneline --summary HEAD` 退出码 `0`，原始输出仅为 `9f6b6a4 docs(ledger): record C1.5 implementation plan`；法定计划存在且 `wc -l` 原始输出为 `513 docs/superpowers/plans/c1.5-plan.md`。
- 2026-08-25：本轮读取 `/root/.codex/skills/handoff/SKILL.md`、`skills/breakdown/SKILL.md`、`skills/architecture-law/SKILL.md`；任务属于 handoff executor 的 breakdown 节点，且平台不变量禁止调用 handoff CLI、派发或启动子任务。
- 2026-08-25：`git status --short --branch` 原始输出为 `## cards/C1.5-charter-2`，当前工作树无已报告改动；分支与历史台账记录的 `cards/C1.5-charter` 不同，未据此切换分支。
- 2026-08-25：`docs/specs/2026-08-23-codegraph-context-assembler-spec.md:3-5` 实读为上游 spec「已批准」且卡号 `C1.5`；本轮以该状态作为拆解上游状态位。
- 2026-08-25：`rg --files | rg '(^|/)(best|target)\\.json$'` 实读仅命中 `graph/codegraph/testdata/repo/codegraph/{best,target}.json` fixture，未发现项目级 `codegraph/best.json` 或 `target.json`；本轮按无项目级代码图处理，fixture 仅用于代码/测试入口核对。
- 2026-08-25：契约头部 `docs/superpowers/specs/c1.5-contract.md:3-6` 实读为 L3、上游已批准、契约「随本提交冻结」，并明确无项目级 `codegraph/target.json`；本轮拆解引用该冻结物，不新增接缝。
- 2026-08-25：契约冻结的主要现状入口为 `Neighborhood`、`Resolve`、`ReAnchor`、`CheckStale`、`DomainTree`、`LoadDomainDecls`、`EntityLookup`；新增装配类型/函数在 `graph/codegraph`，CLI 仍通过 `graphPrintJSON` 输出 stdout JSON，直接依赖锁定 Cobra v1.10.2。
- 2026-08-25：`rg -n 'Snippet|snippet'` 实读确认 Go `TestRef.Snippet`、webui `CgTestRef.snippet`、`DetailPanel.tsx` 渲染分支、`DetailPanel.test.tsx` fixture 与 `graph/codegraph/testdata/repo/codegraph/baseline.json` 旧输入存在；契约要求删 provider/consumer 新输出链，旧 JSON 输入仍由标准反序列化忽略。
- 2026-08-25：CLI 现状实读确认 `graphQueryRunE` 在 `graph/cli/cli.go#graphQueryRunE` 直接调用 `CheckStale(graphRepo, g)` 全图，`graph/codegraph/query.go#Neighborhood` 的结果边收集未跳过 deleted，命令注册在 `#init`，命令计数断言 `graph/cli/cli_test.go#TestGraphCommandCountIncludesMigrate` 固定为 14。
- 2026-08-25：现状源码窗口入口 `graph/codegraph/sym.go#ReAnchor`、领域/声明/实体入口与 contract §2 均能圈入 `graph/codegraph`；`merge.go#View` 当前不带 `Packages`，契约要求 context 从 CLI 已加载的 `*Graph` 读取 `Graph.Packages`，不新增加载路径。
- 2026-08-25：`skills/spec/SKILL.md` 与 `skills/plan/SKILL.md` 当前“有图先查图”菜单仍只列旧 `sym / who-calls / chain / domains`；契约 §7 要求同步新默认、`chain --with-source`、`context <领域>`、视图词表优先与未声明降级。
- 2026-08-25：实读 `skills/defect-families/SKILL.md:10-30`；本项目未发现额外项目清单文件，本稿采用基线通用五族，并追加回答序列化边界、枚举白名单、承重安全属性三族。
- 2026-08-25：`graph/webui/package.json` 实读确认 viewer 的独立验收命令为 `npm test`（Vitest 4.1.10）、`npm run typecheck`（`tsc -b`）与 `npm run build`（`tsc -b && vite build`）；本稿将其作为边界型子系统机内契约验收，浏览器/反代行为列真机清单。
- 2026-08-25：按拆解纪律将边界澄清回写 `docs/superpowers/specs/c1.5-contract.md:10`：webui 是既有 CLI stdout JSON wire 的边界型消费者；`AssembleResult`/`ExtractSourceWindow`/`AssembleContext` 是包内 API；skills 文档是交付物而非运行时子系统；三条均不新增跨子系统契约面。
- 2026-08-25：法定拆解稿已写入 `docs/superpowers/specs/c1.5-breakdown.md`，`wc -l` 原始输出为 `292`；包含待拍板清单、三运行时子系统人工资格核验、逐条契约核对、A~E 子卡/DAG、逐族缺陷审查、6 条真机清单与交稿自检。
- 2026-08-25：当前 `git status --short --branch` 原始输出为 `## cards/C1.5-charter-2`、契约与台账修改、拆解稿未跟踪；`git diff --check` 原始输出为空、退出码 `0`；占位符扫描 `rg -n 'TBD|TODO|待定|同上|适当处理|无风险' ...` 原始输出为空。
- 2026-08-25：拆解形状自检 `rg` 原始输出确认 A/B/C/D/E 五张卡各有 ①契约引用、②意图与为什么、③验收、④入口指针；每个缺陷族标题在五卡中各出现 5 次，未命中 TBD/TODO/待定/同上/适当处理/无风险。
- 2026-08-25：契约与 spec 头部复读确认 spec `:3-5` 为「已批准」、契约 `:3-6` 为「随本提交冻结」且无项目级目标图；契约新增 §10 仅记录不改变冻结语义的边界澄清。
- 2026-08-25：收口前执行 `git diff --check && git status --short --branch`；原始输出为空检查结果，状态为 `## cards/C1.5-charter-2`，改动为契约、拆解稿与台账三项。
- 2026-08-25：执行 `git add docs/superpowers/specs/c1.5-breakdown.md docs/superpowers/specs/c1.5-contract.md docs/ledgers/2026-08-24-codegraph-context-assembler-ledger.md` 成功，原始输出为空、退出码 `0`。
- 2026-08-25：再次暂存台账后执行 `git diff --cached --check && git diff --cached --stat && git status --short --branch`；原始检查输出为空、暂存统计为 3 文件/317 行，状态为契约 M、拆解稿 A、台账 M，分支 `cards/C1.5-charter-2`。
- 2026-08-25：执行 `git commit -m 'docs(breakdown): C1.5 context assembler decomposition'` 成功；原始输出为 `[cards/C1.5-charter-2 39be4ee] docs(breakdown): C1.5 context assembler decomposition`、`3 files changed, 318 insertions(+)`，法定拆解稿、契约边界澄清与台账已入库。
- 2026-08-25：执行 `git add docs/ledgers/2026-08-24-codegraph-context-assembler-ledger.md && git commit -m 'docs(ledger): record C1.5 breakdown'` 成功；原始输出为 `[cards/C1.5-charter-2 d3408c7] docs(ledger): record C1.5 breakdown`、`1 file changed, 1 insertion(+)`。
- 2026-08-25：最终审计命令 `git status --short --branch && git log -2 --oneline --decorate && git diff --check HEAD~2..HEAD && git show --check --oneline --summary HEAD && test -f ...` 成功；原始状态为 `## cards/C1.5-charter-2`，最近提交为 `d3408c7` 与 `39be4ee`，检查无输出且三个法定文件均存在。
- 2026-08-25：本轮执行 `sed -n '1,240p' /root/.codex/skills/handoff/SKILL.md` 与 `sed -n '241,520p' ...`，原始输出覆盖技能文件全部 374 行；遵守平台不变量，未调用 handoff CLI、未派发或启动子任务。
- 2026-08-25：`git status --short --branch && rg --files ...` 原始输出为 `## cards/C1.5-charter-4`，工作树无改动；计划、契约、拆解、spec 与 C1.5 台账文件均存在。
- 2026-08-25：并行实读 C1.5 台账、计划、契约/拆解/spec；计划已冻结 A～E 实现顺序，当前 D 步骤未包含“npm run build 后提交 dist 并删除旧 hash 资产”的补充要求，本轮将在 D 前回写计划。
- 2026-08-25：按协调者补充要求用 `apply_patch` 更新计划 D：先执行仓内 `npm ci`，构建成功后重建并提交 `dist/`、显式清理旧 hash 资产；失败时原样记账并标注 webui/dist 未验证。
- 2026-08-25：首次 A 缝测试命令 `cd graph && go test ./codegraph -run 'Test(AssembleResult|ExtractSourceWindow|NeighborhoodSkipsDeletedOutputEdges|RefSnippetWireRemoval)' -count=1` 失败，退出码 `1`；原始编译红包含 `undefined: AssembleResult`、`undefined: QueryOptions`、`undefined: AssembledNode`、`undefined: AssembledItem`，证明新接缝符号缺席，尚未进入实现。
- 2026-08-25：补空壳后重跑同一 A 缝命令，退出码 `1`；原始断言红为 `compact result` 空装配结果，随后 `TestAssembleResultSourceAndBudget` 因空 Nodes 在 `assemble_test.go:62` 发生 `panic: runtime error: index out of range [0] with length 0`。这确认空壳已编译且行为断言未被空壳满足，进入最小实现阶段。
- 2026-08-25：A 实现首次 `gofmt ... && cd graph && go test ...` 未通过，退出码 `1`；原始编译报错为 `codegraph/assemble.go:138:2: declared and not used: seenExternal`。删除未使用局部变量后重跑，源码窗口测试先发现 fixture 末尾换行被计为空行，随即修正为忽略末尾空行。
- 2026-08-25：A 缝命令修正后重跑，`cd graph && go test ./codegraph -run 'Test(AssembleResult|ExtractSourceWindow|NeighborhoodSkipsDeletedOutputEdges|RefSnippetWireRemoval)' -count=1` 原始输出为 `ok github.com/Xsxdot/charter/graph/codegraph 0.002s`，退出码 `0`；日志含 assemble start/completed、source window attached、budget truncated 事件。
- 2026-08-25：Task A 收尾命令 `cd graph && go build ./... && go vet ./... && go test ./codegraph -count=1` 成功，原始输出仅为 `ok github.com/Xsxdot/charter/graph/codegraph 0.021s`，退出码 `0`；全量编译、vet 与触及包测试均已跑到。
- 2026-08-25：A 变异自验：阈值改 2 后 `go build ./...` 成功且 `TestAssembleResultSharedUtilNeedsThreeDomains` 断言红；恢复阈值后该测试绿。deleted-edge 输出过滤改为 `if false` 后编译成功且增强的重复 live/deleted 边测试红；恢复后绿。Full 关闭 source 变异编译成功且 `TestAssembleResultFullPreservesSource` 红；恢复后绿。droppedNodes 变异先因未使用 `idx` 编译红（不计数），改为可编译的 `DroppedNodes: idx` 后行为红，恢复后绿。所有变异均命中唯一上下文并已恢复。
- 2026-08-25：新增 B 接缝测试后执行 `cd graph && go test ./codegraph -run TestAssembleContext -count=1`，退出码 `1`；原始编译红包含 `undefined: AssembleContext`（4 处），确认 context 新接缝尚未落地。
- 2026-08-25：B 空壳红之后实现 `context.go`；`gofmt ... && cd graph && go test ./codegraph -run TestAssembleContext -count=1` 原始输出为 `ok github.com/Xsxdot/charter/graph/codegraph 0.002s`，退出码 `0`。
- 2026-08-25：Task B 收尾命令 `cd graph && go build ./... && go vet ./... && go test ./codegraph -run 'Test(AssembleContext|AssembleResult|ExtractSourceWindow|NeighborhoodSkipsDeletedOutputEdges|RefSnippetWireRemoval)' -count=1` 成功，原始输出为 `ok github.com/Xsxdot/charter/graph/codegraph 0.004s`，退出码 `0`。
- 2026-08-25：B 变异自验：父域接口条件改为内部边后 `go build ./...` 成功且 `TestAssembleContextBestOnlyErrorAndParentBoundary` 红；未分种 model 纳入实体后同测试红；focus quota 改 6 后 `TestAssembleContextFocusQuota` 红；三次均恢复后 `go build ./... && go test ./codegraph -run TestAssembleContext -count=1` 原始输出 `ok github.com/Xsxdot/charter/graph/codegraph 0.002s`，退出码 `0`。
- 2026-08-25：新增 C 接缝测试并把命令数断言改为 15；`cd graph && go test ./cli -run 'TestGraph(CLIQueryFlagsAndJSONWire|CommandCountIncludesMigrate)' -count=1` 退出码 `1`，原始断言为 `codegraph 业务子命令数=14，want 15` 与 `unknown flag: --with-source`，确认 CLI 新 wire 尚未接线。
- 2026-08-25：C 接线后 targeted `cd graph && go test ./cli -run 'TestGraph(CLIQueryFlagsAndJSONWire|CommandCountIncludesMigrate)' -count=1` 原始输出为 `ok github.com/Xsxdot/charter/graph/cli 0.003s`，退出码 `0`；随后完整 CLI 包测试首次只因旧默认节点数断言仍为 3 而红，按新默认外部领域折叠更新为 2。
- 2026-08-25：C 收尾命令 `cd graph && go build ./... && go vet ./... && go test ./cli -count=1` 原始输出为 `ok github.com/Xsxdot/charter/graph/cli 0.177s`，退出码 `0`；真实 `New().Execute()` stdout JSON、context、reset、stale query subset 均跑到。
- 2026-08-25：C 变异自验：context `ExactArgs` 改 `MinimumNArgs` 后编译成功且多参数测试红；reset 保留 `graphWithSource=true` 后编译成功且同进程默认 compact 测试红；stale 改传全图后编译成功且 query-subset 测试红；`graphPrintJSON` 改为无缩进 `json.NewEncoder` 并把 query 未使用 g 修为 `_` 后编译成功且缩进 wire 测试红。四次均恢复并复跑 targeted green。
- 2026-08-25：E 前置查询 `rg -n 'context <领域>|--with-source|--full|视图词表|未声明|truncated|fociTruncated|sym / who-calls / chain / domains' skills/spec/SKILL.md skills/plan/SKILL.md` 原始命中仅为 `skills/spec/SKILL.md:15` 旧菜单与 `skills/plan/SKILL.md:49/51` 纪律示例；已将新命令、默认、词表差异、未声明降级、两类截断同步写入两份文档。复跑同关键词 rg 与 `git diff --check -- skills/spec/SKILL.md skills/plan/SKILL.md`，退出码 `0`、检查无输出。
- 2026-08-25：D 依赖安装命令 `npm ci`（工作目录 `graph/webui`）成功，退出码 `0`；原始输出为 `added 168 packages, and audited 169 packages in 1s`、`found 0 vulnerabilities`，未修改 manifest。
- 2026-08-25：D 安装后基线门禁均成功：`npm test -- --run src/app/codegraph/DetailPanel.test.tsx` 原始输出 `Test Files 1 passed (1)`、`Tests 1 passed (1)`；`npm run typecheck` 原始输出 `tsc -b`；`npm run build` 原始输出 Vite 6.4.3 构建 `index-CkVmOGdI.js`/`index-DhibVEn-.css`，三者退出码均 `0`。
- 2026-08-25：D 先加行为断言 `expect(screen.queryByText('assert')).toBeNull()` 后重跑指定 Vitest，退出码 `1`；原始断言红为 `expected <pre …(1)></pre> to be null`，实际 DOM 为 `<pre ...>assert</pre>`，确认 snippet 消费分支确实被命中。
- 2026-08-25：D 生产 wire 收缩后，指定 Vitest、`npm run typecheck`、`npm run build` 均退出码 `0`；原始输出分别为 `Test Files 1 passed (1)`/`Tests 1 passed (1)`、`tsc -b`、Vite 6.4.3 生成 `dist/assets/index-_D4Y5DxT.js` 与既有 CSS。构建状态实读为旧 `index-CkVmOGdI.js` 删除、新 hash 未跟踪、`dist/index.html` 更新，后续将显式暂存删除与新资产。
- 2026-08-25：集成门禁命令均成功：`cd graph && go test ./...` 原始输出为 cli `ok ... 0.338s`、cmd 无测试、codegraph `ok ... 0.030s`、webui `ok ... 0.003s`；`cd graph && go vet ./...` 无输出退出码 `0`；`cd graph/webui && npm test -- --run` 原始输出 `Test Files 17 passed (17)`、`Tests 108 passed (108)`；`npm run typecheck` 原始输出 `tsc -b`，均退出码 `0`。
- 2026-08-25：补充导出 API 注释、空源码文件判断和装配参数错误日志后，重跑 `cd graph && go test ./...` 原始输出为 cli `ok ... 0.427s`、cmd 无测试、codegraph `ok ... 0.046s`、webui cached；`cd graph && go vet ./...` 无输出退出码 `0`；webui `npm test -- --run && npm run typecheck` 原始输出 `Test Files 17 passed (17)`、`Tests 108 passed (108)`、`tsc -b`，均退出码 `0`。
- 2026-08-25：D 收尾再次执行 `npm run build`（`graph/webui`）成功，退出码 `0`；原始 Vite 输出为 CSS `index-DhibVEn-.css`、JS `index-_D4Y5DxT.js`，与当前 dist 资产一致，旧 `index-CkVmOGdI.js` 保持删除。
- 2026-08-25：首次按收尾清单执行 `git add ... && git diff --cached --check ...` 失败，退出码 `128`；原始报错为 `fatal: Unable to create '/root/.handoff/repos/charter/.git/worktrees/762c199e/index.lock': Read-only file system`。源码与 dist 未据此回退，待获 Git 元数据写权限后重试。
- 2026-08-25：获 Git 元数据写权限后重试暂存与检查成功，退出码 `0`；`git diff --cached --check` 无输出，暂存统计为 20 个文件/1230 行新增、31 行删除；`graph/webui/dist` 的旧 `index-CkVmOGdI.js` 已被新 hash `index-_D4Y5DxT.js` 替换，工作树状态仅显示当前分支 staged 改动。
- 2026-08-25：执行 `git commit -m 'feat(codegraph): add compact context assembler CLI'` 成功，原始输出为 `[cards/C1.5-charter-4 358b603] feat(codegraph): add compact context assembler`、`20 files changed, 1231 insertions(+), 31 deletions(-)`；提交包含 A～E 实现、测试、计划/说明同步与 dist hash 替换，未 push。
- 2026-08-25：提交后审计：`git status --short --branch` 原始输出为 `## cards/C1.5-charter-4` 加台账单文件未提交修改；`git log -2 --oneline --decorate` 原始输出为 `358b603 (HEAD -> cards/C1.5-charter-4) feat(codegraph): add compact context assembler CLI` 与 `4b663db (cards/C1.5-charter-3) docs(ledger): finalize C1.5 plan audit`；`git diff --check HEAD` 与 `git show --check --oneline --summary HEAD` 均无检查输出/退出码 `0`；已提交 dist 文件为 CSS `index-DhibVEn-.css`、JS `index-_D4Y5DxT.js`、字体与 `index.html`，无旧 hash 孤儿文件。
- 2026-08-25：执行 `git add docs/ledgers/2026-08-24-codegraph-context-assembler-ledger.md && git diff --cached --check && git commit -m 'docs(ledger): record C1.5 implementation audit'` 成功，原始输出为 `[cards/C1.5-charter-4 cca946f] docs(ledger): record C1.5 implementation audit`、`1 file changed, 2 insertions(+)`。
- 2026-08-25：按 Critical 修复要求仅新增 `TestAssembleResultFullDisablesFolding`，并把既有 Full 投影用例显式设为 `FoldExternal:true`、`CollapseUtil:true`；测试覆盖 `Full=true` 与 CLI 默认折叠同时开启时逐个保留 raw 节点且不产生 `representatives/count` wire 形态，尚未修改生产代码。
- 2026-08-25：执行 `cd graph && go test ./codegraph -run 'TestAssembleResult(DefaultProjectionAndFull|FullDisablesFolding)' -count=1` 得到行为红，退出码 `1`；原始失败为 `assemble_test.go:41: full projection did not restore fields: <nil>` 与 `assemble_test.go:69: full output contains folded external item`，日志显示 `full=true ... rawNodes=5 ... outputNodes=3`，确认测试命中现有折叠路径而非 typo。
- 2026-08-25：在 `AssembleResult` 增加最小短路：`foldExternal := opts.FoldExternal && !opts.Full`、`collapseUtil := opts.CollapseUtil && !opts.Full`，仅阻断两条折叠判定，不触碰 raw BFS 或 deleted-edge 过滤；随后进入格式化与定向复验。
- 2026-08-25：执行 `gofmt -w codegraph/assemble.go codegraph/assemble_test.go && cd graph && go test ./codegraph -run 'TestAssembleResult(DefaultProjectionAndFull|FullDisablesFolding)' -count=1` 成功；原始输出为 `ok github.com/Xsxdot/charter/graph/codegraph 0.002s`，退出码 `0`。
- 2026-08-25：变异前用 `rg` 与计数断言确认 `foldExternal := opts.FoldExternal && !opts.Full` 和 `collapseUtil := opts.CollapseUtil && !opts.Full` 各命中唯一一次，退出码 `0`；两条变异锚点均可安全替换。
- 2026-08-25：开始 Full 外部折叠变异：唯一替换 `foldExternal := opts.FoldExternal && !opts.Full` → `foldExternal := opts.FoldExternal`，生产语义暂为变异态，尚未恢复。
- 2026-08-25：外部折叠变异先执行 `cd graph && go build ./...`，原始输出为空、退出码 `0`；该变异编译通过，进入行为断言。
- 2026-08-25：外部折叠变异执行 `cd graph && go test ./codegraph -run '^TestAssembleResultFullDisablesFolding$' -count=1` 转红，退出码 `1`；原始失败为 `assemble_test.go:69: full output contains folded external item`，日志为 `full=true ... rawNodes=5 ... outputNodes=3`，证明该测试拦住外部折叠短路缺失。
- 2026-08-25：外部折叠变异已用 `apply_patch` 恢复为 `foldExternal := opts.FoldExternal && !opts.Full`，开始下一条共享工具折叠变异。
- 2026-08-25：开始 Full 共享工具折叠变异：唯一替换 `collapseUtil := opts.CollapseUtil && !opts.Full` → `collapseUtil := opts.CollapseUtil`，生产语义暂为变异态，尚未恢复。
- 2026-08-25：共享工具折叠变异先执行 `cd graph && go build ./...`，原始输出为空、退出码 `0`；该变异编译通过，进入同一行为断言。
- 2026-08-25：共享工具折叠变异执行 `cd graph && go test ./codegraph -run '^TestAssembleResultFullDisablesFolding$' -count=1` 原始输出为 `ok github.com/Xsxdot/charter/graph/codegraph 0.002s`、退出码 `0`；这不是存活结论，现有 fixture 没有三领域共享节点，故该守卫尚未被该用例打中，需补专门覆盖后重做变异。
- 2026-08-25：在共享工具短路仍处于变异态时新增 `TestAssembleResultFullDisablesSharedUtilityFolding`，显式构造三个不同 caller 领域、一个共享节点及其下游，覆盖 `Full + FoldExternal + CollapseUtil` 的 raw 对齐与 union 排除。
- 2026-08-25：共享工具变异复跑 `gofmt -w codegraph/assemble_test.go && cd graph && go test ./codegraph -run '^TestAssembleResultFullDisables(SharedUtility)?Folding$' -count=1` 转红，退出码 `1`；原始失败为 `assemble_test.go:133: full output node count=4, raw=5: map[caller-a:true caller-b:true caller-c:true shared:true]`，日志为 `full=true ... rawNodes=5 ... outputNodes=4`，证明新用例命中共享工具短路缺失。
- 2026-08-25：共享工具折叠变异已用 `apply_patch` 恢复为 `collapseUtil := opts.CollapseUtil && !opts.Full`；两条可编译变异均已完成行为红与源码恢复。
- 2026-08-25：恢复后执行 `cd graph && go test ./codegraph -run '^TestAssembleResult(DefaultProjectionAndFull|FullDisables(SharedUtility)?Folding|FullPreservesSource)$' -count=1` 成功；原始输出为 `ok github.com/Xsxdot/charter/graph/codegraph 0.003s`，退出码 `0`。
- 2026-08-25：Task A 修复收尾并行执行 `cd graph && go build ./...`、`go vet ./...`、`go test ./codegraph -count=1`；原始结果分别为 build 空输出/退出码 `0`、vet 空输出/退出码 `0`、`ok github.com/Xsxdot/charter/graph/codegraph 0.032s`/退出码 `0`。
- 2026-08-25：集成 Go 门禁执行 `cd graph && go test ./...` 与 `go vet ./...`；原始 `go test` 输出为 cli `ok github.com/Xsxdot/charter/graph/cli 0.396s`、cmd `[no test files]`、codegraph `ok github.com/Xsxdot/charter/graph/codegraph 0.020s`、webui `ok github.com/Xsxdot/charter/graph/webui 0.001s`，退出码 `0`；`go vet ./...` 无输出、退出码 `0`。
- 2026-08-25：收尾审计执行 `gofmt -d graph/codegraph/assemble.go graph/codegraph/assemble_test.go && git diff --check && git status --short --branch && git diff --stat`；gofmt/diff check 均无输出，状态为 `## cards/C1.5-charter-5`，改动仅为台账、`assemble.go`、`assemble_test.go`，统计 `3 files changed, 116 insertions(+), 4 deletions(-)`。
- 2026-08-25：首次执行 `git add docs/ledgers/2026-08-24-codegraph-context-assembler-ledger.md graph/codegraph/assemble.go graph/codegraph/assemble_test.go && git diff --cached --check && git diff --cached --stat && git status --short --branch` 失败，退出码 `128`；原始报错为 `fatal: Unable to create '/root/.handoff/repos/charter/.git/worktrees/330c2600/index.lock': Read-only file system`，未据此回退源码，需用获准的 Git 元数据写权限重试。
- 2026-08-25：使用获准的 Git 元数据写权限重试暂存、`git diff --cached --check`、暂存统计与状态检查成功；原始暂存统计为 `3 files changed, 118 insertions(+), 4 deletions(-)`，状态为三文件 staged、分支 `cards/C1.5-charter-5`。
- 2026-08-25：执行 `git add docs/ledgers/2026-08-24-codegraph-context-assembler-ledger.md graph/codegraph/assemble.go graph/codegraph/assemble_test.go && git diff --cached --check && git commit -m 'fix(codegraph): honor full mode over folding'` 成功；原始输出为 `[cards/C1.5-charter-5 b4ecdec] fix(codegraph): honor full mode over folding`、`3 files changed, 119 insertions(+), 4 deletions(-)`，未 push。
- 2026-08-25：首个修复提交后审计执行 `git status --short --branch && git diff --check && git log -1 --oneline --decorate`；原始状态为 `## cards/C1.5-charter-5` 加台账单文件未提交修改，diff check 无输出，HEAD 为 `b4ecdec (HEAD -> cards/C1.5-charter-5) fix(codegraph): honor full mode over folding`。
- 2026-08-25：执行 `git add docs/ledgers/2026-08-24-codegraph-context-assembler-ledger.md && git diff --cached --check && git commit -m 'docs(ledger): record Full folding repair'` 成功；原始输出为 `[cards/C1.5-charter-5 1f555e4] docs(ledger): record Full folding repair`、`1 file changed, 2 insertions(+)`。
- 2026-08-25：台账提交后审计执行 `git status --short --branch && git diff --check && git log -3 --oneline --decorate`；原始状态仅为 `## cards/C1.5-charter-5`，diff check 无输出，最近提交为 `1f555e4`、`b4ecdec`、`5821c05`。
- 2026-08-25：按任务纪律完整读取 `/root/.codex/skills/handoff/SKILL.md`；本执行回合不调用 handoff CLI、不派发 executor、不切换分支。
- 2026-08-25：现场检查确认工作目录为 `/root/.handoff/worktrees/8c8cbd7a`，当前分支为 `cards/C1.5-review-1`，初始 `git status --short --branch` 原始输出仅为 `## cards/C1.5-review-1`。
- 2026-08-25：文件检索确认 C1.5 专用台账为 `docs/ledgers/2026-08-24-codegraph-context-assembler-ledger.md`，后续事实逐条追加于该文件。
- 2026-08-25：计划标题检索确认本卡包含 Task A 结果装配、Task B context、Task C CLI、Task D webui、Task E skills 文档，以及协调者真机验收；计划明确 A→B→C，D/E 在 C 后完成。
- 2026-08-25：当前 HEAD 为 `302ee00 (docs(ledger): finalize Full folding repair)`，提交历史包含 `358b603 (feat(codegraph): add compact context assembler CLI)` 与后续 Full 折叠修复；本回合开始时除台账外无工作树改动。
- 2026-08-25：现有实现检索确认 `graph/codegraph/assemble.go`、`context.go`、相关测试和 CLI context/flag 测试已存在；当前需基于计划与实际测试继续审计/修补，不能把存在文件当作验收通过。
- 2026-08-25：执行 `cd graph && go test ./...` 成功，原始输出为 `ok github.com/Xsxdot/charter/graph/cli 0.165s`、`[no test files]`（cmd/codegraph）、`ok github.com/Xsxdot/charter/graph/codegraph 0.021s`、`ok github.com/Xsxdot/charter/graph/webui 0.001s`，退出码 `0`。
- 2026-08-25：执行 `cd graph && go vet ./...` 成功，原始输出为空，退出码 `0`。
- 2026-08-25：执行 codegraph 定向命令 `go test ./codegraph -run 'Test(AssembleResult|ExtractSourceWindow|NeighborhoodSkipsDeletedOutputEdges|RefSnippetWireRemoval|AssembleContext)' -count=1` 成功，原始输出为 `ok github.com/Xsxdot/charter/graph/codegraph 0.008s`，退出码 `0`。
- 2026-08-25：执行 CLI 定向命令 `go test ./cli -run 'TestGraph(CLIQueryFlags|Context|CommandCount|StaleSubset|JSONWire)' -count=1` 成功，原始输出为 `ok github.com/Xsxdot/charter/graph/cli 0.003s`，退出码 `0`。
- 2026-08-25：执行 `cd graph/webui && npm test -- --run src/app/codegraph/DetailPanel.test.tsx` 失败，退出码 `127`，原始报错为 `sh: 1: vitest: not found`；执行 `npm run typecheck` 失败，退出码 `127`，原始报错为 `sh: 1: tsc: not found`；执行 `npm run build` 失败，退出码 `127`，原始报错为 `sh: 1: tsc: not found`。
- 2026-08-25：逐段审读实现与契约发现待修行为：共享节点可能先被外部领域折叠、外部代表可能附带 source、共享节点下游无视未收桩的替代路径、代表入边计数未限于本次 raw 结果；context 实体遍历按 map 顺序后按 Query 排序，未按契约的节点 id 顺序；常量注释未写冻结的基线取舍数字。以上均已登记为本回合实现项，尚未修改源码。
- 2026-08-25：新增四条行为测试并执行 `gofmt -w graph/codegraph/assemble_test.go graph/codegraph/context_test.go && cd graph && go test ./codegraph -run 'TestAssembleResult(SharedUtilityWinsExternalFoldAndRepsHaveNoSource|KeepsDownstreamWithUncollapsedAlternatePath|ExternalRepresentativesUseRawIncomingEdges)|TestAssembleContextEntitiesFollowNodeIDOrder' -count=1`；退出码 `1`。原始失败为：`assemble_test.go:253: shared utility must remain an ordinary sourced item: <nil>`、`assemble_test.go:306: alternate uncollapsed path must retain child: map[alt:true focus:true shared:true]`、`assemble_test.go:339: representatives must rank raw-result incoming edges, got "one"`；实体顺序测试未失败。
- 2026-08-25：按红测实现最小修补：共享节点绕过 external fold；external representatives 禁止附 source；共享 utility 下游只在无未收桩替代路径时省略；代表入边仅统计 raw 结果内 live caller；常量旁补齐冻结的基线取舍注释。未修改 context 生产代码，实体顺序测试已证明现有 `EntityLookup(id)` 后按 `Query`（即 id）排序符合契约。
- 2026-08-25：修补后执行同一组四条定向测试成功，原始输出为 `ok github.com/Xsxdot/charter/graph/codegraph 0.006s`，退出码 `0`。
- 2026-08-25：一次格式化命令误含不存在路径 `graph/assemble_test.go`，`gofmt` 退出码 `2`，原始报错为 `lstat graph/assemble_test.go: no such file or directory`；该命令不作为格式化成功结论。
- 2026-08-25：随后并行执行 `cd graph && go test ./...` 成功（cli `0.166s`、cmd 无测试、codegraph `0.019s`、webui cached，退出码 `0`）、`cd graph && go vet ./...` 成功（空输出、退出码 `0`）、`git diff --check` 成功（空输出、退出码 `0`）。
- 2026-08-25：纠正路径后执行 `gofmt -w graph/codegraph/assemble.go graph/codegraph/assemble_test.go graph/codegraph/context.go graph/codegraph/context_test.go graph/cli/cli.go` 成功，原始输出为空，退出码 `0`。
- 2026-08-25：按 Task D 执行 `cd graph/webui && npm ci` 成功，原始输出为 `added 168 packages, and audited 169 packages in 1s`、`30 packages are looking for funding`、`found 0 vulnerabilities`，退出码 `0`；未修改 manifest。
- 2026-08-25：`npm test -- --run src/app/codegraph/DetailPanel.test.tsx` 在安装依赖后成功，原始结果为 `Test Files 1 passed`、`Tests 1 passed`、Vitest `v4.1.11`，退出码 `0`；`npm run typecheck` 成功，原始输出仅为脚本头与 `tsc -b`，退出码 `0`；`npm run build` 成功，原始输出含 `45 modules transformed`、dist CSS `20.00 kB`、JS `265.66 kB`、`built in 717ms`，退出码 `0`。
- 2026-08-25：webui 构建后执行 `git status --short --branch && git diff --stat && git diff --check`；原始状态为分支 `cards/C1.5-review-1` 加六个已跟踪修改文件（台账、CLI、assemble、assemble_test、context、context_test），统计 `6 files changed, 275 insertions(+), 31 deletions(-)`；diff check 无输出。
- 2026-08-25：开始可编译变异 1，将 shared utility 绕过 external fold 的唯一条件 `!sharedTargets[rn.ID]` 移除；生产源码暂处变异态，尚未恢复。
- 2026-08-25：变异 1 执行 `cd graph && go test ./codegraph -run '^TestAssembleResultSharedUtilityWinsExternalFoldAndRepsHaveNoSource$' -count=1` 转红，退出码 `1`；原始失败为 `assemble_test.go:253: shared utility must remain an ordinary sourced item: <nil>`，日志显示装配结果 `outputNodes=2`。已用 `apply_patch` 恢复唯一条件。
- 2026-08-25：开始可编译变异 2，将 external representative 的 `WithSource=false` 改为 `true`；生产源码暂处变异态，尚未恢复。
- 2026-08-25：变异 2 执行同一 external/shared 测试转红，退出码 `1`；原始失败为 `assemble_test.go:260: folded representative must not carry source`，日志显示四次 source window attached。已用 `apply_patch` 恢复 `WithSource=false`。
- 2026-08-25：开始可编译变异 3，移除 `!withoutShared[id]` 替代路径保护；生产源码暂处变异态，尚未恢复。
- 2026-08-25：变异 3 执行 `cd graph && go test ./codegraph -run '^TestAssembleResultKeepsDownstreamWithUncollapsedAlternatePath$' -count=1` 转红，退出码 `1`；原始失败为 `assemble_test.go:306: alternate uncollapsed path must retain child`，节点集合为 `map[alt:true focus:true shared:true]`。已用 `apply_patch` 恢复替代路径保护。
- 2026-08-25：开始可编译变异 4，移除 external representatives 入边计数的 `within[e.From]` raw 结果限制；生产源码暂处变异态，尚未恢复。
- 2026-08-25：变异 4 执行 `cd graph && go test ./codegraph -run '^TestAssembleResultExternalRepresentativesUseRawIncomingEdges$' -count=1` 转红，退出码 `1`；原始失败为 `assemble_test.go:339: representatives must rank raw-result incoming edges, got "one"`。已用 `apply_patch` 恢复 raw 结果限制；四条新增变异均已红且源码恢复。
- 2026-08-25：更正早先的 context 实体排序疑点：新增 `a_model`/`z_model` 测试通过，因 `AssembleContext` 按节点 id 调 `EntityLookup` 且 `EntityResult.Query` 接收该 id，现有 `sort.Slice(... Query)` 实际等价于 id 字典序；无需生产修补。
- 2026-08-25：变异恢复后执行 `gofmt -w graph/codegraph/assemble.go graph/codegraph/assemble_test.go graph/codegraph/context.go graph/codegraph/context_test.go graph/cli/cli.go` 成功，空输出/退出码 `0`；`cd graph && go test ./codegraph -count=1` 成功，原始输出 `ok github.com/Xsxdot/charter/graph/codegraph 0.029s`；CLI 定向测试成功，原始输出 `ok github.com/Xsxdot/charter/graph/cli 0.004s`；Go 全量测试成功（cli cached、cmd 无测试、codegraph cached、webui cached，退出码 `0`）；`go vet ./...` 空输出/退出码 `0`。
- 2026-08-25：新增同进程 flag reset 回归并执行 `gofmt -w graph/cli/cli_test.go && cd graph && go test ./cli -run '^TestGraphQueryFlagsResetBeforeWhoCallsAndContext$' -count=1` 成功，原始输出为 `ok github.com/Xsxdot/charter/graph/cli 0.003s`，退出码 `0`；该测试先执行全 flags chain，再验证 who-calls 不继承 source/full，context 默认保留 source。
- 2026-08-25：执行 Task E 关键词审计成功，`skills/spec/SKILL.md:15` 与 `skills/plan/SKILL.md:14` 均命中 context、with-source、full、视图词表、未声明、truncated、fociTruncated；`git diff --check -- skills/spec/SKILL.md skills/plan/SKILL.md` 与 whole diff check 均空输出/退出码 `0`。
- 2026-08-25：执行 Snippet 消费面审计成功，命中仅为 Go 兼容性测试中的 `snippet` 输入/断言；`graph/webui/src`、`graph/webui/dist/index.html` 与当前 dist JS 未发现消费/渲染引用。
- 2026-08-25：图文件检索仅找到 `graph/codegraph/testdata/repo/codegraph/baseline.json` 与 fixture `best.json`，未找到当前仓根级真实 handoff baseline；计划要求的真实基线字节数与四种 context 真机查询本回合未验证，不能用 fixture/历史 81409、76102 代替。
- 2026-08-25：提交前 `git diff --name-only HEAD` 原始输出为台账、`graph/cli/cli.go`、`graph/cli/cli_test.go`、`graph/codegraph/assemble.go`、`assemble_test.go`、`context.go`、`context_test.go` 七个文件；`git status --short --branch` 显示当前分支 `cards/C1.5-review-1` 上上述七个已跟踪修改。
- 2026-08-25：复核计划 A 的源码窗口条目发现现实现每个节点仍经 `ReAnchor` 与 `ExtractSourceWindow` 重复读取文件，`AssembleResult` 尚无按文件行缓存；本回合将补包内缓存，不改变冻结导出签名。
- 2026-08-25：在 `source.go` 增加包内 `sourceFileCache`/行窗口 helper，并让 `AssembleResult` 在一次装配中复用同文件行数组；导出 `ExtractSourceWindow` 签名未变。执行 gofmt、定向 `Test(AssembleResult|ExtractSourceWindow|RefSnippetWireRemoval)`、`go test ./...`、`go vet ./...` 均成功；原始测试输出分别为 `ok .../codegraph 0.009s`、Go 全量 cli `0.186s`/cmd 无测试/codegraph `0.023s`/webui cached，vet 空输出，均退出码 `0`。
- 2026-08-25：修复显式 `source-span=0` 静默回落：`AssembleResult` 在 `WithSource` 下拒绝 0，CLI chain/context 在 options 阶段统一校验 1..200 与非负 token；新增 CLI 回归断言。执行 gofmt、CLI query option 定向测试、source span codegraph 定向测试、`go test ./...`、`go vet ./...` 均成功；原始输出分别为 CLI `ok .../cli 0.017s`、codegraph `ok .../codegraph 0.004s`、全量 cli `0.163s`/cmd 无测试/codegraph `0.021s`/webui cached、vet 空输出，均退出码 `0`。
- 2026-08-25：最终静态审计执行 `git diff --check` 成功（空输出/退出码 `0`）；变更路径仅为台账、CLI/CLI 测试、assemble/assemble 测试、context/context 测试、source.go 八个文件；契约符号与冻结常量检索退出码 `0`；提交前状态仅列上述八个已跟踪修改，分支仍为 `cards/C1.5-review-1`。
- 2026-08-25：补跑完整 webui Vitest `cd graph/webui && npm test -- --run` 成功，原始输出为 `Test Files 17 passed (17)`、`Tests 108 passed (108)`、Vitest `v4.1.11`、`Duration 2.16s`，退出码 `0`。
- 2026-08-25：最终门禁并行执行成功：`cd graph && go test ./...` 原始输出为 cli cached、cmd `[no test files]`、codegraph cached、webui cached，退出码 `0`；`cd graph && go vet ./...` 空输出/退出码 `0`；完整 `cd graph/webui && npm test -- --run` 为 `Test Files 17 passed (17)`、`Tests 108 passed (108)`、退出码 `0`；`npm run typecheck` 脚本头加 `tsc -b`、退出码 `0`；`npm run build` 为 `45 modules transformed`、CSS `20.00 kB`、JS `265.66 kB`、`built in 688ms`、退出码 `0`；`git diff --check` 空输出/退出码 `0`。
- 2026-08-25：最终暂存前检查 `git status --short --branch && git diff --stat && git diff --check` 成功；分支为 `cards/C1.5-review-1`，8 个已跟踪文件修改，统计 `396 insertions(+), 44 deletions(-)`，diff check 无输出。
- 2026-08-25：首次执行显式 `git add` 失败，退出码 `128`；原始报错为 `fatal: Unable to create '/root/.handoff/repos/charter/.git/worktrees/8c8cbd7a/index.lock': Read-only file system`。未据此回退源码，待获 Git 元数据写权限后重试。
- 2026-08-25：获 Git 元数据写权限后重试显式 `git add` 成功，随后 `git diff --cached --check && git diff --cached --stat && git status --short --branch` 成功；cached diff check 无输出，暂存统计为 `8 files changed, 398 insertions(+), 44 deletions(-)`，8 个文件均 staged，分支 `cards/C1.5-review-1`。
- 2026-08-25：再次暂存台账后执行 `git diff --cached --check && git diff --cached --stat` 成功；cached diff check 无输出，统计为 `8 files changed, 399 insertions(+), 44 deletions(-)`。
