# C1.10 plan ledger

- 2026-08-25：已完整读取 `skills/handoff/SKILL.md`、`skills/plan/SKILL.md`、`skills/spec/SKILL.md`、`skills/contract/SKILL.md`、`skills/defect-families/SKILL.md`、`skills/using-charter/SKILL.md`；本轮为 L3 轻档 plan 节点，只产出计划，不调用 handoff CLI、不启动 executor/子任务。
- 2026-08-25：`git status --short --branch` 输出 `## cards/C1.10-charter-3`；`git log -3 --oneline --decorate` 输出 `6892418 (HEAD -> cards/C1.10-charter-3, cards/C1.10-charter-2) docs(ledger): close C1.10 breakdown audit`、`472d19b docs(ledger): record C1.10 final verification`、`cd493fd docs(ledger): record C1.10 breakdown commit`；当前工作树初始干净，未切分支。
- 2026-08-25：spec `docs/specs/2026-08-24-codegraph-viewer-semantics-spec.md` 第 3 行为 `状态：已批准`；contract `docs/superpowers/specs/c1.10-contract.md` 第 3 行为 `状态：已冻结（随本提交，2026-08-25）`；输入状态满足 plan 前置条件。
- 2026-08-25：当前 `docs/superpowers/plans/c1.10-plan.md` 不存在；本轮法定计划路径需新建。
- 2026-08-25：当前 `graph/webui/src/app/codegraph` 已有 `BestDetail.tsx`、`BestLeafGraph.tsx`、`DomainDetail.tsx`、`CodegraphPage.tsx`、`domainpage.ts` 及测试；未见 `BestDomainPage.tsx` 或 `DomainCascadeDrawer.tsx`，P1-B 命名裁决需在计划中落为新增文件并由 T4 接线。
- 2026-08-25：基线命令 `go test ./codegraph -run 'Test(LoadDomainDecls|ValidateDecls)$' -count=1`（workdir `graph`）退出 0，原始输出 `ok github.com/Xsxdot/charter/graph/codegraph 0.020s`；`go test ./cli -run 'TestGraphValidate(|ReportsDomainCount|DomainDeclIssuePrefix)$' -count=1` 退出 0，原始输出 `ok github.com/Xsxdot/charter/graph/cli 0.004s`；`go test ./... -count=1` 退出 0，原始输出 `ok github.com/Xsxdot/charter/graph/cli 0.161s`、`? github.com/Xsxdot/charter/graph/cmd/codegraph [no test files]`、`ok github.com/Xsxdot/charter/graph/codegraph 0.019s`、`ok github.com/Xsxdot/charter/graph/webui 0.002s`。
- 2026-08-25：基线前端命令 `npm run typecheck`、`npm test -- src/app/codegraph/domainpage.test.ts`、`npm test -- src/app/codegraph/BestDomainPage.test.tsx src/app/codegraph/DomainCascadeDrawer.test.tsx`、`npm test -- src/api/client.test.ts src/app/codegraph/CodegraphPage.test.tsx`、`npm test`（均在 `graph/webui`）均退出 127，原始错误分别为 `sh: 1: tsc: not found` 或 `sh: 1: vitest: not found`；未据此归因，需先按锁文件安装依赖再复核。
- 2026-08-25：基线 `git diff --check` 退出 0、无输出。
- 2026-08-25：执行 `npm ci`（workdir `graph/webui`）成功，原始输出 `added 168 packages, and audited 169 packages in 1s`、`30 packages are looking for funding`、`found 0 vulnerabilities`，退出 0；仅恢复锁文件依赖，未改受版本控制文件。
- 2026-08-25：依赖安装后，`npm run typecheck` 退出 0，原始脚本输出 `tsc -b`；`npm test -- src/app/codegraph/domainpage.test.ts` 退出 0，原始摘要 `Test Files 1 passed (1)`、`Tests 4 passed (4)`；`npm test -- src/api/client.test.ts src/app/codegraph/CodegraphPage.test.tsx` 退出 0，原始摘要 `Test Files 2 passed (2)`、`Tests 22 passed (22)`；`npm test` 退出 0，原始摘要 `Test Files 18 passed (18)`、`Tests 112 passed (112)`。
- 2026-08-25：P1-B 基线 focused 命令 `npm test -- src/app/codegraph/BestDomainPage.test.tsx src/app/codegraph/DomainCascadeDrawer.test.tsx` 退出 1，Vitest 原始输出 `No test files found, exiting with code 1`，过滤文件为两个尚不存在的新测试文件；这证明该测试缝需先由实现轮点亮，不把失败归因于实现。
- 2026-08-25：`rg -n "slog|log\\.|console\\.(debug|info|warn)|print\\(" ...` 结果显示 charter Go core/CLI 没有现成结构化 logger，viewer 使用既有 `console.debug/info/warn` 结构化对象日志（`client.ts`、`useCodegraph.ts`、`CodegraphPage.tsx` 等）；计划对纯函数保持无副作用，将入口/错误/成功日志放在组件与既有 CLI 输出边界，不引入 `print` 或新日志框架。
- 2026-08-25：`besttree.ts#containerFacts` 现状以节点 `file` 最后一个 `/` 前的目录作为包目录，并将多目录容器归为 `dir:''`；计划 T2 复用该已验证口径，按选定组织的节点所属容器筛选 `CgGraph.packages`，只原样投影 `dir/summary`。
- 2026-08-25：已落盘法定计划 `docs/superpowers/plans/c1.10-plan.md`，713 行；包含 P1-B `BestDomainPage` 命名、T1～T4 有界文件集、精确接口、基线命令/预期、红绿步骤、日志/注释步骤、缺陷族、序列化边界、spec 故事归属与真机清单。
- 2026-08-25：计划自审命令 `rg -n 'TBD|待定|同 Task|适当|等等' docs/superpowers/plans/c1.10-plan.md | rg -v '反占位符扫描已执行'` 无输出，原始结果 `placeholder_scan=clean (self-audit line excluded)`；`git diff --check` 无输出；计划文件仍为未跟踪文件，尚未暂存/提交。
- 2026-08-25：计划自审修正：`BestLeafGraph` 仅在 `organization === 'best' && best` 时渲染；current 组织和 best 缺席只显示 T2 当前结构，避免把 best 叶图误当 current 画布。该约束已写入 `BestDomainPage` 的完整组件块和文字判据。
- 2026-08-25：首次执行 `git add docs/superpowers/plans/c1.10-plan.md docs/ledgers/2026-08-25-c1.10-plan-ledger.md` 失败，原始报错为 `fatal: Unable to create '/root/.handoff/repos/charter/.git/worktrees/82e1ca5a/index.lock': Read-only file system`，退出码 128；按当前工作树既有权限规则请求升级权限重试。
- 2026-08-25：同一 `git add` 命令以升级权限重试成功，退出码 0；计划文件与台账已进入 index。随后本条台账追加使其工作树状态需再次暂存。
- 2026-08-25：升级权限执行 `git add docs/ledgers/2026-08-25-c1.10-plan-ledger.md && git diff --cached --check && git diff --cached --stat && git status --short --branch` 成功；`git diff --cached --check` 无输出，stat 为 `docs/ledgers/2026-08-25-c1.10-plan-ledger.md | 20 +`、`docs/superpowers/plans/c1.10-plan.md | 714 +++++++++++++++++++++++++++`，合计 2 files changed、734 insertions；状态为两文件 `A`。
- 2026-08-25：升级权限执行 `git add docs/ledgers/2026-08-25-c1.10-plan-ledger.md` 成功，退出码 0；随后 `git diff --cached --check && git commit -m "docs(plan): C1.10 viewer phase three implementation plan"` 成功，原始提交输出为 `[cards/C1.10-charter-3 a1334ec] docs(plan): C1.10 viewer phase three implementation plan`、`2 files changed, 735 insertions(+)`、两个文件均 `create mode 100644`；未 push。提交后本条台账追加需再提交一次。
- 2026-08-25：随后执行 `git diff --cached --check && git commit -m "docs(ledger): record C1.10 plan commit"` 成功，原始输出为 `[cards/C1.10-charter-3 39c9aa2] docs(ledger): record C1.10 plan commit`、`1 file changed, 1 insertion(+)`；最终复核 `git status --short --branch` 仅输出 `## cards/C1.10-charter-3`，`git log -3 --oneline --decorate` 输出 `39c9aa2`、`a1334ec`、`6892418`；`git diff --check HEAD~2..HEAD` 无输出，两个法定文件均 `required_files=present`。本条追加后需再提交台账。
- 2026-08-25：升级权限执行 `git add docs/ledgers/2026-08-25-c1.10-plan-ledger.md`、`git diff --cached --check && git commit -m "docs(ledger): record C1.10 final plan verification"` 成功；原始提交输出为 `[cards/C1.10-charter-3 61a69f1] docs(ledger): record C1.10 final plan verification`、`1 file changed, 1 insertion(+)`；未 push。本条追加后还需一次台账收口提交。
- 2026-08-25：随后 `git diff --cached --check && git commit -m "docs(ledger): close C1.10 plan ledger"` 成功，原始输出为 `[cards/C1.10-charter-3 f79d3f8] docs(ledger): close C1.10 plan ledger`、`1 file changed, 1 insertion(+)`；未 push。本条追加后需最终台账提交。
- 2026-08-25：最终命令 `git status --short --branch && git log -4 --oneline --decorate && git diff --check HEAD~4..HEAD && test -f docs/superpowers/plans/c1.10-plan.md && test -f docs/ledgers/2026-08-25-c1.10-plan-ledger.md` 成功；工作树仅输出 `## cards/C1.10-charter-3`，HEAD 为 `f79d3f8 docs(ledger): close C1.10 plan ledger`，提交区间 whitespace 检查无输出，两个法定文件 `final_required_files=present`。本条追加后需最终台账提交。
- 2026-08-25：实现轮读取 `skills/handoff/SKILL.md` 与 `skills/implement/SKILL.md`；`pwd` 为 `/root/.handoff/worktrees/dc77536e`，当前分支为 `cards/C1.10-charter-4`，初始 `git status --short` 无输出；仓内无根级 `codegraph/` 目录，计划要求的 charter 范围边界可执行。
- 2026-08-25：已读取 `docs/superpowers/plans/c1.10-plan.md` 全文（714 行）；执行顺序与计划一致，T1→T4，T1/T2 不跨入 handoff 响应装配、声明迁移或根级 `codegraph/domains`。
- 2026-08-25：按 T1 先改测试调用面与 fixture key 后执行 `go test ./codegraph -run 'Test(LoadDomainDecls|ValidateDecls)$' -count=1`（workdir `graph`）退出 1；原始编译错误为 `too many arguments in call to ValidateDecls`，现有签名仍为 `(*View, string, map[string]DomainDecl)`，红因是待实现的四参契约缺席。
- 2026-08-25：T1 最小实现已将 `ValidateDecls` 改为四参、以 `best.Domains` 为唯一领域词表并对 `best == nil` 产出可见 issue，CLI 调用点同步传入 `best`；`go test ./codegraph -run 'Test(LoadDomainDecls|ValidateDecls)$' -count=1` 与 `go test ./cli -run 'TestGraphValidate(|ReportsDomainCount|DomainDeclIssuePrefix)$' -count=1` 均退出 0，原始输出分别为 `ok github.com/Xsxdot/charter/graph/codegraph 0.003s`、`ok github.com/Xsxdot/charter/graph/cli 0.005s`。
- 2026-08-25：T1 首次全量收尾 `go test ./... -count=1` 与 `go vet ./...` 退出 1；原始失败为 `TestGraphEntity`/`TestEntityLifecycleAndDomainDecl` 仍寻找 d_cli 声明，以及 `TestGraphCheckLoadsDomainDeclsAndReportsAnchorOwnership` 在 fixture rename 后得到 `实际 0 条`。随后仅更新受 d_cli→d_cmd fixture key 影响的断言，并在 anchor ownership 回归中显式构造临时 baseline d_cli 声明；第二次同命令退出 1，原始错误为 `invalid character 'é' looking for beginning of value`，输出前有预算跳过诊断。改用 `runGraphSeparate` 隔离 stderr 后重跑成功：`go test ./... -count=1` 退出 0，输出 `ok github.com/Xsxdot/charter/graph/cli 0.389s`、`? github.com/Xsxdot/charter/graph/cmd/codegraph [no test files]`、`ok github.com/Xsxdot/charter/graph/codegraph 0.022s`、`ok github.com/Xsxdot/charter/graph/webui 0.001s`；`go vet ./...` 同命令退出 0、无输出。
- 2026-08-25：T2 先执行 `npm test -- src/app/codegraph/domainpage.test.ts`（workdir `graph/webui`）退出 127，原始错误为 `sh: 1: vitest: not found`；该红仅证明依赖缺席，未作为主缝功能红，按计划先恢复锁文件依赖。
- 2026-08-25：执行 `npm ci`（workdir `graph/webui`）退出 0；原始输出为 `added 168 packages, and audited 169 packages in 1s`、`30 packages are looking for funding`、`found 0 vulnerabilities`。
- 2026-08-25：依赖恢复后首次 `npm test -- src/app/codegraph/domainpage.test.ts` 退出 1；5 个行为测试原始错误均为 `ReferenceError: deriveDomainPage is not defined`，定位为新增测试遗漏导入，非生产结论；补齐测试导入后立即重跑。
- 2026-08-25：补齐 `deriveDomainPage` 导入后同一 focused 命令退出 1；四个常量测试通过，5 个行为测试均真实命中空壳原始错误 `Error: C1.10 domain page derivation is not implemented`，确认主缝行为红因功能缺失而非 typo。
- 2026-08-25：T2 最小派生器实现完成；`npm test -- src/app/codegraph/domainpage.test.ts` 退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 9 passed (9)`；`npm run typecheck` 退出 0，原始脚本输出 `tsc -b`。
- 2026-08-25：首次四发 T2 变异均命中唯一语义位置并使 focused Vitest 转红：K 改为 `>=2` 时 1 条断言红、入缝判据反转时 1 条断言红、节点上限改 6 时 2 条断言红、`droppedNodes` 置 0 时 1 条断言红；这些发次已恢复，但当时未先单独跑 `npm run typecheck`，故仅作为行为读数，不作为完整变异收口，下面按纪律补做带编译闸的四发。
- 2026-08-25：补做变异 1（唯一锚点 `callerDomains.length >= DOMAIN_SHARED_CALLER_DOMAINS` 改为 `>= 2`）前先执行 `npm run typecheck`，退出 0，原始脚本输出 `tsc -b`，确认该变异可编译。
- 2026-08-25：变异 1 随后执行同一 focused Vitest 退出 1，原始摘要 `Tests 1 failed | 8 passed (9)`，失败断言为 K=2 节点被错误折叠；变异已恢复为 `DOMAIN_SHARED_CALLER_DOMAINS`。
- 2026-08-25：变异 2（`noInboundSeams` 判据由 `=== 0` 反转为 `!== 0`）先经 `npm run typecheck` 退出 0（`tsc -b`），再经 focused Vitest 退出 1，原始摘要 `Tests 1 failed | 8 passed (9)`，无入缝空态断言转红；变异已恢复。
- 2026-08-25：变异 3（唯一导出常量 `DOMAIN_LEVEL_NODE_LIMIT` 从 8 改为 6）先经 `npm run typecheck` 退出 0（`tsc -b`），再经 focused Vitest 退出 1，原始摘要 `Tests 2 failed | 7 passed (9)`，常量钉值与 level 截断断言均转红；变异已恢复。
- 2026-08-25：变异 4（唯一 `droppedNodes` 计算替换为常量 0）先经 `npm run typecheck` 退出 0（`tsc -b`），再经 focused Vitest 退出 1，原始摘要 `Tests 1 failed | 8 passed (9)`，截断计数断言转红；变异已恢复。
- 2026-08-25：恢复源文件后 T2 收尾 `npm test -- src/app/codegraph/domainpage.test.ts` 与 `npm run typecheck` 均退出 0；原始摘要为 `Test Files 1 passed (1)`、`Tests 9 passed (9)`，类型检查脚本输出 `tsc -b`。
- 2026-08-25：T3 新建两个组件缝测试后先执行 `npm test -- src/app/codegraph/BestDomainPage.test.tsx src/app/codegraph/DomainCascadeDrawer.test.tsx`，退出 1；原始 Vite 错误分别为 `Failed to resolve import "./BestDomainPage"` 与 `Failed to resolve import "./DomainCascadeDrawer"`，这是新缝生产符号尚未落地的首红，随后先落最小组件壳再继续断言红。
- 2026-08-25：T3 组件实现后首次合并执行 `npm run typecheck` 与 `rg -n "react-router|fetch\\(" src/app/codegraph` 整体退出 1；原始输出仅有 `tsc -b`，无 TypeScript 错误，后续需分开记录 typecheck 与 rg 无匹配退出。
- 2026-08-25：T3 focused 测试中先后修正两处测试查询的 exact-text 假设（`不可破规则`、`Entity` 后面带测试/creator 文本）与一条 fixture 无效 edge；修正后 `npm test -- src/app/codegraph/BestDomainPage.test.tsx src/app/codegraph/DomainCascadeDrawer.test.tsx` 退出 0，原始摘要 `Test Files 2 passed (2)`、`Tests 8 passed (8)`。
- 2026-08-25：分开执行 `npm run typecheck` 退出 0，原始脚本输出 `tsc -b`；`rg -n "react-router|fetch\\(" src/app/codegraph` 退出 1、无输出，表示该目录没有匹配。
- 2026-08-25：T4 先写 wire/page 测试后执行 `npm test -- src/api/client.test.ts src/app/codegraph/CodegraphPage.test.tsx` 退出 1；wire 测试 22 条通过，新增/改造的 2 条页面接线断言均为 `expected null to be truthy`，页面仍渲染旧 `data-best-leaf`，确认红因是 `BestLeafGraph` 尚未替换，非 wire 解析失败。
- 2026-08-25：T4 仅替换 `CodegraphPage` best 叶子挂载点为 `BestDomainPage` 并透传顶层 `data.decls`；focused `npm test -- src/api/client.test.ts src/app/codegraph/CodegraphPage.test.tsx` 退出 0，原始摘要 `Test Files 2 passed (2)`、`Tests 24 passed (24)`；`npm run typecheck` 退出 0，原始脚本输出 `tsc -b`。
- 2026-08-25：整轮 `npm test`（workdir `graph/webui`）退出 0；原始摘要 `Test Files 20 passed (20)`、`Tests 127 passed (127)`，Vitest v4.1.11。
- 2026-08-25：`npm run build`（workdir `graph/webui`）退出 0；原始输出为 `tsc -b` 后 `vite v6.4.3`、`✓ 48 modules transformed`、`dist/index.html 0.45 kB`、`dist/assets/index-I5fC_abQ.css 20.03 kB`、`dist/assets/index-DaRh1Ej9.js 275.59 kB`、`✓ built in 704ms`。
- 2026-08-25：整轮 `go test ./... -count=1`（workdir `graph`）退出 0；原始输出为 `ok github.com/Xsxdot/charter/graph/cli 0.357s`、`? github.com/Xsxdot/charter/graph/cmd/codegraph [no test files]`、`ok github.com/Xsxdot/charter/graph/codegraph 0.019s`、`ok github.com/Xsxdot/charter/graph/webui 0.002s`。
- 2026-08-25：`go vet ./...`（workdir `graph`）退出 0，原始输出为空。
- 2026-08-25：`git status --short` 显示本轮预期改动：Go T1、webui T2/T3/T4、d_cmd fixture、台账，以及 build 重建的 dist 旧 hash 删除/新 hash 未跟踪；`git diff --check` 退出 0、无输出。当前尚未暂存或提交。
- 2026-08-25：禁止路径扫描 `find . \( -path './internal/agentd*' -o -path './codegraph/domains/d_workspace.json' -o -path './codegraph/domains/d_coordination_task.json' \) -print` 退出 0、无输出；`git diff --check` 再次退出 0、无输出。
- 2026-08-25：调用面核对 `rg -n "ValidateDecls\\(" graph -g '*.go'` 仅列出四参实现、CLI 调用和对应测试；`BestLeafGraph` 仅保留为 `BestDomainPage` 结构 tab 的复用，`CodegraphPage` 不再直挂旧叶子组件；fixture 实际 key 为 `d_cmd.json`，独立 `LoadDomainDecls` 单测仍使用临时 d_cli 文件名场景。
- 2026-08-25：`gofmt -d graph/codegraph/decls.go graph/codegraph/decl_test.go graph/cli/cli.go graph/cli/cli_test.go graph/codegraph/entity_test.go` 退出 0、无差异输出。
- 2026-08-25：首次执行 `git add -A` 退出 128；原始错误为 `fatal: Unable to create '/root/.handoff/repos/charter/.git/worktrees/dc77536e/index.lock': Read-only file system`。代码与台账仍未进入 index，需按工作树既有权限规则以升级权限重试。
- 2026-08-25：以升级权限执行 `git add -A && git diff --cached --check && git diff --cached --stat && git status --short` 成功，`git diff --cached --check` 无输出；暂存统计为 21 files changed、1012 insertions、91 deletions，d_cli fixture 被识别为 d_cmd rename，dist 旧 hash 删除/新 hash 添加均已暂存。
- 2026-08-25：以升级权限执行 `git add docs/ledgers/2026-08-25-c1.10-plan-ledger.md && git diff --cached --check && git commit -m "feat(viewer): complete C1.10 domain page"` 成功；原始提交输出为 `[cards/C1.10-charter-4 23f313a] feat(viewer): complete C1.10 domain page`、`21 files changed, 1013 insertions(+), 91 deletions(-)`，未 push。本条提交事实追加后需再提交台账收口。
- 2026-08-25：提交后复核 `git status --short` 仅显示台账 `M`；`git log -2 --oneline --decorate` 原始输出为 `23f313a (HEAD -> cards/C1.10-charter-4) feat(viewer): complete C1.10 domain page`、`50fc65e (cards/C1.10-charter-3) docs(ledger): record C1.10 final clean state`。
- 2026-08-25：升级权限执行 `git add docs/ledgers/2026-08-25-c1.10-plan-ledger.md && git diff --cached --check && git commit -m "docs(ledger): close C1.10 implementation"` 成功；原始提交输出为 `[cards/C1.10-charter-4 5d62f86] docs(ledger): close C1.10 implementation`、`1 file changed, 2 insertions(+)`，未 push。本条后做最终只读复核。
- 2026-08-25：最终只读复核命令退出 0；原始输出为 `## cards/C1.10-charter-4`（仅因本条台账追加暂时 dirty）、HEAD `5d62f86`、前一实现提交 `23f313a`、`git diff --check HEAD~3..HEAD` 无输出；dist 新文件、`d_cmd.json` 存在，禁止的 `d_workspace.json`/`d_coordination_task.json` 不存在。本条追加后需最终台账提交以恢复干净工作树。
- 2026-08-25：升级权限执行 `git add docs/ledgers/2026-08-25-c1.10-plan-ledger.md && git diff --cached --check && git commit -m "docs(ledger): record C1.10 final verification"` 成功；原始提交输出为 `[cards/C1.10-charter-4 d7cac9c] docs(ledger): record C1.10 final verification`、`1 file changed, 2 insertions(+)`，未 push。本条之后仅做最终收尾提交。
- 2026-08-25：修复轮初始现场为 `/root/.handoff/worktrees/44c00bdd`、分支 `cards/C1.10-charter-5`；`git status --short` 无输出，当前工作树干净。
- 2026-08-25：检查到 `domainpage.ts` 的 `edgeContexts` 过滤同域边，且级联 K 统计直接从该跨域集合计算 `callerDomains`；现有测试只覆盖跨域 3/2 领域，不覆盖「本域 1 + 跨域 2」的复现判据。
- 2026-08-25：新增复现测试后执行 `npx vitest run src/app/codegraph/domainpage.test.ts -t "includes the focus domain when counting shared caller domains"`（workdir `graph/webui`）未进入测试，原始错误为 `npm error code EROFS`、`Invalid response body while trying to fetch https://registry.npmjs.org/vitest: EROFS: read-only file system`；需检查依赖入口后再跑，当前不计作功能红灯。
- 2026-08-25：检查 `graph/webui/package.json` 后确认测试脚本为 `vitest run`；当前 worktree 不存在 `node_modules`，`node_modules/.bin/vitest` 检查退出 1，因此先恢复本地依赖再执行复现测试。
- 2026-08-25：执行 `npm ci --cache /root/.handoff/tmp/44c00bdd/npm-cache`（workdir `graph/webui`）退出 0；原始输出为 `added 168 packages, and audited 169 packages in 4s`、`found 0 vulnerabilities`。
- 2026-08-25：复现测试恢复依赖后执行 `npx vitest run src/app/codegraph/domainpage.test.ts -t "includes the focus domain when counting shared caller domains"` 退出 1；原始断言为 `expected { id: 'focus', depth: +0, …(2) } to match object { collapsed: true, …(1) }`，实际 `collapsed: false`，确认红因是同域调用方未参与 K 统计。
- 2026-08-25：最小修复在 `domainpage.ts` 增加全量边的 `callerDomainsByNode` 映射，并将其传入级联列 K 判据；跨域 `edgeContexts` 未改。复现命令再次执行退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 1 passed | 9 skipped (10)`。
- 2026-08-25：修复后执行 `npx vitest run src/app/codegraph/domainpage.test.ts`（workdir `graph/webui`）退出 0；原始摘要为 `Test Files 1 passed (1)`、`Tests 10 passed (10)`。
- 2026-08-25：变异复验前以 `rg -n -F "domains.add(fromDomain)" src/app/codegraph/domainpage.ts | wc -l` 检查语义锚点命中数，原始输出为 `1`，确认替换唯一。
- 2026-08-25：首次同域过滤变异后执行 `npm run typecheck`，原始失败为 `src/app/codegraph/domainpage.ts(225,3): error TS6133: 'edges' is declared but its value is never read.`；该发未计数，因未通过编译闸，随后移除已无用途的 `edges` 参数以形成可编译等价变异。
- 2026-08-25：可编译同域过滤变异执行 `npm run typecheck` 退出 0，原始输出为 `> tsc -b` 且无错误，确认变异通过编译闸。
- 2026-08-25：该可编译变异先执行复现行为断言，`npx vitest run src/app/codegraph/domainpage.test.ts -t "includes the focus domain when counting shared caller domains"` 退出 1，原始失败仍为预期 `collapsed: true`、实际 `collapsed: false`；确认变异确实命中唯一守卫。
- 2026-08-25：随后执行变异下完整 `npx vitest run src/app/codegraph/domainpage.test.ts` 退出 1；原始摘要为 `1 failed | 9 passed (10)`，唯一失败是新增同域调用方复现用例，测试确实拦截该变异。
- 2026-08-25：恢复真实实现后执行 `npm run typecheck && npx vitest run src/app/codegraph/domainpage.test.ts` 退出 0；原始输出为 `> tsc -b`，以及 `Test Files 1 passed (1)`、`Tests 10 passed (10)`。
- 2026-08-25：恢复实现后的 `git diff --check` 退出 0 且无输出；代码差异仅涉及 `domainpage.ts` 的全量调用方领域映射、对应复现测试，以及本台账，未触及其他 C1.10 功能。
- 2026-08-25：最终 Go 测试 `go test ./... -count=1`（workdir `graph`）退出 0；原始输出为 `ok github.com/Xsxdot/charter/graph/cli 0.259s`、`? github.com/Xsxdot/charter/graph/cmd/codegraph [no test files]`、`ok github.com/Xsxdot/charter/graph/codegraph 0.026s`、`ok github.com/Xsxdot/charter/graph/webui 0.003s`。
- 2026-08-25：最终 Go 静态检查 `go vet ./...`（workdir `graph`）退出 0，原始输出为空。
- 2026-08-25：最终 WebUI 检查 `npx tsc -b && npx vitest run`（workdir `graph/webui`）退出 0；原始摘要为 `Test Files 20 passed (20)`、`Tests 128 passed (128)`，tsc 无错误输出。
- 2026-08-25：重建 `npm run build`（workdir `graph/webui`）退出 0；原始输出为 `vite v6.4.3`、`✓ 48 modules transformed`、`dist/assets/index-I5fC_abQ.css 20.03 kB`、`dist/assets/index-BkSj_o3V.js 275.82 kB`、`✓ built in 679ms`。
- 2026-08-25：重建后 `git status --short` 显示旧 bundle `index-DaRh1Ej9.js` 删除、新 bundle `index-BkSj_o3V.js` 未跟踪、`dist/index.html` 更新；dist 产物集合含新 JS、既有 CSS、字体和 index.html，待随本轮提交固化。
- 2026-08-25：首次执行 `git diff --check && git add -A` 失败，原始错误为 `fatal: Unable to create '/root/.handoff/repos/charter/.git/worktrees/44c00bdd/index.lock': Read-only file system`；未改变暂存区，需按工作树权限以升级权限重试。
- 2026-08-25：升级权限执行 `git add -A && git diff --cached --check && git diff --cached --stat && git status --short` 成功；暂存统计为 5 files changed、97 insertions、17 deletions，dist 旧/新 bundle 被识别为 rename，cached whitespace 检查无输出。
- 2026-08-25：升级权限执行 `git add docs/ledgers/2026-08-25-c1.10-plan-ledger.md && git diff --cached --check && git commit -m "fix(viewer): count same-domain callers for collapse"` 成功；原始提交输出为 `[cards/C1.10-charter-5 20a65ed] fix(viewer): count same-domain callers for collapse`、`5 files changed, 98 insertions(+), 17 deletions(-)`，未 push。
- 2026-08-25：提交后只读复核显示 `HEAD` 为 `20a65ed fix(viewer): count same-domain callers for collapse`，`git diff --check HEAD~1..HEAD` 无输出；台账因本条追加暂时 dirty，dist/index.html 唯一引用 `index-BkSj_o3V.js`，新 bundle 存在且旧 `index-DaRh1Ej9.js` 不存在。
- 2026-08-25：升级权限执行 `git add docs/ledgers/2026-08-25-c1.10-plan-ledger.md && git diff --cached --check && git commit -m "docs(ledger): record C1.10 repair verification"` 成功；原始提交输出为 `[cards/C1.10-charter-5 c6d39d9] docs(ledger): record C1.10 repair verification`、`1 file changed, 2 insertions(+)`，未 push。
- 2026-08-25：最终只读复核输出为 `## cards/C1.10-charter-5`、HEAD `c6d39d9`、修复提交 `20a65ed`；`git diff --check HEAD~2..HEAD` 无输出，工作树仅因本条台账追加 dirty；`git ls-files` 已包含重建后的 `index-BkSj_o3V.js`、CSS、字体和 index.html，旧 bundle 不存在。
- 2026-08-25：升级权限执行 `git add docs/ledgers/2026-08-25-c1.10-plan-ledger.md && git diff --cached --check && git commit -m "docs(ledger): finalize C1.10 repair"` 成功；原始提交输出为 `[cards/C1.10-charter-5 f9b7f11] docs(ledger): finalize C1.10 repair`、`1 file changed, 2 insertions(+)`，未 push。本条后仅需最终台账提交。
- 2026-08-25：本轮初始核对：工作树干净，当前分支为 `cards/C1.10-charter-6`；五条补充断言分别缺少 `testRef` 存在、`anchor` 缺席、`decls: {}` 页面空态、未知声明字段页面渲染和页面交互后的 `toHaveBeenCalledTimes` 守卫；按本轮范围只改两份测试文件，生产实现不提交。
- 2026-08-25：首次给 `client.test.ts` 打补丁时上下文锚点错误，`apply_patch` 原文失败为 `Failed to find expected lines ... const present = await fetchCodegraph('demo')`；未产生文件改动，随后改用更短上下文成功补丁。
- 2026-08-25：已提交测试变更（未触及生产实现）：`client.test.ts` 加入 `testRef` 存在与 `anchor` 缺席的逐字段断言；`CodegraphPage.test.tsx` 加入 `decls: {}` 空态、未知声明字段渲染和 tab/组织/lane/级联节点请求次数断言，并为页面测试 mock 提供一次初始代码图请求计数。
- 2026-08-25：`npx tsc -b`（`graph/webui`）首次执行退出 1，原始错误为 `npm error code EROFS`、`Invalid response body while trying to fetch https://registry.npmjs.org/tsc`，并报告 `/root/.npm/_cacache/tmp` 为只读；本次未得到类型检查结果，按平台规则记录后请求升级权限重跑。
- 2026-08-25：升级权限重跑 `npx tsc -b` 仍退出 1；原始输出显示 npm 安装了错误的临时包 `tsc@2.0.4`，随后提示 `This is not the tsc command you are looking for` 与 `Use npm install typescript`；当前 worktree 缺少可用本地 TypeScript 依赖，需执行仓库既有 `npm ci` 后再验证。
- 2026-08-25：`npm ci --cache /root/.handoff/tmp/9c9ac760/npm-cache`（`graph/webui`）退出 0；原始摘要为 `added 168 packages, and audited 169 packages in 5s`、`found 0 vulnerabilities`。
- 2026-08-25：依赖恢复后 `npx tsc -b`（`graph/webui`）退出 0，原始输出为空。
- 2026-08-25：补充断言后的 focused `npm test -- src/api/client.test.ts src/app/codegraph/CodegraphPage.test.tsx`（`graph/webui`）退出 0；原始摘要为 `Test Files 2 passed (2)`、`Tests 27 passed (27)`。
- 2026-08-25：开始 wire 变异复验：临时在 `client.ts#parseResponse` 将存在的 `testRef` 改为空串、缺席的 `anchor` 补为空串；该变异仅用于验证，未作为交付改动。
- 2026-08-25：wire 变异先跑 `npx tsc -b`（`graph/webui`）退出 0，原始输出为空，满足变异编译闸。
- 2026-08-25：wire 变异锚点核对 `rg -n -F "invariant.testRef = ''" src/api/client.ts` 与 `rg -n -F "transition.anchor = ''" src/api/client.ts` 各命中 1 次（第 49、52 行），满足唯一命中条件。
- 2026-08-25：wire 变异行为断言 `npm test -- src/api/client.test.ts -t "additive-only 消费 decls"` 退出 1；原始失败为期望 `testRef: "TestRule"`、实际 `testRef: ""`，确认 testRef 存在分支转红；因首个普通断言失败，anchor 分支尚未执行。
- 2026-08-25：移除 wire 变异中的 `testRef` 默认值，仅保留缺席 `anchor` 补空串；该变异重新执行前 `npx tsc -b` 退出 0，原始输出为空。
- 2026-08-25：仅 anchor 默认值变异的行为断言同一 focused 命令退出 1；原始失败为期望缺席 `anchor`，实际出现 `anchor: ""`，确认 anchor 缺席分支转红。
- 2026-08-25：wire 变异已恢复；`git diff -- graph/webui/src/api/client.ts` 无输出，且 `mutation probe` 无命中，确认生产实现未保留变异。
- 2026-08-25：开始页面空对象变异复验：临时将 `domainpage.ts#semanticModel` 改为 `decls === undefined` 才为空态，`decls: {}` 时注入“暂无数据”占位声明；该变异仅用于验证，未作为交付改动。
- 2026-08-25：页面空对象变异先跑 `npx tsc -b`（`graph/webui`）退出 0，原始输出为空，满足变异编译闸。
- 2026-08-25：页面空对象变异 focused `npm test -- src/app/codegraph/CodegraphPage.test.tsx -t "decls 为空对象"` 退出 1；原始 DOM 证据显示页面出现 `暂无数据` 而非声明空态，断言 `/声明是人写的应然承诺，扫描器不生成/` 找不到，确认空对象判据转红。
- 2026-08-25：页面空对象变异已恢复；`git diff -- graph/webui/src/app/codegraph/domainpage.ts` 无输出，且 `mutation probe` 无命中，确认生产实现未保留变异。
- 2026-08-25：不重复请求变异前以 `rg -n -F "const goBestScope = (next: string | null) => {" CodegraphPage.tsx | wc -l` 核对目标锚点命中数为 `1`，满足唯一命中条件。
- 2026-08-25：开始不重复请求变异：临时让 `goBestScope` 在领域切换时调用 `reload()`，模拟切换动作重新 fetch；该变异仅用于验证，未作为交付改动。
- 2026-08-25：不重复请求变异先跑 `npx tsc -b`（`graph/webui`）退出 0，原始输出为空，满足变异编译闸。
- 2026-08-25：不重复请求变异 focused `npm test -- src/app/codegraph/CodegraphPage.test.tsx -t "领域页切 tab"` 退出 1；原始失败为 `expected "vi.fn()" to be called 1 times, but got 3 times`，定位到切换动作触发了额外 reload，调用次数守卫转红。
- 2026-08-25：不重复请求变异已恢复；`git diff -- graph/webui/src/app/codegraph/CodegraphPage.tsx` 无输出，且 `mutation probe` 无命中，确认生产实现未保留变异。
- 2026-08-25：恢复真实实现后 `npx tsc -b`（`graph/webui`）退出 0，原始输出为空。
- 2026-08-25：收尾 `npm test`（`graph/webui`）退出 0；原始摘要为 `Test Files 20 passed (20)`、`Tests 131 passed (131)`，Vitest v4.1.11。
- 2026-08-25：恢复后只读范围核对成功：`git status --short --branch` 仅列台账、`client.test.ts`、`CodegraphPage.test.tsx` 三个文件；`git diff --check` 无输出；生产实现三文件 diff 无输出。
- 2026-08-25：首次暂存命令退出 128，原始错误为 `fatal: Unable to create '/root/.handoff/repos/charter/.git/worktrees/9c9ac760/index.lock': Read-only file system`；未改变暂存区，按工作树权限规则请求升级权限重试。
- 2026-08-25：升级权限执行同一 `git add` 成功，退出码 0；三份交付文件已进入暂存区。
- 2026-08-25：暂存区检查 `git diff --cached --check` 退出 0、无 whitespace 输出；暂存统计为三文件 `148 insertions(+), 5 deletions(-)`，台账因本条追加显示 `MM`，提交前需再次暂存。
- 2026-08-25：升级权限重新暂存并检查成功；`git diff --cached --check` 无输出，统计为三文件 `150 insertions(+), 5 deletions(-)`，状态三文件均为已暂存 `M `。
- 2026-08-25：最终提交前暂存复核 `git diff --cached --check` 退出 0、无输出；当前暂存统计为三文件 `151 insertions(+), 5 deletions(-)`，三文件均已暂存。
- 2026-08-25：本轮实现提交成功，原始输出为 `[cards/C1.10-charter-6 e31f353] test(viewer): cover additive decls and request stability`、`3 files changed, 152 insertions(+), 5 deletions(-)`；未 push。本条后需提交台账收口。
- 2026-08-25：台账收口提交成功，原始输出为 `[cards/C1.10-charter-6 0c070e2] docs(ledger): record C1.10 test assertions`、`1 file changed, 1 insertion(+)`；未 push。本条后做最终只读复核。
- 2026-08-25：本轮初始核对命令 `git status --short --branch` 输出 `## cards/C1.10-charter-7`，工作树干净；当前待修现场集中在 `BestDomainPage.tsx` 的 SemanticTab/StructureTab 渲染，既有 `DomainCascadeDrawer` 已渲染 `droppedNodes` 截断标记。
- 2026-08-25：现状源码核对确认 SemanticTab 与 StructureTab 的子树没有 `className`，端口区只有裸 `label · N`，出端口为空时没有空态，页面也未渲染 `focusTruncation`；`BestDomainPage.test.tsx` 当前 4 支测试未覆盖这些真机 finding。
- 2026-08-25：先执行 `npm test -- src/app/codegraph/BestDomainPage.test.tsx src/app/codegraph/DomainCascadeDrawer.test.tsx`（`graph/webui`）得到退出码 127，原始输出为 `sh: 1: vitest: not found`；新增红灯尚未执行，需恢复仓库依赖后重跑。
- 2026-08-25：执行 `npm ci --cache /root/.handoff/tmp/e40fe48e/npm-cache`（`graph/webui`）退出 0，原始输出为 `added 168 packages, and audited 169 packages in 4s`、`found 0 vulnerabilities`，恢复本地依赖。
- 2026-08-25：依赖恢复后 focused `npm test -- src/app/codegraph/BestDomainPage.test.tsx src/app/codegraph/DomainCascadeDrawer.test.tsx` 退出 1；Vitest 原始摘要为 `Test Files 1 failed | 1 passed (2)`、`Tests 2 failed | 8 passed (10)`，新增断言分别找不到 `状态机` 与 `入缝 7 条，按配额显示前 5`，确认红因是页面行为缺失而非拼写/编译错误。
- 2026-08-25：首轮实现后 focused 测试剩 1 红；原始 DOM 显示级联第 1 层实际有既有 `next` 加 9 个新节点，共 10 个候选，按上限 8 丢弃 2 个，而测试误写为 1；该红属于测试 fixture 预期错误，已改为断言 2，未改生产逻辑。
- 2026-08-25：实现后的 focused `npm test -- src/app/codegraph/BestDomainPage.test.tsx src/app/codegraph/DomainCascadeDrawer.test.tsx` 退出 0，原始摘要为 `Test Files 2 passed (2)`、`Tests 10 passed (10)`；`npx tsc -b`（`graph/webui`）退出 0 且无错误输出。
- 2026-08-25：focus 截断渲染变异前唯一锚点核对 `rg -n -F "{model.focusTruncation ? (" graph/webui/src/app/codegraph/BestDomainPage.tsx | wc -l` 输出 `1`，满足唯一命中条件。
- 2026-08-25：focus 截断渲染第一次变异将条件改为 `false` 后执行 `npx tsc -b` 退出 2，原始错误为 `BestDomainPage.tsx(130,15): error TS18048: 'model.focusTruncation' is possibly 'undefined'.` 与第 130 行列 54 同类错误；该发未计数，因变异未通过编译闸。
- 2026-08-25：将 focus 截断变异改为可编译的等价语义 `model.focusTruncation ? null : null` 后，`npx tsc -b` 退出 0、无错误输出。
- 2026-08-25：可编译 focus 截断变异先执行行为断言 `npm test -- src/app/codegraph/BestDomainPage.test.tsx -t "shows focus quota and cascade level truncation in the page"`，退出 1；原始失败为找不到 `入缝 7 条，按配额显示前 5`，确认页面截断守卫转红。
- 2026-08-25：focus 截断变异已恢复为真实渲染表达式，后续将重新执行类型检查与 focused 行为测试确认恢复。
- 2026-08-25：出端口空态变异前唯一锚点核对 `rg -n -F "data-empty=\"no-outbound-seams\"" graph/webui/src/app/codegraph/BestDomainPage.tsx | wc -l` 输出 `1`，满足唯一命中条件。
- 2026-08-25：可编译出端口空态变异执行 `npx tsc -b` 退出 0、无错误输出。
- 2026-08-25：出端口空态变异先执行行为断言 `npm test -- src/app/codegraph/BestDomainPage.test.tsx -t "renders the semantic card subtitles and named port sections"`，退出 1；原始失败为找不到 `无跨域出边`，确认空态守卫转红。
- 2026-08-25：出端口空态变异已恢复为真实 `data-empty="no-outbound-seams"` 文案渲染，后续将执行恢复后的 focused 与全量检查。
- 2026-08-25：恢复实现后 `npx tsc -b`（`graph/webui`）退出 0、无错误输出；focused `npm test -- src/app/codegraph/BestDomainPage.test.tsx src/app/codegraph/DomainCascadeDrawer.test.tsx` 退出 0，原始摘要为 `Test Files 2 passed (2)`、`Tests 10 passed (10)`。
- 2026-08-25：WebUI 全量 `npm test`（`graph/webui`）退出 0，Vitest 原始摘要为 `Test Files 20 passed (20)`、`Tests 133 passed (133)`。
- 2026-08-25：全量 `npx tsc -b`（`graph/webui`）退出 0、无错误输出。
- 2026-08-25：`npm run build`（`graph/webui`）退出 0；Vite 原始输出为 `✓ 48 modules transformed`、`dist/assets/index-CpZITxjd.css 20.96 kB`、`dist/assets/index-CEsCpWpy.js 279.73 kB`、`✓ built in 686ms`。
- 2026-08-25：Go 全量 `go test ./... -count=1`（`graph`）退出 0；原始输出为 `ok github.com/Xsxdot/charter/graph/cli 0.200s`、`? github.com/Xsxdot/charter/graph/cmd/codegraph [no test files]`、`ok github.com/Xsxdot/charter/graph/codegraph 0.026s`、`ok github.com/Xsxdot/charter/graph/webui 0.003s`。
- 2026-08-25：Go 静态检查 `go vet ./...`（`graph`）退出 0，原始输出为空。
- 2026-08-25：构建后 `git status --short` 显示旧 dist bundle `index-BkSj_o3V.js`/`index-I5fC_abQ.css` 删除、新 bundle `index-CEsCpWpy.js`/`index-CpZITxjd.css` 未跟踪，`dist/index.html` 已引用新 hash；这些产物将在收尾暂存提交。
- 2026-08-25：静态收口核对命令退出 0；`git diff --check` 无输出，禁止路径/`console.log` 搜索无匹配，`dist/index.html` 引用 `assets/index-CEsCpWpy.js` 与 `assets/index-CpZITxjd.css`，源码含六格标题/副标题、端口标题、出端口空态和入缝截断文案。
- 2026-08-25：首次 `git add -A` 退出 128，原始报错为 `fatal: Unable to create '/root/.handoff/repos/charter/.git/worktrees/e40fe48e/index.lock': Read-only file system`；未改变 index，按工作树权限规则请求升级权限重试。
- 2026-08-25：升级权限执行 `git add -A` 成功，随后 `git diff --cached --check` 退出 0、无 whitespace 输出；暂存统计为 7 files changed、169 insertions、44 deletions，dist 新旧 bundle 被识别为 rename 加 CSS 替换，代码/测试/台账均已暂存。
- 2026-08-25：提交前 `git diff --cached --check` 退出 0、无输出；`git diff --cached --name-status` 仅列台账、重建 dist、`BestDomainPage.tsx` 与其测试，未包含 handoff 宿主或声明迁移路径。
