# C1.10 plan ledger

- 2026-08-25：已完整读取 `skills/handoff/SKILL.md`、`skills/plan/SKILL.md`、`skills/spec/SKILL.md`、`skills/contract/SKILL.md`、`skills/defect-families/SKILL.md`、`skills/using-charter/SKILL.md`；本轮为 L3 轻档 plan 节点，只产出计划，不调用 handoff CLI、不启动 executor/子任务。
- 2026-08-25：`git status --short --branch` 输出 `## cards/C1.10-charter-3`；`git log -3 --oneline --decorate` 输出 `6892418 (HEAD -> cards/C1.10-charter-3, cards/C1.10-charter-2) docs(ledger): close C1.10 breakdown audit`、`472d19b docs(ledger): record C1.10 final verification`、`cd493fd docs(ledger): record C1.10 breakdown commit`；当前工作树初始干净，未切分支。
- 2026-08-25：spec `docs/specs/2026-08-24-codegraph-viewer-semantics-spec.md` 第 3 行为 `状态：已批准`；contract `docs/superpowers/specs/c1.10-contract.md` 第 3 行为 `状态：已冻结（随本提交，2026-08-25）`；输入状态满足 plan 前置条件。
- 2026-08-25：当前 `docs/superpowers/plans/c1.10-plan.md` 不存在；本轮法定计划路径需新建。
- 2026-08-25：当前 `graph/webui/src/app/codegraph` 已有 `BestDetail.tsx`、`BestLeafGraph.tsx`、`DomainDetail.tsx`、`CodegraphPage.tsx`、`domainpage.ts` 及测试；未见 `BestDomainPage.tsx` 或 `DomainCascadeDrawer.tsx`，P1-B 命名裁决需在计划中落为新增文件并由 T4 接线。
- 2026-08-25：基线命令 `go test ./codegraph -run 'Test(LoadDomainDecls|ValidateDecls)$' -count=1`（workdir `graph`）退出 0，原始输出 `ok github.com/Xsxdot/charter/graph/codegraph 0.020s`；`go test ./cli -run 'TestGraphValidate(|ReportsDomainCount|DomainDeclIssuePrefix)$' -count=1` 退出 0，原始输出 `ok github.com/Xsxdot/charter/graph/cli 0.004s`；`go test ./... -count=1` 退出 0，原始输出 `ok github.com/Xsxdot/charter/graph/cli 0.161s`、`? github.com/Xsxdot/charter/graph/cmd/codegraph [no test files]`、`ok github.com/Xsxdot/charter/graph/codegraph 0.019s`、`ok github.com/Xsxdot/charter/graph/webui 0.002s`。
- 2026-08-25：基线前端命令 `npm run typecheck`、`npm test -- src/app/codegraph/domainpage.test.ts`、`npm test -- src/app/codegraph/BestDomainPage.test.tsx src/app/codegraph/DomainCascadeDrawer.test.tsx`、`npm test -- src/api/client.test.ts src/app/codegraph/CodegraphPage.test.tsx`、`npm test`（均在 `graph/webui`）均退出 127，原始错误分别为 `sh: 1: tsc: not found` 或 `sh: 1: vitest: not found`；未据此归因，需先按锁文件安装依赖再复核。
- 2026-08-25：基线 `git diff --check` 退出 0、无输出。
- 2026-08-25：执行 `npm ci`（workdir `graph/webui`）成功，原始输出 `added 168 packages, and audited 169 packages in 1s`、`30 packages are looking for funding`、`found 0 vulnerabilities`，退出 0；仅恢复锁文件依赖，未改受版本控制文件。
- 2026-08-25：依赖安装后，`npm run typecheck` 退出 0，原始脚本输出 `tsc -b`；`npm test -- src/app/codegraph/domainpage.test.ts` 退出 0，原始摘要 `Test Files 1 passed (1)`、`Tests 4 passed (4)`；`npm test -- src/api/client.test.ts src/app/codegraph/CodegraphPage.test.tsx` 退出 0，原始摘要 `Test Files 2 passed (2)`、`Tests 22 passed (22)`；`npm test` 退出 0，原始摘要 `Test Files 18 passed (18)`、`Tests 112 passed (112)`。
- 2026-08-25：P1-B 基线 focused 命令 `npm test -- src/app/codegraph/BestDomainPage.test.tsx src/app/codegraph/DomainCascadeDrawer.test.tsx` 退出 1，Vitest 原始输出 `No test files found, exiting with code 1`，过滤文件为两个尚不存在的新测试文件；这证明该测试缝需先由实现轮点亮，不把失败归因于实现。
- 2026-08-25：`rg -n "slog|log\\.|console\\.(debug|info|warn)|print\\(" ...` 结果显示 charter Go core/CLI 没有现成结构化 logger，viewer 使用既有 `console.debug/info/warn` 结构化对象日志（`client.ts`、`useCodegraph.ts`、`CodegraphPage.tsx` 等）；计划对纯函数保持无副作用，将入口/错误/成功日志放在组件与既有 CLI 输出边界，不引入 `print` 或新日志框架。
- 2026-08-25：`besttree.ts#containerFacts` 现状以节点 `file` 最后一个 `/` 前的目录作为包目录，并将多目录容器归为 `dir:''`；计划 T2 复用该已验证口径，按选定组织的节点所属容器筛选 `CgGraph.packages`，只原样投影 `dir/summary`。
- 2026-08-25：已落盘法定计划 `docs/superpowers/plans/c1.10-plan.md`，713 行；包含 P1-B `BestDomainPage` 命名、T1～T4 有界文件集、精确接口、基线命令/预期、红绿步骤、日志/注释步骤、缺陷族、序列化边界、spec 故事归属与真机清单。
- 2026-08-25：计划自审命令 `rg -n 'TBD|待定|同 Task|适当|等等' docs/superpowers/plans/c1.10-plan.md | rg -v '反占位符扫描已执行'` 无输出，原始结果 `placeholder_scan=clean (self-audit line excluded)`；`git diff --check` 无输出；计划文件仍为未跟踪文件，尚未暂存/提交。
- 2026-08-25：计划自审修正：`BestLeafGraph` 仅在 `organization === 'best' && best` 时渲染；current 组织和 best 缺席只显示 T2 当前结构，避免把 best 叶图误当 current 画布。该约束已写入 `BestDomainPage` 的完整组件块和文字判据。
- 2026-08-25：首次执行 `git add docs/superpowers/plans/c1.10-plan.md docs/ledgers/2026-08-25-c1.10-plan-ledger.md` 失败，原始报错为 `fatal: Unable to create '/root/.handoff/repos/charter/.git/worktrees/82e1ca5a/index.lock': Read-only file system`，退出码 128；按当前工作树既有权限规则请求升级权限重试。
- 2026-08-25：同一 `git add` 命令以升级权限重试成功，退出码 0；计划文件与台账已进入 index。随后本条台账追加使其工作树状态需再次暂存。
- 2026-08-25：升级权限执行 `git add docs/ledgers/2026-08-25-c1.10-plan-ledger.md && git diff --cached --check && git diff --cached --stat && git status --short --branch` 成功；`git diff --cached --check` 无输出，stat 为 `docs/ledgers/2026-08-25-c1.10-plan-ledger.md | 20 +`、`docs/superpowers/plans/c1.10-plan.md | 714 +++++++++++++++++++++++++++`，合计 2 files changed、734 insertions；状态为两文件 `A`。
