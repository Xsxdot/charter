# C12.1 实现轮台账（K1 responsibility 同刀迁移）

执行者：handoff b25090c5 executor · 分支 cards/C12.1-charter-2（基线 HEAD e9fba66）

## 基线复核（本节点亲跑）

- `cd graph && go build ./... && go test ./codegraph/ ./cli/ -count=1` → 两包 ok。
- `cd graph/webui && npm ci` → added 168 packages；`npm run typecheck && npm test` → typecheck 退出 0；**Test Files 21 passed (21)，Tests 136 passed (136)**。与 plan §0 读数一致。
- 夹具 decls 现状：testdata/repo/codegraph/domains/ 仅 d_cmd.json（responsibility="命令入口与调度"）；d_svc 无声明。plan 的 TestAssembleContextSummaryComesFromDecls 断言值与此吻合。

## T1 红灯铺设

- Go：`go vet ./codegraph/` 过；`go test ./codegraph/ -count=1` 红 6 支，原文留证：
  - TestBestJSONGolden `got ...{"label":"协作控制","responsibility":"","type":"logic"}... want（无 responsibility）`
  - TestBestDomainWireShape `got {"label":"","responsibility":"","parent":"d_coordination"}` want 无键
  - TestValidateBestStopsEnforcingResponsibility `校验器不得再报 "领域 d_leaf 的 responsibility 不能为空"`
  - TestValidateBestIsPure `合法 best 不应有问题: [... responsibility 不能为空 ...]`——validBest 夹具随刀瘦身后的同因连带红，非范围外噪音，T2 转绿
  - TestAssembleContextSummaryComesFromDecls `Summary 必须等于 decl 正文，got "命令入口"`
  - TestMigrateTargetV2ProducesDualArtifacts Notes 计数 3≠2（占位符提示行仍在）
  - migrate_test.go:131 占位符断言随刀删除（plan 补遗表编译强制项，T1 一并处理，避免 T2 触测试文件）
  - `go test ./cli/ -count=1` → ok；`go build ./...` → OK。cli 层零范围外红。
- TS：focused vitest（6 文件）→ **6 failed | 61 passed (67)**，红全部落在新支：
  - besttree.test.ts×2：`expected '对外服务' to be '对外服务的声明正文'`（旧 best 来源仍在）；`expected '对外服务' to be undefined`
  - BestDetail.test.tsx：`[data-declaration-text]`/`[data-declaration-missing]` 查询为 null
  - BestPanorama.test.tsx：missing 数 0≠2
  - BestScopePanorama.test.tsx：写入路径文本缺席
  - CodegraphPage.test.tsx：装配层缺未声明态
  - client.test.ts 新回放对**绿**（保持项，锁传输边界语义，不参与红灯计数）
- T1 完成：Go 红 5+1 连带、TS 红 6、保持项绿。进入 T2。

## T2 Go 半边最小实现

- 刀口：best.go 删 BestDomain.Responsibility 与 ValidateBest 非空检查（结构体注释补 decl 唯一所有者）；context.go LoadDomainDecls 上移、contextVocabulary 加 decls 参、best 分支 Summary=declSummary(decls,id)（新私有 helper，置于 unknownBestDomainError 前）；降级分支 domain.Summary 不动；migrate.go 去占位符写入与提示行；testdata best.json 三 domain 剥键。
- **plan 清单外新增两处同刀编译强制点**（vet 抓出，性质同补遗表 migrate_test.go:131）：
  - `graph/codegraph/check_test.go:41` `Responsibility: "fixture",` —— 夹具字段行，机械剥除；
  - `graph/codegraph/gap_test.go:31-35/:93-94` 八处 BestDomain 字面量字段 —— 机械剥除。
  两处均只删字段行，不改任何断言。plan §0「check.go 不调用 ValidateBest」的判据仍真——这两处是测试夹具的字段字面量，不是校验行为断言。
- 绿灯证据：`go build ./... && go vet ./codegraph/ ./cli/ && go test ./codegraph/ ./cli/ -count=1` → 两包 ok。
- 七支目标测试 -run 单跑全 PASS（Golden/WireShape/StopsEnforcing/LegacyKey/SummaryComesFromDecls/V2ProducesDualArtifacts/ValidateBestIsPure）。
- `grep -n "Responsibility" graph/codegraph/best.go` → 零输出（exit 1）。
- 生产侧残余 Responsibility 引用全部合法：decl.go:23、decls.go:45/:55、entity.go:72（decl 新所有者）；migrate.go:48（冻结 v2 解码器，未动）；context.go:379（declSummary 读 decls）。

## T3 TS 半边最小实现

- 刀口：types.ts CgBestDomain 删 responsibility；besttree.ts 三模型字段转可选+declaredResponsibilityOf 唯一取数点+三导出函数加 decls 尾参（scopeCard 加第 5 参透传）；三组件 Props 加 decls、渲染位按 DOM 契约二态化（BestDetail 用 span 内联保留「理想子系统 ·」前缀行，Panorama 两处为独立 div；ext 卡剥壳 card.id.slice(4)）、useMemo 依赖补 decls；CodegraphPage 三挂载点穿线 decls={data.decls}。
- 夹具换底：besttree.test.ts / BestDetail.test.tsx / BestPanorama.test.tsx / BestScopePanorama.test.tsx / CodegraphPage.test.tsx / domainpage.test.ts / BestDomainPage.test.tsx / BestLeafGraph.test.tsx 的 best 字面量全部剥键；decl 夹具的 responsibility 全部保留。
- 静态复核：`responsibility` 在 src/api/types.ts 仅剩 :36（CgDomainDecl）；besttree.ts 无任何 best 域对象同名字段读取，唯一读取点是 declaredResponsibilityOf 内部。
- 绿灯证据：`npm run typecheck` → tsc -b 退出 0；focused vitest（codegraph 目录+client.test.ts）→ **20 files passed, 136 tests passed**，T1 六支红灯全转绿。

## T4 全量回归、变异复验与单提交

- 全量回归（亲跑原文）：`go build ./... && go test ./... -count=1` → cli / codegraph / webui 三包 **ok**；`git diff --check` 干净。`npm run typecheck` → 0；`npm test` → **Test Files 21 passed (21)，Tests 143 passed (143)**（基线 136 → 净增 7 支新断言，无 skip）。
- 静态刀口复核：`grep -n "Responsibility" graph/codegraph/best.go` 零输出；types.ts 仅 :36 一处（CgDomainDecl）；`domains?[...].r|domain.responsibility` 正则扫 besttree.ts 零输出。
- 变异钻取一（TS 兜底回填）：先 `grep -c "declaredResponsibilityOf(decls, id)"` ==1 断言命中唯一，再临时改 `?? domain.label` → besttree.test.ts「undefined」断言红 + BestPanorama「missing」断言红（2 failed | 24 passed）；恢复后同两文件 26 全绿。
- 变异钻取二（Go 金样本回流）：临时回填 `Responsibility string \`json:"responsibility"\`` → TestBestJSONGolden、TestBestDomainWireShape 两支 --- FAIL；删除恢复后 -run 两支 ok。两次变异副本均以 git diff 确认未残留。
- 越界路径扫描：git status 改动集 = Go 9 文件（4 生产/夹具 + 3 测试 + testdata + 台账外）+ TS 15 文件 + 台账，无 internal/agentd、codegraph/domains/*、docs/codegraph-scan-recipe.md。
- 提交形态：T1~T4 合并为单提交（contract §2.2-8/-10 冻结条压倒逐 task 惯例），不 push。
