# C15 台账

- 2026-08-27：用户在 C14 分支上指定两点：箭头改接到边框（保留当前间距）；默认不透明，点击无关项才透明，再点取消选中。开卡 C15，定级 L1。
- 2026-08-27：现状读数——`ScopeCanvas.tsx` `centerOf` 把边接到卡心；`renderCard` 在非选中/非邻居分支无条件 `opacity-50` + `data-dimmed`（`ScopeCanvas.test.tsx` 空选中即断言 `data-dimmed=true`）。原型 `graph.js:54-64` 正向下沿→上沿、回边右沿、同层下沿；`dim = id => o.sel && o.sel !== id`（领域层）/ `dimN = id => o.sel && !near.has(id)`（容器层）。
- 2026-08-27：TDD 首红——`scopeEdgeAnchors is not a function`（缝 1 新符号）；空选中 `data-dimmed` 期望 null 实得 `"true"`（缝 2）。
- 2026-08-27：`npm run typecheck` 退出 0；`npm test` 16 files / 221 tests 全绿。
- 2026-08-27：变异三发均编译过且被拦住——①去掉 `selectedNodeId` 守卫；②正向锚点改卡心；③取消选中改成始终 `onSelect(node.id)`。
- 2026-08-27：`npm run build` 产出 `index-NXNuPSzx.js` / `index-BJCLfF_x.css`。SuperDev 登记的 `charter-codegraph-webui` @ 18789 本机 `curl` 连不上，真机点击未在本轮完成。
