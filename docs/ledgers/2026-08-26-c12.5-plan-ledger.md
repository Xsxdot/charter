# C12.5 plan 节点台账（K5 行为轴流程图页面）

> 本节点：plan（产出 `docs/superpowers/plans/c12.5-plan.md`）；工作树零 src 改动；
> 全部验证在沙箱副本 `/root/.handoff/tmp/af29849d/k5probe/webui` 内亲跑。
> 实现轮执行者把每次红/绿原始输出**追加**到本文件。

## 基线与事实（plan 节点亲跑）

- 分支 `cards/C12.5-charter`，`git merge-base --is-ancestor cards/C12.4-charter-3 HEAD` → 基线包含确认。工作树干净。
- 协调者更正：`prototypes/codegraph-two-axis/pages/behav-flow.html` **不在基线也不在本机**（gitignored 分支副本，只存在协调者本机）。蛇形算法由协调者从本机原型逐字摘录提供（两步法：多列布局 + 奇数列整列上下翻转；锚点绘制时按坐标现算；TOP=46/BAND=30 预留带必须保留；画布高从 BOT+带算）。原型 wrap 线上的「接下列 ↑」文字徽章是改造前残留，**禁止复制**——breakdown K5 验收第 3 条反面断言编码的正是禁止它。
- 沙箱基线绿：`npm ci`（168 packages）→ `npm run typecheck` 退出码 0；`npm test` → **Test Files 28 passed (28)，Tests 248 passed (248)**。（K4 收口读数 28/236 之后，0211cea 补正轮加了测试——以本轮亲跑 248 为准。）
- 缝 2 模型读毕（flowpage.ts 376 行）：FlowPageModel 顶层九键恒定 `entryNodeId/entryFound/degraded/ownership/registrationDispersion/family/steps/callChain/danglingChildRefs`；FlowStepView 含 id/order/kind/line/to?/cond?/then?/else?/body?/targetName/targetDomain/targetIsEntry/iface?/implementations/unknownKind。文件头明文「蛇形折列等画布布局是消费侧关注点（K5），本模块不输出坐标」——布局归 flowlayout 纯函数有缝内授权。
- 形态基准（prototypes/base/README.md『C12 形态确认』）：蛇形折列第 1 列自上而下从底部接进第 2 列、第 2 列自下而上从顶部接进第 3 列；紫框 ▸ 下层入口递归下钻；双线框接口调用右栏列全部实现；降级一律显式。

## 沙箱探针记录（plan 节点亲跑，全部在 /root/.handoff/tmp/af29849d/k5probe/webui）

- flowlayout.test.ts 首跑 **1 failed | 11 passed**：7 步两列分布期望写 4/3、实际算法选 k=0 得 3/4——均衡模拟没有把填充夹在 cols 内（会溢出第 3 列），修实现（fillCounts 最后一列吸收剩余 + 均衡模拟同规则）；修复后测试期望按确定性输出改 3/4。教训：折列填充必须恒等于 cols 列，否则 wrap 跨列跳跃。
- flowlayout.test.ts 终版 **12 passed (12)**；FlowChart.test.tsx 首跑 10/11——卫语句位置断言用 `Number(style.left)` 在 jsdom 得 NaN，改用 data-x/data-y 位置标记（本就是 breakdown「位置标记断言，非像素」的正确形态）后 **11 passed (11)**；flowlayout.test.ts 曾有错误 import 路径 `'../../../src/api/types'`（多打一层）被 tsc 抓住改正。
- FlowPageView.test.tsx 首跑 **14 passed (14)**（清理一处三元两支相同的笔误后复绿）。
- 全量：`npm run typecheck` 退出码 0；`npm test` → **Test Files 31 passed (31)，Tests 285 passed (285)**＝基线 28 文件/248 支 + 本卡新增 3 文件/37 支（flowlayout 12 + FlowChart 11 + FlowPageView 14），对账分毫不差。
- 变异 A（degraded 时也渲染 FlowChart）：首版断言 **没红**（14 全过）——degraded 时 steps 为空，空画布不产生 [data-step]，反面断言空转＝假绿陷阱。修正断言为三重锁（[data-flow-degraded] 在场 ∧ [data-flow-chart] 为 null ∧ [data-step] 为 null）后复验：**1 failed | 13 passed**，恢复后全绿。
- 变异 B（折列 path 换成文字「接下列」）：**1 failed | 10 passed**（wrap path 存在性 + 文字反面断言同支命中），恢复后全绿。
- 变异 C（卫语句不甩侧并入主干行）：**2 failed | 21 passed**（flowlayout 卫语句支 + FlowChart 位置支双红），恢复后全绿。
- 恢复确认：三文件从备份还原后全量 285/285 + typecheck 0 复跑通过。
- 工作树边界：全程仅台账 untracked（git status --short 只显示 docs/ledgers/2026-08-26-c12.5-plan-ledger.md），src 零改动。

## 计划文档收口（plan 节点亲跑）

- 一致性修正：沙箱 FlowPageView.tsx 头注释引用缺口登记旧编号 §1.5 → 改 §1.4（与计划文档章节号对齐），注释级变更；复跑 `npm run typecheck`（0）+ 全量 `npm test` → **285 passed (285)** 确认仍绿。
- 逐字校验：python 脚本从 `docs/superpowers/plans/c12.5-plan.md` 提取全部 ```ts/```tsx 围栏块，与沙箱六文件逐一 diff → **ALL_VERBATIM**（6/6 匹配）；`<!-- CONTINUE -->` 拼装标记无残留。
- 本节点产出物：`docs/superpowers/plans/c12.5-plan.md`（T1~T4 四段式 + 蛇形算法规格 §1.5 + 模型缺口登记 §1.4 + 缺陷族八族 §6 + 序列化边界十二项 §7 + 验收映射 §8 + 故事归属 §9 + 真机细化 §10 + 自审 §11）。
- 自查：①未把没亲跑到结果的命令写成结论——计划内全部期望读数（28/248 基线、37 支新增、31/285 全量、变异 A/B/C 各自红数）均出自上列本机亲跑；②本轮零 handoff CLI 调用、零新 executor 进程。


