# C12.3 plan 节点台账（K3 缝 2：deriveFlowPage 行为轴视图模型）

> 节点：charter plan（产出物 `docs/superpowers/plans/c12.3-plan.md`；本文件是该节点的亲跑记录，
> 实现轮执行者把每次红/绿原始输出**追加**到本文件）。
> 基线：分支 `cards/C12.3-charter`，HEAD ee46218（含 K1 同刀迁移 + K2 缝 1 实装 + 契约修订 R1/R2）。
> 纪律：只读本仓图数据允许 codegraph graph 子命令；不派发、不起 executor；判据先在基线跑。

## 亲跑记录（plan 节点）

- 2026-08-26 分支确认：`git branch --show-current` → `cards/C12.3-charter`；
  `git log --oneline -8` 首行 ee46218 docs(C12): 契约修订 R1/R2；工作树干净。R1（触达域散度归缝 2）在基线内已核对（c12-contract.md §8）。
- 消费面 grep（pattern `flowpage|deriveFlowPage|FlowPageInput|FlowPageModel|passthrough` over graph/webui/src）：
  命中仅 flowpage.ts（壳自身）、ticket0.passthrough.test.ts（:7 import、:18/:21/:29 断言）、scopepage.test.ts:228/:237（缝 1 自己的 passthrough 退役断言，与缝 2 无关）。组件层无消费方（K5 才接）。
- `graph/webui/node_modules` 初始缺失 → `npm ci --no-audit --no-fund` → added 168 packages（退出码 0）。
- 基线绿亲跑：`cd graph/webui && npm run typecheck` 退出码 0；`npm test` → **Test Files 22 passed (22)，Tests 163 passed (163)**（= K2 收口读数，对账一致）。
- implements 方向核对：`graph/codegraph/types.go:153-155` 注释原文「Implements 是接口满足边 [实现, 接口]」——pair[0]=实现符号、pair[1]=接口符号。join 规则据此写死。
- FlowStep wire 形状核对：`graph/codegraph/types.go:113-128`（ID/Order/Kind/To/Cond/Line/Then/Else/Body/Iface）；TS 镜像 `graph/webui/src/api/types.ts:19-30`。字段必填性由扫描侧保证、引用完整性 Validate 随 flows 真数据同批开启（types.go:116-117 原文）→ 派生器必须容忍悬空子干引用并显式标注。
- 原型副本不在库：`prototypes/codegraph-two-axis/` 不在本工作树也不在 git（prototypes/.gitignore 只放行 base/，base/README.md:34 明言「副本删除也不会丢」）——入口族切分规则无原型代码可抄，按 spec 文本 + 夹具固化假设（breakdown K3 缺陷族表「跨平台假设」行的既定要求）。
- tsconfig.app.json 亲读：strict + noUnusedLocals + noUnusedParameters + verbatimModuleSyntax + erasableSyntaxOnly——计划代码块按此书写（import type、无 enum/namespace）。
- vite.config.ts 亲读：vitest environment jsdom + setupFiles src/test/setup.ts；`npm test` = vitest run。
- focused 命令形态验证：`npm test -- src/app/codegraph/ticket0.passthrough.test.ts` → 2 passed（命令过滤可用）。

## 协调者更正留痕（2026-08-26）

- plan 节点首轮曾在**工作树** `rm ticket0.passthrough.test.ts` 做探针，被协调者拒绝。两条理由：①plan 节点不改代码，产出物只有计划与台账；②ticket0 整文件退役归 K6（contract §2.5 退役集），且 C12.2 审阅已查出该文件存在值级断言无后继的风险——删掉＝覆盖静默丢失，契约 §4.2「取代」是覆盖平移不是删掉了事。处置改为：C12.1 先例的沙箱副本验证 + 计划内「值级断言→后继」逐支对照表 + ticket0 最小行级处置（只删 passthrough 一行断言）。

## 沙箱探针亲跑记录（/root/.handoff/tmp/bf95ec75/opencode/k3probe/webui，cp -a 自工作树，工作树全程未动）

- 方法：`mkdir -p …/k3probe && cp -a graph/webui …/webui`（含 node_modules）；在沙箱内应用计划的 T1 测试全文 + T2 实装全文 + ticket0 删一行（:21 passthrough 断言）+ 头注释两行更新。
- **红灯相**（Ticket 0 壳 + 终版 16 支测试）：`npm test -- src/app/codegraph/flowpage.test.ts` → **Tests 16 failed (16)**，红因 TypeError 群（undefined 上读 nodeIds/map/find）+ 键集断言失配——回声壳缺全部新字段，符合红灯清单预期。
- **绿灯相**（实装落地）：focused → **16 passed (16)**；ticket0（删一行后）→ **2 passed (2)**；`npm run typecheck` → 退出码 0（真码捕获：`npm run typecheck > log 2>&1; echo $?`，首轮用管道 tail 取 `$?` 的做法取到的是 tail 的码，已纠正）；全量 → **Test Files 23 passed (23)，Tests 179 passed (179)**（基线 22 文件/163 支 + flowpage.test.ts 新增 16 支 = 179，对账分毫不差；ticket0 两支保留）。
- **变异钻取**：临时把 `degraded: flow === undefined` 反转为 `!==` → focused **5 failed | 10 passed**，转红名单：「flows 键整体缺席」「flows 在但该入口缺席」「flows 命中该入口」「幽灵入口显式降级」「steps 按 order 升序稳定排序」（末支因断言 degraded=false 而红）。时点注记：变异跑在 15 支版套件上；其后追加「领域链上溯」（只断言 ownership，与 degraded 无关）未重跑变异——implement 轮按 T3 指令重做时以实际输出为准。复原后 focused 回绿。变异副本未入库（沙箱内操作）。
- **探针抓掉两处真缺陷（写进计划的版本已修正）**：
  1. 夹具领域字面量缺 `CgDomain.kind` 必填字段 → `tsc -b` **TS2741 ×18**（vitest/esbuild 不查类型，只有 typecheck 能抓；TYPECHECK_EXIT=2 原始输出在案）。修正＝全部领域夹具补 `kind: 'boundary'|'logic'`。
  2. 族期望值笔误：入口名 'flow cmd' 按「去末词」族规则族 id 是 `'flow'`，测试初稿误写 `'flow cmd'` → AssertionError（expected familyId 'flow cmd', received 'flow'）。实装行为正确，修期望值。
- 探针后追加「领域链上溯」（parent 链投影专断言）一支并**重跑两相**：红灯相 16 failed (16)、绿灯相 16 passed + typecheck 0 + 全量 23/179，以上读数均为终版套件读数。
- 工作树核验：探针全程 `git status --short` 仅 `?? docs/ledgers/2026-08-26-c12.3-plan-ledger.md`；flowpage.ts 经 `git checkout --` 复原为 Ticket 0 壳，flowpage.test.ts 未落入工作树。

