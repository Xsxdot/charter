# C12.2 plan 节点台账（K2 缝 1 实装：deriveScopePage）

> 节点：charter/plan（产出物 = `docs/superpowers/plans/c12.2-plan.md`，不是实现代码）
> 分支：`cards/C12.2-charter`（base = cards/C12.1-charter-2，HEAD 4efc1b3），工作树干净起步。
> 纪律：边干边落；每确立一个事实追加一行；未亲自跑到结果的命令不写结论。

## 基线亲跑记录（2026-08-26）

- `git status && git branch --show-current && git log --oneline -5` → On branch cards/C12.2-charter; nothing to commit; HEAD 4efc1b3（K1 实装提交），前一条 e9fba66（C12.1 plan）。
- `cd graph/webui && npm ci` → 成功（30 packages funding 提示，found 0 vulnerabilities）。首次跑 typecheck 因依赖未装报 `sh: 1: tsc: not found`——先装依赖再跑，非代码问题。
- `cd graph/webui && npm run typecheck` → 退出码 0（tsc -b 无输出）。
- `cd graph/webui && npm test` → **Test Files 21 passed (21)，Tests 143 passed (143)**，vitest v4.1.11，node v24.16.0。（contract §4.2 记录的 Ticket 0 基线是 21 文件 / 136 测试；K1 净增 7 支后为 143。）
- `npm test -- src/app/codegraph/ticket0.passthrough.test.ts` → Test Files 1 passed (1)，Tests 3 passed (3)——**focused 过滤语法 `npm test -- <path>` 在 vitest v4 可用**，breakdown K2 验收第 1 条命令形态可照抄。

## 机械圈定与现状事实（grep/read 亲证）

- 仓根无 `codegraph/` 项目图（`ls /root/.handoff/worktrees/f46de67d/codegraph` → No such file or directory）：存量无图判定与 contract §1 一致，本节点不用 codegraph CLI，Ticket 0 后新符号依旧无图可入。
- `graph/webui/src/app/codegraph/scopepage.ts` 全文 30 行：`ScopePageInput` 五字段已钉（§2.3-19）、`ScopePageModel = { scopeId, passthrough: true }`、`deriveScopePage` 回声壳。缝地址与输入字段与冻结一致，未动。
- **scopepage 消费面 grep**（pattern `scopepage|deriveScopePage|ScopePageInput|ScopePageModel` over graph/webui）：仅两个文件命中——scopepage.ts 自身 + ticket0.passthrough.test.ts（:8 import、:19 调用）。组件层尚无消费方（K4 才接）。
- `passthrough` 引用面 grep：ticket0.passthrough.test.ts :21（缝 1 断言）/:28（缝 2 断言）、flowpage.ts :20/:27（缝 2 自有字段，K3 管）、scopepage.ts :25/:29。
- **机械圈定补遗（breakdown K2 有界文件集漏 1 处）**：删 `ScopePageModel.passthrough` 字段会同时强制改 `ticket0.passthrough.test.ts`——:21 断言运行时必红 + tsconfig `noUnusedLocals: true` 使 :8 的 `deriveScopePage` import 在删除缝 1 用例后变成编译错。两处都是编译/测试强制点，随 T2 同一提交处置。
- `tsconfig.app.json`：`strict`、`noUnusedLocals`、`noUnusedParameters`、`verbatimModuleSyntax`（类型必须 `import type` / inline `type` 修饰）、`erasableSyntaxOnly`（禁 enum/namespace）。计划里的全部代码块按此书写。
- `package.json` scripts：`test = vitest run`、`typecheck = tsc -b`；无独立 lint script。
- **纯派生层零 console 约定亲证**：`console.*` grep 命中 client.ts 与各组件（`[codegraph]` 前缀结构化日志），besttree.ts / domainpage.ts / scopepage.ts 零命中——纯函数层的诊断出口是模型显式字段，不是日志。
- `types.ts` 现状形状（逐行读过）：`CgContainer {label, kind, entry?, domain?}`(:6)；`CgNode.kind: 'entry'|'func'|'model'` + `modelKind?` + `channel?`(:7-12)；`CgGraph.edges: [string,string][]`、`projections?: [string,string,string][]`(:42)；`CgDomain.parent?`(:5)——current 组织的领域树导航可用；`CgBestDomain` 已无 responsibility（K1 同刀完成，:63），besttree 经 `declaredResponsibilityOf` 把空串归一成 undefined（besttree.ts:172-177）。
- Projection 三元组语义亲证：Go `graph/codegraph/types.go:138-141` —— `[投影点节点 id, model 节点 id, kind]`。
- besttree 可复用导出（只读，签名未变）：`topLevelSubsystemIds`、`subsystemOf`、`childBestDomainIds`、`containerSubsystems`、`containerFacts`、`isBestLeaf`、`assembleDirections(target?, report?) => DirectionReadout[]`（含 `status: DirectionStatus` 四档）、`bestScopeGraph`。C12.1 plan 冻结的「K2 只消费 topLevelSubsystemIds/subsystemOf/containerFacts，签名不变」核对无误，另加只读消费 assembleDirections/childBestDomainIds/isBestLeaf（均为既有公开导出，不改其行为）。
- domainpage.ts 现状（§2.5-37 退役对象，本卡不动）：坏取法 `inboundEdges.slice(0, DOMAIN_FOCUS_QUOTA)` 在 ：281，常量 :17；`edgeContexts`/`callerDomainsByNode` 私有 helper 在 ：134/:146——吸收处置归 K6（contract §7），K2 只作算法参考。
- 台账落点沿用项目约定 docs/ledgers/（16 个既有台账同目录，命名 `YYYY-MM-DD-<卡>-<节点>-ledger.md`）。

## 判断与裁决留痕

- **触达域散度的缝归属裁决**：contract §2.3-22 把四类债读数（含触达域散度）写在缝 1 名下；但 spec《测试决定（接缝清单）》把「触达域散度」列在**缝 2** 的覆盖清单里，breakdown K3 验收第 5 条也把它的数值化断言派给 K3，而 contract §2.4 对 flowpage 输出反而没有对应条款。三处对读，唯一自洽的归属是缝 2/K3；且缝 1 若要算它，必须先实现入口族分组（§2.4-34）或入口归属判据（§2.4-32）——两者都是 K3 的判据，提前实现即越缝复制。**裁决：缝 1 输出兜底桶占比/复用度/真假共享内核三类读数，触达域散度归 K3，计划 §7 显式认领此裁决并写明出处**。若协调者不认可，走返工而不是执行者自造。
- **布局算法不在本卡**：布局选型归 plan（contract §7），但布局是画布关注点，消费方是 K4 的 ScopeCanvas；K2 模型只供布局所需的拓扑事实（节点/边/权重/方向），不输出坐标。坐标计算落点在 K4 plan 圈定，本计划在 §7 显式声明该边界，防止 K4 落空。
- **对外面数据必须在缝 1 出口**：spec 故事 10 的折叠展开、「不占名额」都是右栏渲染行为，K4 只消费 ScopePageModel；折叠判据阈值又只许住在派生器模块（§2.3-23）——因此符号粒度入缝清单（含 folded 标记）必须是模型字段，否则 K4 无米下锅或被迫第二套口径。

## 计划代码块探针验证（plan 节点亲跑，验后完整还原）

做法：从计划文档机械提取第 2/3 个 ts 代码块（测试与实装），按 T2 指令临时应用到工作树（scopepage.ts 整体替换 + 新建 scopepage.test.ts + ticket0 按 T2 步骤 2 修剪），跑绿后 git checkout 还原两个跟踪文件并删除新文件，git status --porcelain 确认仅剩本台账与计划文档。

- 第一轮探针抓到**真缺陷两处**（这正是「判据先在基线跑」的价值）：
  1. 实装引用 input.report 两处（misplaced 统计 + assembleDirections 债色）——但 contract §2.3-19 钉死输入只有六字段，report 可选键不在其中，tsc 报 TS2339「Property 'report' does not exist on type 'ScopePageInput'」。处置：删 ScopeNode.misplacedCount 与 ScopeEdge.status 两字段及其全部计算路径；直调债四档色的 join 显式移交 K4 装配（用既有纯函数 assembleDirections(target, report)，非组件重算）。misplacedCount 同理依赖 report.warns，一并删除。
  2. 实装 import 漏 CgTarget——接口引用它但未导入，tsc 报 TS2304「Cannot find name 'CgTarget'」。处置：import 行补齐。
  另修正测试夹具两处自相矛盾（实装行为正确、断言写窄了）：中层与容器层的圈外端点同样折 ext 卡保留横跳可见性，故 ss_a 中层应见 ext:ss_b 与 ext:ss_c 两张圈外卡、a_mid 叶层应见 c_fb 加两张 ext 卡。
- 第二轮探针全绿（原始计数行）：npm test -- src/app/codegraph/scopepage.test.ts src/app/codegraph/ticket0.passthrough.test.ts → **Tests 23 passed (23)**；npm run typecheck → tsc -b 无输出退出 0；npm test 全量 → **Test Files 22 passed (22)，Tests 163 passed (163)**。
- 数字对账：基线 143 支 − ticket0 缝 1 用例退役 1 支 + scopepage.test.ts 新增 21 支 = 163 支，分毫不差；文件数 21 + 1 = 22。
- 探针后工作树还原干净：git status --porcelain 仅剩本台账与 c12.2-plan.md 两个未跟踪文件。
- 结论：T2 的 focused 绿、typecheck 绿、T3 步骤 3 的全量回归数字已由 plan 节点亲证；唯 T1 红灯阶段未亲跑（壳已还原，红灯是对回声模型的推断），实现轮执行者仍须按 T1 先跑红再进 T2。
