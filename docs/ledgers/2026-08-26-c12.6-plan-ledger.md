# C12.6 plan 节点台账（K6 装配切换、退役与整轮收口）

> 节点：charter 流程 plan 节点；分支 cards/C12.6-charter（基线链 P1 约束：上游 = cards/C12.5-charter-2 @ 71763a3）。
> 本文件随跑随记：每确立一个事实追加一行；原始输出照抄不归因。

## 基线复核（判据先在基线跑）

- 2026-08-26 `git status`：On branch cards/C12.6-charter, working tree clean；HEAD 71763a3（= cards/C12.5-charter-2 同一提交，`git log --oneline cards/C12.5-charter-2 -1` 输出 71763a3）。
- 2026-08-26 `cd graph/webui && npm ci`：added 依赖装齐，found 0 vulnerabilities。
- 2026-08-26 `npm run typecheck`：退出码 0（tsc -b 无输出）。
- 2026-08-26 `npm test`：**Test Files 31 passed (31)，Tests 285 passed (285)**。
- 2026-08-26 `cd graph && go build ./... && go test ./... -count=1`：GO_BUILD_OK；cli ok 0.209s / codegraph ok 0.032s / webui ok 0.003s，全部通过。

## 现状机械枚举（importer 全图，grep 实跑）

- `grep -rln "from './X'"` 逐模块枚举结果（生产代码 importer，不含 .test）：
  - besttree ← scopepage.ts、TwoAxisPage.tsx、ScopeCanvas.tsx、MigrationDrawer.tsx、CodegraphPage.tsx、Best* 五组件 → K6 重接线后前四个留存，besttree 必留。
  - domains ← CodegraphPage.tsx、domainlayout.ts、CallTree/FocusGraph/DomainDetail/DomainPanorama/BestPanorama/BestScopePanorama → 重接线后仅 CodegraphPage（hasDomains 单域提示 pill）+ domainlayout（退役）。
  - graphmath ← domains.ts、CodegraphPage.tsx（mergeView）、CallTree/DetailPanel/DomainDetail/DomainPanorama/FocusGraph/BestPanorama/BestScopePanorama → 重接线后 domains.ts + CodegraphPage(mergeView)。
  - domainlayout ← DomainPanorama.tsx、BestPanorama.tsx、BestScopePanorama.tsx（全在退役集）→ 删三组件后零调用方 → 随刀删。
  - domainpage ← BestDomainPage.tsx、DomainCascadeDrawer.tsx（均在退役集）→ §2.5-37 删。
  - DetailPanel 的 fetchCodegraphSource 消费随组件删除；api/client.ts 导出面不动（client.test.ts 直测）。
- ticket0.passthrough.test.ts 现存 2 支 degraded 值级断言的后继已核实在 flowpage.test.ts：『flows 键整体缺席…』(:427) 与 『flows 命中该入口 → degraded=false』(:447)——整文件删除零覆盖间隙（c12.3 T2 对照表兑现）。
- flowpage.test.ts :406 『模型顶层九键冻结』断言键集为 9 键——entryName 扩展后须同步改 10 键（本卡修改集内）。
- `passthrough` 字样现存于 scopepage.test.ts :228/:237——那是「直通标记退场」的反向锁本身（hasOwnProperty=false 断言），必须保留；故 K6 收口 rg 检查对 passthrough 采用「非测试文件零匹配 + 测试文件仅剩反向断言」的符号级模式（breakdown K6 验收 2 的模式按 c12.3 预告调整，弱化不存在）。

## 沙箱探针（/root/.handoff/tmp/811560e9/k6probe）

（待补）

## 沙箱探针结果（/root/.handoff/tmp/811560e9/k6probe，副本与工作树 src 逐字同源起步）

- 沙箱基线复跑 `npm test`：Test Files 31 passed (31)，Tests 285 passed (285)——与工作树基线一致。
- T1 缝 2 扩展（flowpage entryName + 测试 2 支 + 九键改十键）：focused 21 支全绿。
- T2 页面壳接线（FlowPageView 标题/祖先链 + 测试 3 支）：首跑 1 失败——测试自身 bug
  （`q()` 找不到元素时 throw 而非返回 null，断言「不在场」误用 q）；改为 querySelector 后 focused 17 支全绿。
- 装配初版踩坑（重要现状事实）：`mergeView` 返回 CgView，edges 是 `{from,to,status}` 对象数组，
  与缝 1 冻结输入 CgGraph 的 `[string,string][]` 元组不兼容——把合成视图喂 deriveScopePage 运行期即炸
  （`.for is not iterable` @ scopepage.ts:302）。**缝 1 六字段冻结输入没有 diff 维度，两轴无法消费 diff 视图**；
  装配改为「diff 视图选中时仍渲染基准快照 + 显式告知条」。此为设计裁决，plan §1.3 留痕待拍板。
- hasDomains(v: CgView) 对 baseline.domains=undefined 直接 TypeError（domains.ts:37）——旧 util 的签名
  建立在 CgView 上，新装配不消费；连同 graphmath/domainlayout 一并按 P5-A 无调用方即删（枚举见 plan §0）。
- 删除 33 文件 + theme.test.ts 注释两行 + 装配重写后：typecheck 退出码 0（无暗引用）；
  全量 **Test Files 14 passed (14)，Tests 193 passed (193)**。
- 新增 classname-drift.test.ts（8 支）→ 15/201；新增 CodegraphPage.wire.test.tsx 穿线回归（2 支，
  breakdown K6 序列化边界族钦定的 wire→types→模型→组件全链一次）→ 终态 **16 文件 / 203 tests**；
  文件数对账：31 − 17 + 2 = 16，分毫可查。wire 首跑 2 失败——夹具默认组织＝最优树
  （best 在场时 TwoAxisPage 初始 organization='best'），根层卡是 s_api/s_store 而非 d_cli；
  先断最优卡再切「按现状领域」后修正通过。
- 中途一次 typecheck 抓到夹具缺字段：CgStaleNode 需 file/line（stale 夹具补齐后过）。
- 变异四条全部转红后复原：①抹 MigrationDrawer 触发钮 className → drift 1 红；复原 8 绿。
  ②NOISE_FOLD_REUSE_THRESHOLD 10→9 → scopepage 2 红。③degraded 反转 → flowpage 5 红。
  ④unknownKind 计数静默化为 0 → scopepage 1 红。
- rg 残留五词检查（沙箱 src 全域）：deriveDomainPage=0、DomainCascadeDrawer=0、CallTree=0、FocusGraph=0、
  passthrough 仅 scopepage.test.ts :228/:237 两处反向锁（非测试文件 0）；探针文件塞回 deriveDomainPage 导出 → grep=1，删除 → =0（M5 牙口验证）。
- 终态复跑：typecheck 0；全量 15 文件 / 201 tests 全绿。

## 工作树零改动声明

- 本节点全程只写 docs/（本台账 + c12.6-plan.md）；graph/webui 实现改动只存在于沙箱，
  待 implement 节点按 plan 落地。node_modules 为 npm ci 产物（gitignore 内），不入库。

## 出稿与自检

- 2026-08-26 计划落盘：docs/superpowers/plans/c12.6-plan.md（1077 行；T1～T5 五 task，全部代码块为沙箱绿灯版逐字照抄）。
- 自审三查、五项检查、占位符扫描、内部锁声明（1 处：classname-drift 源码扫描）、跨卡审计（单上下文兜底，三处待拍板：D1 diff 视图改判 / D2+D3 删除集按枚举扩大 / K6 验收 1「≥21 文件」计数作废备案）均已在计划 §7-§10 落款。
- 交叉引用自查修正 6 处（§编号漂移/vitem 错字/步骤指针）；哨兵行比对：计划代码块与沙箱终版逐字一致。
- 本节点工作树改动 = 本台账 + c12.6-plan.md 两文件；graph/webui 实现只存在于沙箱，归 implement 节点。

# 实现轮追加（C12.6 implement 节点，2026-08-26）

## 基线复跑（工作树，cards/C12.6-charter-2 @ f9e62c2）

- `cd graph/webui && npm ci`：found 0 vulnerabilities。
- `npm run typecheck` → 退出码 0。
- `npm test` → Test Files 31 passed (31)，Tests 285 passed (285)。
- `cd graph && go build ./... && go test ./... -count=1` → GO_BUILD_OK；cli/codegraph/webui 全 ok。

## T1 flowpage entryName（红→绿）

- 写红后 `npm test -- src/app/codegraph/flowpage.test.ts`：
  Tests 4 failed | 17 passed (21)。失败四支＝①键集断言 expected [ 'callChain', …(8) ] to deeply equal [ 'callChain', …(9) ]；
  ②幽灵入口用例 expect undefined to be ''；③④两条新用例 expect undefined to be 'flow cmd' / to be ''。
  （plan §2 步骤 2 预期 3 失败；实际 4——多的一支是步骤 1b 在幽灵用例插入的 entryName 断言所在用例，
  四支失败原因全部为 entryName 功能缺失，无 typo。）
- 最小实现两处（interface 十键 + return 一行）后同命令：Tests 21 passed (21)；`npm run typecheck` 退出码 0。
