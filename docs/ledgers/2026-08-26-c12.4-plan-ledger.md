# C12.4 plan 节点台账（K4 结构轴页面组件与右栏三 tab）

> 本文件 = C12.4 plan 节点亲跑记录。实现轮执行者把每次红/绿原始输出**追加**到本文件。
> 分支 cards/C12.4-charter；基线 = 该分支 HEAD（含 K1/K2/K3 全部产出）。

## plan 节点亲跑记录

- 2026-08-26 基线核对：`git branch --show-current` → `cards/C12.4-charter`；`git log --oneline -1` → `0421fba test(C12.3): K3 补测轮…`；`git status --short` 干净。
- 2026-08-26 基线绿亲跑：`cd graph/webui && npm ci`（0 vulnerabilities）→ `npm run typecheck` 退出码 **0**；`npm test` → **Test Files 23 passed (23)，Tests 182 passed (182)**。（K3 plan 收口读数是 23/179；其后 0421fba「K3 补测轮」加了 3 支，182 与之相容——以本轮亲跑为准。）
- 2026-08-26 focused 过滤形态亲证：`npm test -- src/app/codegraph/scopepage.test.ts` → **21 passed (21)**，退出码 0。
- 现状符号核对（read 实跑）：scopepage.ts 全文 604 行读毕——`ScopePageModel` 七键（scopeId/organization/organizationAvailable/nodes/edges/inboundSeams/empty）、`ScopeNode` 十六字段、`InboundSeam` 十字段、`ScopeEdge` kind='call'|'projection' + projectionType、`ResponsibilityState` 三态、`ScopeEntryRef.channel?`；**模型不含 invariants/stateMachine 投影**（缺口登记见 plan §模型缺口）。besttree.ts：`assembleDirections(target?, report?): DirectionReadout[]`（:344，key=`${from}->${to}`，:324）、`DirectionStatus` 四值（:29）、`migrationGroups(best, baseline, report)`（:448，吃 report.warns 的 container-misplaced）。
- a11y 缺陷现场确认：BestDomainPage.tsx :172-177 —— `role="tablist"` 内混排 tab 按钮与 `data-organization="best"/"current"` 组织切换按钮（§2.3-21 要机械消解的对象）。
- 组件测试 harness 亲证：src/test/setup.ts 接 jest-dom/vitest matchers + afterEach cleanup；vite.config.ts test.environment=jsdom；package.json devDeps 有 @testing-library/react@16 / jest-dom@7 / jsdom@30。fireEvent.mouseDown/move/up 平移手势已有测试先例（DomainPanorama.test.tsx:75-87）；wheel 缩放无先例，沙箱探针验证（见下）。
- api/client.ts 导出面亲证：`fetchCodegraph(project)`（:57）、`fetchCodegraphSource(project, file, line, span=40)`（:62）——零请求断言的 mock 对象就这两个符号。

## 沙箱探针（C12.1/C12.3 先例：副本内开发验证，工作树零改动）

- 方法：`cp -a graph/webui → /root/.handoff/tmp/7d3c66ba/k4probe/webui`，在副本内落地本计划全部 10 个文件并跑红/绿/变异；工作树全程只有本台账 untracked（`git status --short` 核验留痕于收尾提交）。
- **红灯相与探针抓掉的真缺陷**（全部已修正进本计划的代码块）：
  - scopelayout.test 首版 2 红：①夹具误用容器卡导致包框计数断言错（测试书写错）；②孤立行实现按输入序摆放而注释承诺 id 升序——**实现缺陷**，改为 `[...isolated].sort(byId)` 后绿。
  - RightPanel.test 首版 13 红：①测试用 getByTestId 而组件只暴露 data-* 契约属性——查询纪律统一为按契约属性 q(selector)；②同一测试内二次 render 未卸载前次实例，document 级查询命中旧 DOM——补 unmount；③夹取方向算反（手柄在左缘：左拖加宽右拖收窄）与持久化期望值算错（420+(400-300)=520）——测试数学修正。
  - ScopeCanvas.test 首版 3 红：①同上残留问题；②平移期望 y 算错（10→-15 位移是 -25）；③**组件缺陷**——容器原子说明只随 selected prop 变化收起，单元语境里点选卡片后说明不消——改为 onClick 即清 atomicNote。
  - TwoAxisPage.test 首版 2 红：①程序入口区属「选中卡的右栏」，须先单击入口容器卡再点入口按钮（交互序列修正）；②一处断言笔误删除。
- **绿灯相（终版代码，即本计划嵌入的逐字内容）**：scopelayout 6 绿；RightPanel 17 绿；ScopeCanvas 14 绿；MigrationDrawer 4 绿；TwoAxisPage 13 绿——合计新增 54 支全绿。
- `npx tsc -b --force` 退出码 0（vitest 不查类型，夹具类型错误只有 tsc 能抓——本轮零 TS 报错）。
- 全量：`npm test` → **Test Files 28 passed (28)，Tests 236 passed (236)**＝基线 23 文件/182 支 + 新增 5 文件/54 支，对账分毫不差。
- **变异钻取（验后全部恢复，恢复后重跑 236 全绿 + tsc 0）**：
  - 变异 A：ScopeCanvas 压暗标记改无条件 → ScopeCanvas.test **1 failed | 13 passed**（14 支中）（『三者同屏』支红）；
  - 变异 B：删容器原子分支（双击容器放行下钻）→ 两文件合计 **2 failed | 25 passed**（27 支中）（TwoAxisPage『scope 不变』支 + ScopeCanvas『原子说明』支红）；
  - 变异 C：RightPanel channel 分组塌缩为单桶 → RightPanel.test **1 failed | 16 passed**（17 支中）（【释2】单桶反面支红）。
- wheel 缩放在 jsdom 的可行性由探针证实：`fireEvent.wheel(el, {ctrlKey:true})` 能驱动 native wheel listener 并改变状态（ScopeCanvas『Ctrl+滚轮』支绿）。


## plan 节点收尾补记

- 计划文档落盘：docs/superpowers/plans/c12.4-plan.md（2757 行；十个代码块经 python 逐字校验与沙箱探针版本 byte-identical）。
- 环境事实：本环境无 rg——计划中所有检索/扫描命令已改为亲跑过的 grep/python 形态（防漂 className 扫描器在沙箱按「非 = 前缀的 >」找标签边界验证输出 ALL_BUTTONS_HAVE_CLASSNAME；朴素单行正则会把 JSX 箭头 => 当标签结束，教训随命令写入 T6 步骤 2）。
- 收尾前复跑：RightPanel focused 17 绿 + tsc -b 0（注释引用从 §模型缺口 改为 §1.5 后同步沙箱与计划并复验逐字一致）。
- 工作树核验：git status 仅 docs/ledgers/2026-08-26-c12.4-plan-ledger.md 与 docs/superpowers/plans/c12.4-plan.md 两个新增文件；src 零改动。

## 实现轮执行记录（C12.4-charter-2，R3 修订生效轮）

- 2026-08-26 分支核对：`git branch --show-current` → `cards/C12.4-charter-2`；`git log --oneline -1` → `314578b plan(C12.4)…`；`git status --short` 干净。
- 2026-08-26 实现轮基线亲跑：`cd graph/webui && npm ci`（found 0 vulnerabilities）→ `npm run typecheck` 退出码 **0**；`npm test` → **Test Files 23 passed (23)，Tests 182 passed (182)**。与 plan 节点读数一致。
- R3 枚举差分（实现轮自查，不照抄 plan §0）：plan 原判「纯增量卡、零修改既有文件」；协调者修订 R3 把 **scopepage.ts + scopepage.test.ts 的修改**授权进本卡有界文件集（唯一目的＝decls[domainId].invariants 投影进 ScopePageModel）。故本卡改动集 = plan 十个新建文件 ∪ {scopepage.ts, scopepage.test.ts} ∪ 台账；其余不触碰清单照 plan 原文执行（flowpage.ts 只 import FALLBACK_BUCKET_KINDS，本轮 grep 亲证不受影响）。
- R3 数据事实（协调者提供，handoff 仓 codegraph/domains/*.json）：2 个 decl 文件、invariants 合计 7 条、每条含 text+testRef、stateMachine 全项目 0 条。类型格位 graph/webui/src/api/types.ts:15 `CgInvariant{text;testRef?}` 与 :35-37 `CgDomainDecl.invariants?: CgInvariant[]` 本仓 read 亲证。
- R3 红灯相亲跑：先落 scopepage.test.ts 新 describe『C12.4 R3：invariants 投影』5 支 → `npm test -- src/app/codegraph/scopepage.test.ts` → **Tests 5 failed | 21 passed (26)**，失败形态全部为 AssertionError（如 `expected undefined to be null`）＝投影字段功能缺失的断言红，非 typo；既有 21 支全绿未受扰。
- T1 绿灯亲跑：scopelayout focused → **Tests 6 passed (6)**；`npm run typecheck` 退出码 0。夹具相对 plan 逐字版的差分：ScopeNode 字面量按 R3 后形状补 `invariants`（domainNode={state:'no-decl'}、containerNode=null）。提交 f3e674e。
- T2 红灯相亲跑：RightPanel.test.tsx 先落 → `Failed to resolve import "./RightPanel" from "src/app/codegraph/RightPanel.test.tsx". Does the file exist?`，非零退出。测试文件相对 plan 逐字版差分（R3）：①三张夹具卡补 invariants（容器 null；d_leaf=present 两条件，其一无 testRef）；②plan 的「未接线指针」支删除，替换为『C12.4 不变式区（R3 接线）』4 支——present 双条目+testRef 锚渲染+data-unwired 必须缺席、unwritten/no-decl 双空态属性值互斥（互相 querySelector null 反面断言）、容器卡整区块不在场；③状态机指针支保留（R3-A）。
- T2 绿灯亲跑：RightPanel focused → **Tests 20 passed (20)**；typecheck 退出码 0。计数对账：plan 沙箱版 17 支 − 删「未接线指针」1 支 + 新增 R3 不变式区 4 支 = 20，吻合。实现相对 plan 逐字版差分（仅 R3 授权处）：文件头边界注释改为「不变式按 R3 真渲染、状态机维持指针」；新增 InvariantsBlock 子组件（present/unwritten/no-decl 三态互斥投影，testRef 经 data-invariant-test 渲染）；info 体 invariants 区块去掉 data-unwired="true"，容器卡整区块条件不在场（沿 debt 区块同款）；状态机 tab 原样保留。提交见 git log。
- T3 红灯亲跑：MigrationDrawer.test.tsx 先落 → `Failed to resolve import "./MigrationDrawer"`，非零退出。与 plan 逐字一致（无 R3 触点）。
- T3 绿灯亲跑：MigrationDrawer focused → **Tests 4 passed (4)**；typecheck 退出码 0。
- T4 红灯亲跑：ScopeCanvas.test.tsx 先落 → `Failed to resolve import "./ScopeCanvas"`，非零退出。夹具差分（R3）：node() 默认补 invariants={state:'no-decl'}、c_big 容器 override invariants=null，其余与 plan 逐字一致。
- T4 绿灯亲跑：ScopeCanvas focused → **Tests 14 passed (14)**；typecheck 退出码 0。
- T5 红灯亲跑：TwoAxisPage.test.tsx 先落 → `Failed to resolve import "./TwoAxisPage"`，非零退出。
- T5 绿灯亲跑：TwoAxisPage focused → **Tests 14 passed (14)**（plan 沙箱版 13 + 新增 R3 穿线支『R3 不变式接线穿线』1）；typecheck 退出码 0。夹具账目差分：k4World 的 decls.topA 增补 invariants=[{text:'甲系统的承重不变式',testRef:'TestTopAInvariant'}]——不影响既有手工可核读数（responsibility 未动，债读数/oversized/folded 口径全部不变），只服务 R3 穿线断言。
- T6 变异钻取（每条先断言目标串 count==1 唯一命中；验后恢复并以 `git diff --stat` 空输出确认；恢复后 focused 复绿）：
  - 变异 A｜ScopeCanvas `{...(!related ? {'data-dimmed':'true'} : {})}` 改无条件 dimmed。第一发直接替换对象字面量为 JSX 属性位语法错（vitest "no tests"）——按纪律编译不过不算数，改等价变异 `!related`→`true` 重做：ScopeCanvas.test **1 failed | 13 passed**，红的是『三者同屏』支，与沙箱预期一致。
  - 变异 B｜删 ScopeCanvas onCardDoubleClick 的 container 分支：ScopeCanvas **1 failed | 13 passed**（『原子说明』支）+ TwoAxisPage **1 failed | 13 passed**（『scope 不变』支），合计 2 红，与沙箱预期一致。恢复后 ScopeCanvas focused 14 绿。
  - 变异 C｜RightPanel channel 成员过滤塌缩（`entry.channel === bucket` → `=== undefined || === bucket`）：RightPanel.test **2 failed | 18 passed**，含【释2】单桶反面支『channel 全缺时只剩通道未标注单桶』。
  - 变异 D｜R3 专属：scopepage domainInvariants 把 no-decl 塌缩进 unwritten（`if (!decl) return { state:'no-decl' }` → `'unwritten'`）。先 `npm run build` 退出码 0（编译过闸）再数红：scopepage.test **3 failed | 23 passed**，全落在 R3 三态互斥 describe——缝级三态断言有牙实证。
- T6 防漂 className 预检：python 扫描器输出 **ALL_BUTTONS_HAVE_CLASSNAME**（四组件全部 button 带 className）。
- T6 全量回归亲跑：`npm run typecheck` 退出码 **0**；`npm test` → **Test Files 28 passed (28)，Tests 245 passed (245)**；`git diff --check` 干净。对账：基线 23 文件/182 支 + scopepage R3 新增 5 + scopelayout 6 + RightPanel 20 + MigrationDrawer 4 + ScopeCanvas 14 + TwoAxisPage 14 = 28 文件/245 支，分毫不差（plan 沙箱读数 236 = 本轮 245 − R3 增量 5 − RightPanel 净增 3 − TwoAxisPage 穿线 1 + 沙箱未计的 RightPanel 替换差 1…以逐文件相加为准：182+58=240+5=245 ✓ 即 182+5+58=245）。
- T6 越界扫描：`git diff --name-only 314578b..HEAD` = plan 十新建文件 ∪ {scopepage.ts, scopepage.test.ts}（R3 授权）共 12 文件；无 Go/api/types/client/配置/旧组件路径。`grep -rn "from './TwoAxisPage'" src | grep -v TwoAxisPage.test` → **NO_IMPORTERS_OUTSIDE_TEST**（装配接线归 K6 的证据；plan 原命令会命中组件自身测试文件，已按其意图排除后复核）。
- 收尾自审：错误分支日志（localStorage 写失败 warn 带键名）、成功路径出口日志（tab select/scope enter/card select/entry open/migration toggle 全带 [codegraph] info 上下文）；新文件头注释职责+边界齐备；触及包测试绿 + 全量编译 0；导出签名与 plan §1 Produces 块一致（RightPanel/MigrationDrawer/ScopeCanvas/TwoAxisPage/scopelayout 五处逐字核对）；R3 差异均已在本台账留痕。

## 补正轮执行记录（C12.4-charter-3，只补 review 三条 minor）

范围声明：本轮只动 c12-contract.md、RightPanel.tsx、RightPanel.test.tsx ∪ 本台账；其余零改动（未碰五个组件其他部分、未碰 scopepage.ts）。

- 2026-08-26 分支核对：`git branch --show-current` → `cards/C12.4-charter-3`；`git log --oneline -1` → `304fe1c docs(C12.4): K4 变异复验与全量回归证据落台账`；工作树干净。
- 依赖安装：`npm ci` → found 0 vulnerabilities。基线 focused：`npm test -- src/app/codegraph/RightPanel.test.tsx` → **Test Files 1 passed (1)，Tests 20 passed (20)**。
- 第 2/3 条红灯相亲跑（先落三支新测试再动实现）：`npm test -- src/app/codegraph/RightPanel.test.tsx` → **Tests 3 failed | 20 passed (23)**，失败形态全部为断言红、非 typo：
  - `AssertionError: expected '通道未标注 · 1bare cmd' to contain '通道未标注 · 2'`＝grpc 条目静默消失（缺陷本体复现）；
  - `AssertionError: expected 5 to be 7 // Object.is equality`＝穷尽性锁抓到两条词表外条目失踪；
  - `AssertionError: expected "warn" to be called 1 times, but got 0 times`＝读方向无 warn。
- 实现差分（RightPanel.tsx 两处，最小修法）：①新增 `channelBucketOf(channel)` 穷尽式映射函数——词表值各归其桶、缺席与词表外统一落 UNLABELED_BUCKET，分桶过滤改用它；②readStoredWidth catch 分支补 `console.warn('[codegraph] right width restore failed', { key: WIDTH_KEY, cause })` 与写方向同款带键名留痕。选「并入既有通道未标注桶」而非另设中性桶的理由：breakdown K4 缺陷族表本就承诺「未知值走中性缺省」且该桶即被指定为降级桶，补正轮不新增界面词汇。
- 测试差分（RightPanel.test.tsx +3 支）：①词表外值级断言（grpc 落 unlabeled 桶、锁死标签与计数「通道未标注 · 2」、全界面恰 5 桶无幽灵桶）；②穷尽性总和锁（四词表值+缺席+grpc+smtp 七条夹具，DOM 桶成员数之和===7 且 id 集合恰一次）；③读方向 getItem 抛错独立一支（默认宽 360 挂载 + warn 恰一次 + meta.key 锁死）——与写方向支分开不合并。OOV 夹具经 oovChannelEntry 双重断言构造（CgEntryChannel 静态四值，词表外值只能经未类型化 wire JSON 到达）。
- 绿灯亲跑：focused → **Tests 23 passed (23)**；`npm run typecheck`（tsc -b）退出码 **0**；全量 `npm test` → **Test Files 28 passed (28)，Tests 248 passed (248)**。对账：上轮收口 245 + 本轮新增 3 = 248 ✓。
- 变异钻取×2（备份至 /root/.handoff/tmp/b02cfabd/mutation-backup 后才动手；每条先 python 断言目标串 count==1 唯一命中；先过编译闸再数红；验后复原）：
  - 变异 M｜channelBucketOf 尾行改 `return typeof channel === 'string' ? channel : UNLABELED_BUCKET` 并放宽签名为 string（模拟词表外值再次消失）：typecheck 退出码 0（编译过闸）→ RightPanel **Tests 2 failed | 21 passed (23)**，红的恰是新两支（值级断言 element not found e_grpc + 穷尽性 5≠7），既有 21 支不受扰——守卫唯一命中实证。
  - 变异 N｜删 readStoredWidth 的 console.warn 行（替换为 void cause 保编译）：typecheck 退出码 0 → RightPanel **Tests 1 failed | 22 passed (23)**，红的恰是读方向支——与写方向支互不掩盖实证。
  - 复原确认：`diff` 与备份逐字节一致（RESTORED_IDENTICAL），focused 复绿 23 passed；最终提交无变异残留。
- 契约记账（第 1 条，代笔）：c12-contract.md §8 按 R1/R2 同体例补记协调者修订 R3（触发点 C12.4 plan §1.5；裁定 invariants 投影 + stateMachine 维持指针；理由/边界/形态约束按卡上裁决原文转录）。其中「handoff 仓 codegraph/domains/ 2 decl 文件 7 条不变式」属 handoff 仓数据事实，本轮在本仓不可复核，按协调者口径转录（BestDomainPage.tsx 不变式渲染区与 §2.3-19 六输入字段含 decls 已在本仓 read 亲证）。
