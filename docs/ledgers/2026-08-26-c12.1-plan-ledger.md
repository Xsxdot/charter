# C12.1 plan 节点台账（K1 responsibility 同刀迁移）

分支 `cards/C12.1-charter`（自 `cards/C12-charter-2` HEAD 45cd36b 链式钉基线）。产出物：`docs/superpowers/plans/c12.1-plan.md`。

## 查证事件（本节点亲跑，按时间序）

- 上游状态位：contract 已冻结、breakdown 已拍板（P1～P5 全裁），文件头直接引用。
- 基线绿亲跑（改动前）：`cd graph && go build ./...` → GO_BUILD_OK；`go test ./codegraph/ ./cli/ -count=1` → 两包 `ok`（0.028s / 0.295s）。webui 侧 `npm ci`（node_modules 缺失补装）→ `npm run typecheck` 退出 0 → `npm test` → **Test Files 21 passed (21)，Tests 136 passed (136)**。与 contract §4.2 读数一致。
- 序列化钉值探针（一次性模块 `/root/.handoff/tmp/01e0151b/opencode/bestprobe`，replace 指向本工作树 graph/）：`json.Marshal(codegraph.BestDomain{})` → `{"label":"","responsibility":""}`——无 omitempty 实锤；`ValidateBest` 对空职责领域产出 `领域 d_x 的 responsibility 不能为空` 实锤。
- `ValidateBest` 调用面 grep：仅 `migrate.go:157` 与 `cli.go:139/:260`；`check.go` 不调用——测试夹具删字段不会经 Check 链误伤。
- `AssembleContext` 调用面 grep：唯一外部调用方 `graph/cli/cli.go:601`，签名不动则 CLI 零改动。
- `contextVocabulary(v,best,domainID)` 现签名在 `context.go:336`；decls 在 `AssembleContext:127` 才加载——穿线必须把加载上移。
- TS 读数面全量 grep（`responsibility`，含测试）：生产侧 types.ts:62、besttree.ts:11/:20/:83/:177/:201/:576、BestScopePanorama.tsx:207、**BestPanorama.tsx:219**、BestDetail.tsx:125、BestDomainPage.tsx:40（decl 侧，保留）；测试侧 besttree.test.ts×10 处、domainpage.test.ts×8 处、BestDetail.test.tsx×7 处、BestDomainPage.test.tsx×3 处、BestLeafGraph.test.tsx×3 处、BestPanorama.test.tsx×3 处、BestScopePanorama.test.tsx×4 处、CodegraphPage.test.tsx×5 处。

## 同刀清单对 breakdown 的六处机械圈定补遗（均有编译/grep 实证）

1. `graph/codegraph/migrate_test.go`——:131 读 `.Responsibility`（编译强制）、:101 断言 Notes==3、:104 断言含 "Responsibility" 提示行（行为强制）；breakdown 有界文件集漏列。
2. `graph/webui/src/app/codegraph/domainpage.test.ts`——:84 起 `const best: CgBest = {...}` 带类型注解，excess property check 使删镜像字段后 typecheck 必红；breakdown 漏列。
3. `graph/webui/src/app/codegraph/BestPanorama.tsx`——:219 渲染 `card.responsibility`；contract §1 表只列三组件、breakdown 文件集只给了它的 test——生产文件本身必须同刀。
4. `graph/webui/src/app/codegraph/CodegraphPage.tsx`——:252/:261/:284 三处挂载点穿线 `decls={data.decls}`（breakdown 注明「props 穿线」但清单漏了本文件）。
5. `graph/webui/src/api/client.test.ts`——breakdown K1 缺陷族表自己点名要的 wire 回放穿线断言落在这里；清单未列。
6. `graph/codegraph/testdata/repo/codegraph/best.json`——三个 domain 带 responsibility 键；夹具代表「当前 wire 形状」，随刀剥键（Go 侧未知键容忍另有专测）。

## 判断与放弃的尝试

- **单提交约束**：contract §2.2-8/-10 两次明文「同一提交」→ T1～T4 全部工作树改动合并为一个提交，提交动作归 T4；逐 task 提交惯例被契约压倒，计划内显式声明。
- `migrate.go#migrateV2Domain.Responsibility`（:48）**保留不动**：它是冻结历史 v2 target.json 的严格解码器（`decodeStrict` 开 DisallowUnknownFields），真实旧文件的 domains[] 可含该键，删除会拒迁存量仓；breakdown【释1】对本文件的刀口只点了写入占位符与提示行，与此一致。
- `graph/cli/cli_test.go:324` 的裸 JSON best 夹具（含 `"responsibility":"服务"`）**不改**：字符串字面量不受编译强制；它恰好构成 CLI 层旧形状文件容忍的活证据，库层容忍另加专测（TestLoadBestToleratesLegacyResponsibilityKey）。
- decl 侧不随刀：`decl.go:23`、`decls.go:45/:55`、`entity.go:72`、`anchor_test.go`、`decl_test.go`、`client.test.ts:80/:93`（CgDomainDecl）全是新所有者一侧，保持原样。
- contextVocabulary 改签名的副作用：LoadDomainDecls 上移后，「声明加载失败」会先于「未知领域」报错（testdata/repo 声明恒合法，既有测试不受影响）；计划内作为已接受行为变化记录。
- TS 模型字段定 `responsibility?: string`（可选）：undefined=无声明文件、string=decls 非空正文，区分「缺失」与「空串」（纪律序列化边界设问）；tsconfig 仅 strict 无 exactOptionalPropertyTypes，直赋 undefined 合法（已核 tsconfig.app.json:15）。
- 变异执法设计：反面断言「职责位不得出现 best 来源文本」在 DOM 层不可直接判（label 本来就合法渲染为标题），故以 T4 两条变异钻取落实——helper 回填 label 必红、金样本回填键必红。

## 产出物落盘

- `docs/superpowers/plans/c12.1-plan.md`（T1 红灯铺设 → T2/T3 双侧最小实现 → T4 全量回归+变异×2+单提交；四缝缝级断言双向核对、内部锁两处声明、占位符扫描零命中）。
- 出稿后自检：五项检查逐项过（缺陷族八族、序列化边界五条链路、上下文预算 24 文件圈定、边界型真机新增为零、接缝双向）；派发前自审——本计划无任何驱动派发系统自身的验收步骤。
- 本回合未调用 handoff CLI、未起新 executor 进程；全部结论来自本地读文件与亲跑命令。
