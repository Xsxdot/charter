# B296 spec 台账

- 2026-08-29 发现自 B266 implement 收口。卡面 note：台账要写本提交 hash，amend 换 hash，文件里永远对不上最终 HEAD。B266 已忽略并继续；本卡另定记法。
- 2026-08-29 B266 时间线（账本事件）：review-2 以「台账 hash 4aaef820 ≠ HEAD 47609c04」major fail；协调者接受并打回；随后三轮 implement 追 hash，撞 3/3；重置后再派仍对不上（25086668 vs HEAD 1a40bd82）；用户拍板忽略、记本卡；review-3 按指示不 fail。
- 2026-08-29 现状：`skills/{implement,plan,contract,breakdown,spec}/SKILL.md` 五份正文各有一段「边干边落台账」——要求记「提交、跑过的命令与原始输出」，且「与本节点产出物同批提交」。`skills/review/SKILL.md` 红线无「hash 必须等于 HEAD」；审查者发明了这条，协调者批准了它。
- 2026-08-29 handoff 侧：`git add <范围内路径> && git commit --amend --no-edit` 是 B249 静态白名单里「charter 台账纪律的法定动作」。同批收口靠这一次 amend。amend 必然换 hash。一个 git 提交的树不能包含自己的最终 hash。
- 2026-08-29 先例：C11/C17 台账已在躲（「最终 hash 以 amend 后为准 / 以收尾行报告」）；B223 台账写过「将提交哈希写入同一提交后再次 amend 会改变提交哈希」。
- 2026-08-29 开卡：B296 待办→spec。项目字段仍是 handoff（发现自 B266）；产物与改动在 charter 仓。
- 2026-08-29 定级倾向 L1：改的是纪律正文记法 + 一条审查禁令，plan 写出来只会复述本 spec；验收=读再生块+确认禁止句在场，一眼可核。不改 handoff 契约面。
- 2026-08-29 用户「批准」。spec 状态行回写已批准；同一份文件挂 spec: 与 plan:；卡 move implement。本机落地。
- 2026-08-29 基线红：`regen(tmp)` 七块均不含「提交事实的记法」/「台账记录的 hash」。RED_OK。
- 2026-08-29 已改 `skills/{implement,plan,contract,breakdown,spec,review}/SKILL.md`。compose_map 未改。
- 2026-08-29 再生绿：tmp regen 四产出块含「提交事实的记法」，`charter-review.md` 含禁令句；integrate/recon 不含记法（它们没有台账段）。GREEN_OK。
- 2026-08-29 `python3 scripts/charter_provision.py install`：template/workflow 跳过；纪律块 breakdown/contract/implement/plan/review 因正文不一致已入账；recon 跳过。integrate 也被 put——该块本卡未改 skill，是账本相对当前仓的既有漂移，install 顺手对齐。随后 `check`：workflow/template 一致，纪律块 7/7 一致。
- 2026-08-29 `python3 scripts/test_charter_provision.py`：26 tests OK（0.085s）。
- 2026-08-29 `handoff discipline get`：`charter-implement v2` 含记法句；`charter-review v3` 含禁令句。记法句在五份 skill 各恰好 1 处，禁令句在 review 恰好 1 处。
- 2026-08-29 C9 G：roadmap 第 64 条写入生效检查观察项。`~/.grok/skills` 本地副本不是本卡安装目标（执行者读账本块）；未改。
- 2026-08-29 提交：`git commit -m "fix(B296): stop chasing ledger self-hash after amend"` 原始输出：`[cards/B296-charter 4d8a4eee] fix(B296): stop chasing ledger self-hash after amend`、`9 files changed, 89 insertions(+), 5 deletions(-)`。随后只 amend 一次把本条收进同批；不把 amend 后 HEAD 写回。
