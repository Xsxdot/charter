# C1.10 contract ledger

- 2026-08-25：`git status --short --branch` 输出 `## cards/C1.10-charter`；`git log -1 --oneline --decorate` 输出 `b8edeed (HEAD -> cards/C1.10-charter, origin/master, origin/HEAD) docs(spec): 三份 spec 批准回写 + C1.10 第四条按裁决 A 定稿`。
- 2026-08-25：spec `docs/specs/2026-08-24-codegraph-viewer-semantics-spec.md` 头部状态为 `已批准`（2026-08-24；用户裁决第四条取 A），无需回写状态。
- 2026-08-25：本地 `git show 9879e16` 输出 `fatal: ambiguous argument '9879e16': unknown revision or path not in the working tree.`；本轮按用户指示只以当前工作树 HEAD `b8edeed` 为现状事实源。
- 2026-08-25：本仓现有台账目录列出 compare/migration 等历史文件，没有 `C1.10` 专用台账；本文件为本轮新建台账。
- 2026-08-25：`skills/contract/SKILL.md` 已完整读取；其要求包括现状签名查证、存量无图项目以契约增量文档冻结、Ticket 0 编译证据、符号锚解析和冻结即提交。
- 2026-08-25：当前 charter HEAD 的 `graph/codegraph/types.go#TestRef` 仍有 `Snippet string json:"snippet,omitempty"`；与计划注记「C1.5 已删 Snippet」不一致，按当前工作树事实记录，不把删除当作本轮契约事实。
- 2026-08-25：当前 charter HEAD 的 `graph/codegraph/decl.go#DomainDecl` 仍将 `Domain` 注释为 baseline 领域 id；`graph/codegraph/decls.go#ValidateDecls` 第 89~93 行按 `v.Domains[id]` 校验，尚未按 `best.Domains` 校验。
- 2026-08-25：当前 charter HEAD 的 `graph/webui/src/api/types.ts#CgGraph` 已有 `lifecycle`/`packages` additive 段，`#CodegraphResp` 已有可选 `best`/`target`/`report`，但没有 `decls`。
- 2026-08-25：当前 charter HEAD 的 `graph/webui/src/app/codegraph/graphmath.ts#CgView` 只合并 `domains/containers/nodes/edges`；`#mergeView` 未携带声明、best 或生命周期/包段，`#chainTree` 默认 `maxDepth=10`。
- 2026-08-25：当前 charter HEAD 的 `graph/webui/src/app/codegraph/domains.ts#leafRoots` 以 entry 与跨域被调节点作叶子根；`DomainDetail` 当前展示应然摘要/入口/跨域接口，但没有语义/结构双 tab或声明模型。
- 2026-08-25：当前 charter HEAD 的 `graph/webui/src/app/codegraph/CodegraphPage.tsx#CodegraphPage` 已有现状领域下钻与 best 组织两套状态，但尚无本卡组织切换、语义/结构 tab或级联抽屉状态。
- 2026-08-25：外部 handoff 仓当前 `main` HEAD 为 `7adeb8f9`，`internal/agentd/codegraph.go#handleProjectCodegraph` 第 77 行真实响应键仅 `baseline`/`views`/`stale`；`codegraph_test.go#TestCodegraphEndpoint` 第 61~65 行也只解码三键。该 provider 不在本工作树，本轮不能把 best/target/report/decls 现状写成 charter 代码事实。
- 2026-08-25：外部 handoff `internal/agentd/server.go` 第 503~504 行已注册 `GET /api/projects/{name}/codegraph` 与 `/source`；本轮未修改外部仓。
- 2026-08-25：`nl -ba docs/specs/2026-08-24-codegraph-viewer-semantics-spec.md | sed -n '124,140p'` 输出第 140 行仅规定「深度 3、每级取节点上限、高扇入收桩阈值」写死，未给每级上限与收桩阈值数值；该缺口会改变结构模型与测试，待协调者/用户裁决后继续。
- 2026-08-25：协调者裁决冻结四值：K≥3 按调用方领域去重数收桩；focus 配额 5，超出报告 `focus-quota`；级联深度 3；每级节点上限 8，超出报告本级 `droppedNodes`，同级按跨域入边序取前 8；四值均写纯函数模块、不进配置，并分别用钉值测试锁定 3/5/3/8。
- 2026-08-25：协调者说明 C1.5 同源常量及 `sharedCallerDomains(v)` 位于未合并分支 `cards/C1.5-review-1`（注记提交 `9879e16a`）；本地 `git -C /root/.handoff/repos/charter show 9879e16` 原始输出 `fatal: ambiguous argument '9879e16': unknown revision or path not in the working tree.` 与 `fatal: invalid object name '9879e16'.`，因此该出处未在当前仓源码中亲自验证，契约将标注「上游分支出处，当前工作树未验证」。
- 2026-08-25：协调者裁决当前 `graph/codegraph/types.go#TestRef` 的 `Snippet` 只代表 master 基线读数；C1.5 分支已删，契约涉及 TestRef 时标注该差异，不把当前字段写成终态。
- 2026-08-25：执行 `mkdir -p docs/superpowers/specs`，退出码 0；法定契约路径目录已建立。
- 2026-08-25：已落盘 `docs/superpowers/specs/c1.10-contract.md`，冻结 wire、best 词表迁移、纯函数签名、K≥3/配额 5/深度 3/上限 8、测试与欠账；同时 Ticket 0 在 `graph/webui/src/api/types.ts` 增加 `CgDomainDecl*`、`CodegraphResp.decls?` 与 `CgNode.modelKind?`，新增 `domainpage.ts` 类型/常量/空壳及四支钉值测试。
- 2026-08-25：在 `graph/webui` 执行 `npm run typecheck` 失败，原始输出为 `sh: 1: tsc: not found`，退出码 127；当前尚未有 TypeScript 编译通过证据。
- 2026-08-25：在 `graph/webui` 执行 `npm ci` 成功，原始输出为 `added 168 packages, and audited 169 packages in 1s`、`found 0 vulnerabilities`，退出码 0；依赖已按 package-lock 安装到未跟踪的 node_modules。
- 2026-08-25：在 `graph/webui` 执行 `npm run typecheck` 成功，输出脚本 `tsc -b`，退出码 0。
- 2026-08-25：首次测试命令 `npm test -- --runInBand` 失败，原始输出为 `CACError: Unknown option \`--runInBand\``，Node.js v24.16.0，退出码 1；该选项不是 Vitest 支持项，未据此判断测试失败。
- 2026-08-25：在 `graph/webui` 重跑 `npm test` 成功：`Test Files 18 passed (18)`、`Tests 112 passed (112)`、`Duration 6.34s`，退出码 0；包含本卡四支常量钉值测试。
- 2026-08-25：在 `graph` 执行 `go test ./...` 成功：`ok github.com/Xsxdot/charter/graph/cli`、`ok github.com/Xsxdot/charter/graph/codegraph`、`ok github.com/Xsxdot/charter/graph/webui`，退出码 0；Ticket 0 未改变 Go 行为，现有图库编译/测试保持通过。
- 2026-08-25：执行 `go run ./cmd/codegraph --repo /root/.handoff/worktrees/c454e0fa resolve --doc docs/superpowers/specs/c1.10-contract.md` 失败，原始输出为 `Error: 读取基线 /root/.handoff/worktrees/c454e0fa/codegraph/baseline.json: open /root/.handoff/worktrees/c454e0fa/codegraph/baseline.json: no such file or directory`，退出码 1；这是存量无图项目的预期工具阻断，未把锚点解析写成通过。
- 2026-08-25：目标图检查命令输出 `root_target_exit=1`、`root_best_exit=1`、`fixture_target_exit=0`、`fixture_best_exit=0`；根目录无项目图，测试夹具有 best/target，契约按存量无图冻结。
- 2026-08-25：执行 `git diff --check` 成功，退出码 0，无 whitespace 错误。
- 2026-08-25：执行 `git status --short --branch` 输出 `## cards/C1.10-charter`，当前变更仅为契约文档、C1.10 台账、TS wire 类型和 Ticket 0 纯函数骨架/钉值测试。
- 2026-08-25：出稿复核将契约第 79 条改为「viewer 侧平行副本」，并补写上游 C1.5 `DefaultContextDepth`/`DefaultContextFocusQuota`/`DefaultUtilSharedByDomains` 名称；不把前三项误报为本卡新常量。
- 2026-08-25：最终再次执行 `git diff --check` 成功，退出码 0。
- 2026-08-25：提交前执行 `git diff --check` 成功，退出码 0；随后仅追加本条台账事实，无代码/契约内容变更。
- 2026-08-25：首次执行 `git add docs/superpowers/specs/c1.10-contract.md docs/ledgers/2026-08-25-c1.10-contract-ledger.md graph/webui/src/api/types.ts graph/webui/src/app/codegraph/domainpage.ts graph/webui/src/app/codegraph/domainpage.test.ts` 失败，原始输出为 `fatal: Unable to create '/root/.handoff/repos/charter/.git/worktrees/c454e0fa/index.lock': Read-only file system`，退出码 128；按权限规则改用升级权限重试。
- 2026-08-25：同一 `git add` 命令以升级权限重试成功，退出码 0；契约、台账、TS wire 类型、纯函数骨架和钉值测试已进入 index。
- 2026-08-25：暂存检查 `git diff --cached --check` 成功，退出码 0；`git diff --cached --stat` 输出 5 个文件、404 行新增。因上条成功事实追加后台账变为 `AM`，需再次暂存台账再提交。
- 2026-08-25：再次执行 `git add docs/ledgers/2026-08-25-c1.10-contract-ledger.md` 以升级权限成功，退出码 0；台账与其他四个产出文件现均已暂存。
- 2026-08-25：执行 `git commit -m "docs(contract): C1.10 viewer semantics contract freeze"` 成功，原始输出为 `[cards/C1.10-charter 0c46e26] docs(contract): C1.10 viewer semantics contract freeze`、`5 files changed, 407 insertions(+)`，退出码 0；未 push。追加本条后需再提交台账变更以保持工作树干净。
