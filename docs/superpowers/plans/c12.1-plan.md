# C12.1 实现计划：K1 responsibility 同刀迁移（best 删字段、职责正文归 decl 唯一所有）

> 状态：实现轮执行计划（plan 节点产出物）
> 输入：`docs/specs/2026-08-25-codegraph-viewer-two-axis-spec.md`（已批准）、`docs/superpowers/specs/c12-contract.md`（已冻结，§2.2-8/-9/-10/-11/-13、§3-3、§4.3 同刀回归）、`docs/superpowers/specs/c12-breakdown.md`（已拍板，P2-A 时序、P4-A Summary 语义、【释1】同刀补遗）
> 基线：分支 `cards/C12-charter-2` HEAD 45cd36b；本卡工作分支 `cards/C12.1-charter`（链式钉死，拍板约束 1）。
> 仓界：只改 charter 仓 `graph/` 与测试夹具；handoff 侧三项交棒欠账（正文搬运、扫描配方、宿主核验）不设卡不代做。
> **提交形态**：contract §2.2-8 与 §2.2-10 两次明文「同一提交」——T1～T4 的全部改动合并为**一个提交**，由 T4 执行 commit。逐 task 提交惯例被冻结契约压倒。

## 0. 基线判据与执行边界（判据先在基线跑，本节点已亲跑）

出稿前在基线 HEAD 复核的全部判据与现状事实（原始读数留痕于 `docs/ledgers/2026-08-26-c12.1-plan-ledger.md`）：

- `cd graph && go build ./...` → GO_BUILD_OK；`go test ./codegraph/ ./cli/ -count=1` → 两包 `ok`。
- `cd graph/webui && npm ci && npm run typecheck && npm test` → typecheck 退出 0；**Test Files 21 passed (21)，Tests 136 passed (136)**。
- 序列化探针（一次性模块，replace 到本仓 graph/）：`json.Marshal(codegraph.BestDomain{})` 输出 **`{"label":"","responsibility":""}`**——键在场且无 omitempty；`ValidateBest` 对空职责领域产出 issue **`领域 d_x 的 responsibility 不能为空`**。这两条是验收 2 反转重写的现状锚。
- `ValidateBest` 调用面 = `migrate.go:157`、`cli.go:139/:260`，`check.go` 不调用：夹具删字段不会经 Check 链误伤任何既有断言。
- `AssembleContext` 唯一外部调用方 = `graph/cli/cli.go:601`：库内改私有签名、公开签名不动 → CLI 零改动。
- TS 读数面全量 grep 实录（生产侧）：`types.ts:62`、`besttree.ts:11/:20/:83/:177/:201/:576`、`BestScopePanorama.tsx:207`、`BestPanorama.tsx:219`、`BestDetail.tsx:125`；`BestDomainPage.tsx:40` 读的是 decl（新所有者），保留。

### 对 breakdown 有界文件集的六处机械圈定补遗

breakdown 清单按其声明是「编译/grep 机械圈定」，本节点重新机械圈定后补六处（每处带证据，均随同一提交处置）：

| 补遗文件 | 证据 |
|---|---|
| `graph/codegraph/migrate_test.go` | :131 读 `.Responsibility`（编译强制）；:101 断言 Notes==3、:104 断言含 "Responsibility" 提示行（行为强制） |
| `graph/webui/src/app/codegraph/domainpage.test.ts` | :84 起 `const best: CgBest = {…}` 带类型注解，删镜像字段后 excess property check 使 typecheck 必红 |
| `graph/webui/src/app/codegraph/BestPanorama.tsx` | :219 渲染 `card.responsibility`；contract §1 只列三组件，生产文件本身漏列 |
| `graph/webui/src/app/codegraph/CodegraphPage.tsx` | :252/:261/:284 三处挂载点需穿线 `decls={data.decls}`（breakdown 注明「props 穿线」但清单漏本文件） |
| `graph/webui/src/api/client.test.ts` | breakdown K1 缺陷族表自己点名的 wire 回放穿线断言落点 |
| `graph/codegraph/testdata/repo/codegraph/best.json` | 三个 domain 带 responsibility 键；夹具代表当前 wire 形状，随刀剥键 |

### 明确不触碰清单（越界即错）

- `graph/codegraph/migrate.go#migrateV2Domain.Responsibility`（:48）**保留**：冻结历史 v2 target.json 的严格解码器（`decodeStrict` 开 DisallowUnknownFields），真实旧文件的 domains[] 可含该键，删除会拒迁存量仓。【释1】对本文件的刀口只有写入占位符与 migrationNotes 提示行。
- decl 一侧全部保留（职责正文的**新所有者**）：`decl.go:23`、`decls.go:45/:55`、`entity.go:72`、`anchor_test.go`、`decl_test.go`、`client.test.ts:80/:93`、`types.ts:36`（CgDomainDecl）。
- `graph/cli/cli_test.go:324` 裸 JSON best 夹具不改：字符串字面量不受编译强制，恰好构成 CLI 层旧形状容忍的活证据；库层容忍另加专测。
- `graph/cli/cli.go`、`scopepage.ts`、`flowpage.ts`、`domainpage.ts`、`BestLeafGraph.tsx`、`BestOverlays.tsx`、`useCodegraph.ts`、`client.ts` 生产代码零改动。
- 不触 `internal/agentd`、`codegraph/domains/*`、扫描器；不创建 `codegraph/diffs/*.json`（存量无图）。

## 1. 跨任务接口与依赖

```text
T1（同刀红灯铺设：Go+TS 全部锁缝测试写完跑红）
   ├─> T2（Go 半边最小实现）──┐
   └─> T3（TS 半边最小实现）──┴─> T4（全量回归、变异复验、单提交收口）
```

T1 先行点亮全部锁缝断言（今天全红或部分绿，见各支标注）；T2/T3 互不依赖、可任意先后（工作树中间态不提交）；T4 消费两侧产出做回归与收口。**本卡不触 spec 接缝清单的缝 1（deriveScopePage）/缝 2（deriveFlowPage）**——那两缝属 K2/K3；本卡的缝级断言钉在四条既有声明缝上：

| 缝 | 入口符号 | 缝级断言落点 |
|---|---|---|
| best wire 缝（三方共用文件格式） | Go `json.Marshal`/`LoadBest`；viewer `fetchCodegraph` | TestBestJSONGolden（反转）、TestBestDomainWireShape、client.test.ts 回放对 |
| agent 取数缝 | `AssembleContext` | TestAssembleContextSummaryComesFromDecls |
| 迁移缝 | `MigrateTarget` | migrate_test 占位符三断言反转 |
| 组件缝 | `BestDetail`/`BestPanorama`/`BestScopePanorama`/`CodegraphPage` | 各组件测试的未声明态/正文态断言 |

### T2 Produces（Go 精确签名）

```go
// best.go —— BestDomain 删字段后（Responsibility 整行删除）
type BestDomain struct {
	Label  string `json:"label"`
	Parent string `json:"parent,omitempty"`
	Type   string `json:"type,omitempty"`
}

// ValidateBest 签名不变；行为变化仅一条：不再产出含 "responsibility 不能为空" 的
// issue。其余规则（meta.project、parent 存在性/环、顶层 type 二值、非顶层无 type、
// 容器引用存在、容器只挂叶子）逐条保持。
func ValidateBest(b *Best) []string

// context.go —— 私有函数加参；decls 由 AssembleContext 上移加载后传入
func contextVocabulary(v *View, best *Best, decls map[string]DomainDecl, domainID string) (ContextDomain, func(Node) bool, error)

// AssembleContext 公开签名不变（cli.go:601 零改动）：
func AssembleContext(v *View, g *Graph, best *Best, repoRoot, domainID string, opts QueryOptions) (*ContextResult, error)

// migrate.go —— migrationNotes 从三条减为两条；migrateInitialBest 不再写职责正文
func migrationNotes() []string // {"初版是现状的机械翻译，不是最优结构。", "container-misplaced 初版下的预期条数为 0；这不代表没有 gap。"}
```

P4-A 语义钉死：best 在场分支的 `ContextDomain.Summary = decls[domainID].Responsibility`（无声明为空串，`omitempty` 下键省略）；best 缺席降级分支 Summary 仍取现状域自身 summary 字段，不变。两种情况都不 panic。

### T3 Produces（TS 精确签名）

```ts
// types.ts
export interface CgBestDomain { label: string; parent?: string; type?: string }

// besttree.ts —— 三个模型字段变可选；undefined 即无声明文件（§2.2-11），禁止兜底回退
export interface BestSubsystem {
  id: string; label: string; responsibility?: string; type: string
  childIds: string[]; descendantIds: string[]
}
export interface BestCardReadout {
  id: string; label: string; responsibility?: string; type: string
  containerCount: number; misplacedCount: number; subdomainCount: number
}
export interface BestScopeCard {
  id: string; label: string; responsibility?: string; type: string; external: boolean
  containerCount: number; misplacedCount: number; childCount: number
}

// besttree.ts —— 三个导出函数尾部加可选 decls 参数（其余参数原序不动；
// BestEdgeDetail 处 :281 的四参调用因此零改动可编译）
export function bestSubsystems(best: CgBest, decls?: CgDomainDecls): BestSubsystem[]
export function aggregateBestCards(best: CgBest, report?: CgCheckReport, decls?: CgDomainDecls): Record<string, BestCardReadout>
export function bestScopeGraph(
  best: CgBest, target: CgTarget | undefined, report: CgCheckReport | undefined,
  scopeId: string | null, decls?: CgDomainDecls,
): BestScopeGraph

// 三组件 Props 各增一个可选字段；CodegraphPage 以 decls={data.decls} 穿线
export interface BestDetailProps { /* 既有字段不动 */ decls?: CgDomainDecls }
export interface BestPanoramaProps { /* 既有字段不动 */ decls?: CgDomainDecls }
export interface BestScopePanoramaProps { /* 既有字段不动 */ decls?: CgDomainDecls }
```

**跨卡签名交接**（供 K2/K6 plan 对表）：K2 只消费 `topLevelSubsystemIds/subsystemOf/containerFacts`，签名不变；K6 重接线时 `bestScopeGraph` 四参调用合法（第五参可选）。`responsibility` 字段语义契约：存在 ⇔ decls 提供非空正文（helper 按构造保证）；undefined ⇔ 无声明；**不存在第三态**。

### DOM 契约（组件缝的行为判据，jsdom 可验）

每个职责渲染位二选一，带 data 标记供行为查询：

- 有正文：`<div data-declaration-text>{正文}</div>`
- 无声明：`<div data-declaration-missing>未声明 · 请写入 codegraph/domains/{id}.json</div>`，其中 `{id}` 为 best 领域 id（`ext:` 圈外卡剥前缀，沿 `BestScopePanorama.tsx:197` 既有的 `card.id.slice(4)` 取法）。

## 2. T1｜同刀红灯铺设（锁缝断言先行，最薄路径）

### 文件集（本 task 只动测试）

- `graph/codegraph/best_test.go`
- `graph/codegraph/context_test.go`
- `graph/codegraph/migrate_test.go`
- `graph/webui/src/app/codegraph/besttree.test.ts`
- `graph/webui/src/app/codegraph/BestDetail.test.tsx`
- `graph/webui/src/app/codegraph/BestPanorama.test.tsx`
- `graph/webui/src/app/codegraph/BestScopePanorama.test.tsx`
- `graph/webui/src/app/codegraph/CodegraphPage.test.tsx`
- `graph/webui/src/api/client.test.ts`

**注意**：vitest 不做类型检查（esbuild 转译），T1 阶段 TS 测试里传尚不存在的 `decls` prop / 第三实参可以运行并按预期红；typecheck 留到 T3/T4 才有意义。Go 侧新测试全部可在现结构上编译（只是断言反着写）。**testdata fixture 与生产代码本 task 一律不动**（否则旧 ValidateBest 会制造范围外的 cli 红噪音）。

### Consumes / Produces

Consumes：§0 基线锚（金样本字符串、issue 文案、Summary="命令入口"、Notes==3）、既有测试夹具与 harness 形态（照抄各自文件现状写法）。

Produces：下述全部测试代码本身（红→T2/T3 后转绿的锁缝断言）。

### 步骤

1. **Go wire 缝反转**：`best_test.go` 内（a）`TestBestJSONGolden` 字面量删 `Responsibility: "编排任务生命周期"`，want 串中 `"responsibility":"编排任务生命周期",` 删除；（b）`TestBestDomainOmitempty` 整支替换为下方 `TestBestDomainWireShape`；（c）新增 `TestLoadBestToleratesLegacyResponsibilityKey`（此支基线即绿，属过渡期回归锁，不参与红灯计数）；（d）`TestValidateBestRules` 删 `{name: "responsibility required", …}` 一行、其余 case 的字面量删 `Responsibility:` 字段，新增下方 `TestValidateBestStopsEnforcingResponsibility`；（e）`TestBestJSONRoundTripProperty` 删 `Responsibility: randomString(rng),`；（f）`validBest()` 两处删字段。

    ```go
    // TestBestDomainWireShape 钉死删除职责正文后的 BestDomain wire 形状
    // （C12 契约 §2.2-8/-9）：parent/type 保持 omitempty，label 保持必出；
    // 序列化输出不得再出现 responsibility 键——这是跨仓手写文件格式契约，
    // 键一旦回流，handoff 侧会把 best 再度读成双写正文。
    func TestBestDomainWireShape(t *testing.T) {
    	leaf, err := json.Marshal(BestDomain{Parent: "d_coordination"})
    	if err != nil {
    		t.Fatalf("编码叶子领域: %v", err)
    	}
    	if want := `{"label":"","parent":"d_coordination"}`; string(leaf) != want {
    		t.Fatalf("叶子领域编码漂移:\n got %s\nwant %s", leaf, want)
    	}
    	top, err := json.Marshal(BestDomain{Label: "L", Type: "boundary"})
    	if err != nil {
    		t.Fatalf("编码顶层领域: %v", err)
    	}
    	if want := `{"label":"L","type":"boundary"}`; string(top) != want {
    		t.Fatalf("顶层领域编码漂移:\n got %s\nwant %s", top, want)
    	}
    	empty, err := json.Marshal(BestDomain{})
    	if err != nil || strings.Contains(string(empty), "responsibility") {
    		t.Fatalf("零值编码不得再含 responsibility 键: raw=%s err=%v", empty, err)
    	}
    }

    // 原「responsibility required」用例随字段同刀反转：职责正文唯一所有者迁到
    // decl 文件（C12 契约 §2.2-9），best 只留结构——校验器不得再对职责正文发声，
    // 也不得顺手弱化无关规则（parent 校验必须仍然命中）。
    func TestValidateBestStopsEnforcingResponsibility(t *testing.T) {
    	b := &Best{
    		Meta:       BestMeta{Version: 1, Project: "test"},
    		Domains:    map[string]BestDomain{"d_leaf": {Label: "leaf", Parent: "d_ghost"}},
    		Containers: map[string]string{},
    	}
    	issues := ValidateBest(b)
    	if !containsIssue(issues, "d_ghost") {
    		t.Fatalf("无关规则不得被顺手弱化，issues=%v", issues)
    	}
    	for _, issue := range issues {
    		if strings.Contains(issue, "responsibility") {
    			t.Fatalf("职责正文已归 decl 所有，校验器不得再报 %q", issue)
    		}
    	}
    }

    // 旧版 best.json 含 responsibility 键（C12 刀前的手写文件）：encoding/json
    // 默认忽略未知键（c12-contract §3-1），加载必须成功且结构字段无损——
    // 存量项目在重扫前仍是旧形状，这是过渡期的真实输入而非脏数据。
    func TestLoadBestToleratesLegacyResponsibilityKey(t *testing.T) {
    	root := t.TempDir()
    	if err := os.MkdirAll(filepath.Join(root, "codegraph"), 0o755); err != nil {
    		t.Fatal(err)
    	}
    	raw := `{"meta":{"version":1,"project":"legacy"},` +
    		`"domains":{"d_x":{"label":"X","responsibility":"旧正文","type":"logic"}},` +
    		`"containers":{}}`
    	if err := os.WriteFile(filepath.Join(root, "codegraph", "best.json"), []byte(raw), 0o644); err != nil {
    		t.Fatal(err)
    	}
    	best, err := LoadBest(root)
    	if err != nil || best == nil {
    		t.Fatalf("旧形状 best 必须照常加载: best=%#v err=%v", best, err)
    	}
    	if best.Domains["d_x"].Label != "X" || best.Domains["d_x"].Type != "logic" {
    		t.Fatalf("未知键不得影响结构解析: %+v", best.Domains["d_x"])
    	}
    	if issues := ValidateBest(best); len(issues) != 0 {
    		t.Fatalf("旧形状文件在新校验器下必须合法: %v", issues)
    	}
    }
    ```

    §2.2-13 保持项反面断言**已在库**：`TestValidateBestRules` 表内 `{name: "container points to leaf", … want: "非叶子"}` 就是违例正例夹具，本 task 只验证它仍红转绿路径，不改写。

2. **agent 取数缝**：`context_test.go` 的 `:187` 字面量删 `Responsibility: "父域",`；追加整支：

    ```go
    // P4-A 裁决：agent 取数路径的领域摘要不随字段删除而消失，改由 decl 文件供正文；
    // 无声明的领域摘要如实为空（omitempty 后键省略），不得回退到 best 树上找补。
    // 断言值特意取 decl 正文全文（"命令入口与调度"），与旧 best 来源（"命令入口"）可区分。
    func TestAssembleContextSummaryComesFromDecls(t *testing.T) {
    	g, err := LoadGraph("testdata/repo")
    	if err != nil {
    		t.Fatal(err)
    	}
    	v := Merge(g, nil)
    	best := loadFixtureBest(t)

    	declared, err := AssembleContext(v, g, best, "testdata/repo", "d_cmd", QueryOptions{})
    	if err != nil {
    		t.Fatal(err)
    	}
    	if declared.Domain.Summary != "命令入口与调度" {
    		t.Fatalf("Summary 必须等于 decl 正文，got %q", declared.Domain.Summary)
    	}

    	undeclared, err := AssembleContext(v, g, best, "testdata/repo", "d_svc", QueryOptions{})
    	if err != nil {
    		t.Fatal(err)
    	}
    	if undeclared.Domain.Summary != "" {
    		t.Fatalf("无声明时 Summary 必须为空串，got %q", undeclared.Domain.Summary)
    	}
    }
    ```

3. **迁移缝**：`migrate_test.go` —（a）`:101` `len(got.Notes) != 3` 改 `!= 2`；（b）`:104` 子串清单删去 `"Responsibility",`；（c）`:127` 起的 for 循环后追加产物级断言（`strings`/`filepath` 已在该文件 import）：

    ```go
    	// 占位符写入与提示行已随 §2.2-8 同刀移除：迁移产物不得再携带
    	// responsibility 键——否则迁移命令本身就在制造第二份职责正文。
    	migratedRaw, err := os.ReadFile(filepath.Join(repo, "codegraph", "best.json"))
    	if err != nil {
    		t.Fatal(err)
    	}
    	if strings.Contains(string(migratedRaw), "responsibility") {
    		t.Fatalf("迁移产物不得再写职责正文键: %s", migratedRaw)
    	}
    ```

4. **跑 Go 红**：

    ```bash
    cd graph && go build ./... && go test ./codegraph/ -count=1
    ```

    预期非零，红灯原文必须包含以下三类且**仅**因新断言而红：`TestBestJSONGolden` 金样本漂移（got 含 responsibility）、`TestBestDomainWireShape` 零值含键、`TestValidateBestStopsEnforcingResponsibility` 报出 responsibility issue、`TestAssembleContextSummaryComesFromDecls` got "命令入口"、migrate Notes 计数与产物含键。若出现其它原始错误，贴原文记录台账后只修测试调用面，不改判据。

5. **TS 模型与组件缝红灯**：`besttree.test.ts` 尾部追加（顶部 import 行补 `, CgDomainDecls` 类型导入）：

    ```ts
    const decls: CgDomainDecls = {
      ss_api: { domain: 'ss_api', responsibility: '对外服务的声明正文' },
    }

    describe('C12.1 职责正文唯一所有权', () => {
      it('decls 提供正文时三个派生模型的 responsibility 都是声明正文', () => {
        expect(bestSubsystems(best, decls).find((s) => s.id === 'ss_api')?.responsibility)
          .toBe('对外服务的声明正文')
        expect(aggregateBestCards(best, report, decls).ss_api?.responsibility)
          .toBe('对外服务的声明正文')
        expect(bestScopeGraph(best, target, report, null, decls).cards.find((c) => c.id === 'ss_api')?.responsibility)
          .toBe('对外服务的声明正文')
      })

      it('decls 缺席时 responsibility 为 undefined——不是空串也不是 best 来源文本', () => {
        for (const subsystem of bestSubsystems(best)) expect(subsystem.responsibility).toBeUndefined()
        for (const card of Object.values(aggregateBestCards(best, report))) expect(card.responsibility).toBeUndefined()
        for (const card of bestScopeGraph(best, target, report, null).cards) expect(card.responsibility).toBeUndefined()
      })
    })
    ```

    三个组件测试各追加一支（fixture 与渲染参数照抄各自文件第一支用例的现状写法，`decls` 为新增可选 prop）：

    - `BestDetail.test.tsx`：

      ```tsx
      it('职责位显示 decls 正文；缺席时显示未声明+写入路径', () => {
        const decls: import('../../api/types').CgDomainDecls = {
          s_api: { domain: 's_api', responsibility: '人写的对外服务声明' },
        }
        const withDecl = render(<BestDetail best={best} baseline={baseline} report={report} decls={decls} subsystemId="s_api" />)
        expect(withDecl.container.querySelector('[data-declaration-text]')?.textContent).toContain('人写的对外服务声明')
        withDecl.unmount()

        const withoutDecl = render(<BestDetail best={best} baseline={baseline} report={report} subsystemId="s_api" />)
        expect(withoutDecl.container.querySelector('[data-declaration-text]')).toBeNull()
        const missing = withoutDecl.container.querySelector('[data-declaration-missing]')
        expect(missing?.textContent).toContain('未声明')
        expect(missing?.textContent).toContain('codegraph/domains/s_api.json')
      })
      ```

    - `BestPanorama.test.tsx`（首支用例 `'对外服务'` 文本断言改为经 decl 路径供给：给该支 render 加 `decls={{ ss_api: { domain: 'ss_api', responsibility: '对外服务' } }}`——断言文本不变、来源换成 decl，旧断言不废）；另追加：

      ```tsx
      it('decls 缺席时卡面职责位显式未声明，不出现任何正文占位', () => {
        const { container } = render(<BestPanorama best={best} target={target} report={report}
          selectedSubsystem="" selectedEdge="" onSelectSubsystem={vi.fn()} onSelectEdge={vi.fn()} />)
        expect(container.querySelector('[data-declaration-text]')).toBeNull()
        const missing = container.querySelectorAll('[data-declaration-missing]')
        expect(missing.length).toBe(2)
        expect(missing[0]?.textContent).toContain('codegraph/domains/')
      })
      ```

    - `BestScopePanorama.test.tsx`：

      ```tsx
      it('decls 缺席时子领域卡与圈外卡都显式未声明，写入路径用剥壳后的领域 id', () => {
        const { container } = render(<BestScopePanorama best={best} target={nestedTarget} report={report}
          scopeId="ss_api" selectedDomain="" selectedEdge="" migrationItems={[]}
          onSelectDomain={vi.fn()} onSelectEdge={vi.fn()} onEnter={vi.fn()} onSelectMigration={vi.fn()} />)
        expect(container.querySelector('[data-declaration-text]')).toBeNull()
        expect(container.textContent).toContain('codegraph/domains/api_read.json')
        expect(container.textContent).toContain('codegraph/domains/ss_store.json')
      })
      ```

    - `CodegraphPage.test.tsx`（页面装配缝）：

      ```tsx
      it('best 页面职责位随 decls 有无切换：装配层不凭空造声明', async () => {
        state.data = bestResp
        const noDecl = render(<CodegraphPage />)
        await waitFor(() => expect(noDecl.container.querySelectorAll('[data-best-subsystem]').length).toBe(2))
        expect(noDecl.container.querySelector('[data-declaration-missing]')).toBeTruthy()
        noDecl.unmount()

        state.data = { ...bestResp, decls: { s_api: { domain: 's_api', responsibility: '页面级声明正文' } } }
        const withDecl = render(<CodegraphPage />)
        await waitFor(() => expect(withDecl.container.querySelectorAll('[data-best-subsystem]').length).toBe(2))
        expect(withDecl.container.querySelector('[data-declaration-text]')?.textContent).toContain('页面级声明正文')
      })
      ```

6. **wire 回放穿线**（`client.test.ts`，基线即绿——它的职责是锁传输边界语义而非制造红灯，属保持项回归）：

    ```ts
    it('C12.1 同刀后 best 领域不带职责键：缺失可辨；旧文件带键也 tolerated', async () => {
      const fetchMock = vi.fn()
      vi.stubGlobal('fetch', fetchMock)
      const base = {
        baseline: {
          meta: { project: 'demo', branch: 'main', commit: 'c', scannedAt: 'now', generator: 'test' },
          containers: {}, nodes: {}, edges: [],
        },
        views: {}, stale: [],
      }
      fetchMock.mockResolvedValueOnce(jsonResponse({
        ...base,
        best: { meta: { version: 1, project: 'demo' }, domains: { d_x: { label: 'X', type: 'logic' } }, containers: {} },
      }))
      const fresh = await fetchCodegraph('demo')
      expect((fresh.best?.domains.d_x as unknown as { responsibility?: string }).responsibility).toBeUndefined()

      fetchMock.mockResolvedValueOnce(jsonResponse({
        ...base,
        best: { meta: { version: 1, project: 'demo' }, domains: { d_x: { label: 'X', responsibility: '旧正文', type: 'logic' } }, containers: {} },
      }))
      const legacy = await fetchCodegraph('demo')
      expect((legacy.best?.domains.d_x as unknown as { responsibility?: string }).responsibility).toBe('旧正文')
    })
    ```

7. **跑 TS 红**：

    ```bash
    cd graph/webui && npm test -- src/app/codegraph/besttree.test.ts src/app/codegraph/BestDetail.test.tsx src/app/codegraph/BestPanorama.test.tsx src/app/codegraph/BestScopePanorama.test.tsx src/app/codegraph/CodegraphPage.test.tsx src/api/client.test.ts
    ```

    预期非零；红灯原文逐支记录台账：besttree 三函数断言 got 旧 best 正文、三组件 `[data-declaration-missing]` 查询为 null、CodegraphPage 装配缺未声明态。`client.test.ts` 新支应绿（保持项）。其余未触及测试文件本命令不加载。

### T1 缺陷族对抗审查（节选，完整八族见 §6）

假红/假绿：红灯驱动支全部从声明缝入口构造（Marshal/ValidateBest/AssembleContext/MigrateTarget/组件 props→DOM），无内部 helper 直调；`TestLoadBestToleratesLegacyResponsibilityKey` 与 client 回放基线绿是刻意的（它们锁的是「不回退」这一负向性质，只能以保持项形式存在）。

## 3. T2｜Go 半边最小实现（S1 · 逻辑型）

### 文件集

- `graph/codegraph/best.go`
- `graph/codegraph/context.go`
- `graph/codegraph/migrate.go`
- `graph/codegraph/testdata/repo/codegraph/best.json`

### Consumes / Produces

Consumes：T1 的红灯清单；`LoadDomainDecls(repoRoot)`（context.go:127 现状调用）；`decls.go#DomainDecl.Responsibility`。

Produces：§1 T2 Produces 块的全部签名与行为。

### 步骤

1. **best.go 刀口**：（a）`BestDomain` 结构体删 `Responsibility` 整行（:35），结构体 doc 注释追加一行：`// 职责正文不在本结构：唯一所有者是 codegraph/domains/<id>.json 的 DomainDecl（C12 契约 §2.2-9），best 只留结构。`（中文措辞，避免在 grep 判据文件引入英文标识符）；（b）`ValidateBest` 删 :92-94 三行非空检查。**注释纪律**：本文件最终状态 grep "Responsibility" 必须零输出（验收 1），注释同样不得出现该词。
2. **context.go decls 穿线**：（a）把 ：127-131 的 `LoadDomainDecls` 块整体上移到 ：106 `contextVocabulary` 调用之前（错误处理原样随迁：`slog.Default().Error(...stage: "load-declarations"...)` + `fmt.Errorf("context %s 加载声明失败: %w", ...)`），后续 ：132 起的声明查找复用同一 `decls` 变量、删除重复加载；（b）`contextVocabulary` 签名改为 `(v *View, best *Best, decls map[string]DomainDecl, domainID string)`，调用点 :106 同步传参；（c）best 分支返回值 ：369-370 改为：

    ```go
    	out := ContextDomain{ID: domainID, Label: domain.Label, Kind: domain.Type,
    		Summary: declSummary(decls, domainID), Parent: domain.Parent, Children: children}
    ```

    并在 contextVocabulary 附近新增私有 helper（放在 unknownBestDomainError 之前）：

    ```go
    // declSummary 是 best 分支领域摘要的唯一取数点：职责正文唯一所有者是 decl
    // 文件（C12 契约 §2.2-9），无声明如实为空串（omitempty 后键省略），
    // 禁止任何来自 best 结构的兜底回填。
    func declSummary(decls map[string]DomainDecl, domainID string) string {
    	return decls[domainID].Responsibility
    }

    ```
   （d）现状降级分支（best==nil，:352）`Summary: domain.Summary` **不动**——那是现状视图自身的包级摘要字段，不是 best 双写。（e）函数 doc 注释补一句行为约定：「领域摘要自 C12 起取 decls 正文，无声明为空」。
3. **migrate.go 刀口**：`migrateInitialBest` 删 :179-182 的占位符逻辑与 :185 的 `Responsibility: responsibility,`，循环体只剩 `Label: subsystem.Name, Type: subsystem.Type`；`migrationNotes()` 删 `"Responsibility 是占位符，请逐项补写.",` 整行。`migrateV2Domain` 解码器**一字不动**（§0 不触碰清单）。
4. **testdata 剥键**：`graph/codegraph/testdata/repo/codegraph/best.json` 三个 domain 各删 `"responsibility": "…",` 一个键值对，其余字节不动。顺序约束：必须在第 1 步之后做（先剥键会让 cli validate 在旧校验器下爆出范围外红灯）。
5. **跑 Go 绿**：

    ```bash
    cd graph && go build ./... && go vet ./codegraph/ && go test ./codegraph/ ./cli/ -count=1
    ```

    预期退出 0；T1 五类红灯逐支转绿。`grep -n "Responsibility" graph/codegraph/best.go` 零输出。
6. **日志与注释核对**：本 task 零新增日志点——`AssembleContext`/`contextVocabulary` 是纯装配函数，成功路径静默、失败经 error/slog 已覆盖（项目既有约定，c1.10-plan T2 同款结论）；`declSummary`/`BestDomain`/`migrationNotes` 的「为什么」已写入各自注释（第 1/2/3 步内嵌），迁移提示行删除的理由写在 migrate_test 新断言注释里。

### 测试范围声明

只跑 `go build ./...`、`go vet ./codegraph/`、`go test ./codegraph/ ./cli/ -count=1`。全量 `go test ./...` 归 T4。

## 4. T3｜TS 半边最小实现（S3 读数点 · 镜像删字段 + decls 穿线 + 夹具换底）

### 文件集

生产：`types.ts`、`besttree.ts`、`BestDetail.tsx`、`BestPanorama.tsx`、`BestScopePanorama.tsx`、`CodegraphPage.tsx`。
测试换底+收红：`besttree.test.ts`、`BestDetail.test.tsx`、`BestPanorama.test.tsx`、`BestScopePanorama.test.tsx`、`BestLeafGraph.test.tsx`、`BestDomainPage.test.tsx`、`domainpage.test.ts`、`CodegraphPage.test.tsx`（T1 已加的新支不动，只换旧夹具）、`client.test.ts`（T1 已完成，本 task 不再动）。

### Consumes / Produces

Consumes：T1 红灯清单；`CodegraphResp.decls?`（types.ts:114 已在）；`CgDomainDecl.responsibility`（types.ts:36 保留）。
Produces：§1 T3 Produces 块全部签名 + §1 DOM 契约。

### 步骤

1. **镜像删字段**：`types.ts:62` 整行替换为：

    ```ts
    /** CgBestDomain 理想树领域：parent 为空即顶层子系统。只留结构——
     *  职责正文唯一所有者是 CgDomainDecl（codegraph/domains/<id>.json，C12 契约 §2.2-9）。 */
    export interface CgBestDomain { label: string; parent?: string; type?: string }
    ```

2. **besttree 换源**：import 行补 `CgDomainDecls` 类型；三个接口的 `responsibility: string` 改 `responsibility?: string` 并各加注释 `/** decls 有非空正文时存在；undefined 即无声明文件（§2.2-11）。 */`；三个导出函数按 §1 签名加尾参；内部改动：

    ```ts
    /** 职责正文唯一读取点：来源 decls，绝不回退 best 结构。声明缺席或正文为空
     *  都归一成 undefined，调用方据此渲染「未声明」态（§2.2-11 禁兜底）。 */
    function declaredResponsibilityOf(decls: CgDomainDecls | undefined, domainId: string): string | undefined {
      const text = decls?.[domainId]?.responsibility
      return text || undefined
    }
    ```

    `bestSubsystems` 的 `:177` 改 `responsibility: declaredResponsibilityOf(decls, id),`；`aggregateBestCards` 的 `:201` 改 `responsibility: subsystem.responsibility,`（上游已换源）并把 `:196` 改 `for (const subsystem of bestSubsystems(best, decls))`；私有 `scopeCard` 加第 5 参 `decls`、`:576` 改 `responsibility: declaredResponsibilityOf(decls, domainId),`；`bestScopeGraph` 的两处 `scopeCard(...)` 调用透传 decls。
3. **三组件渲染位**：按 §1 DOM 契约替换三处职责渲染（`BestDetail.tsx:125`、`BestPanorama.tsx:219`、`BestScopePanorama.tsx:207`），Props/destructure 加 `decls?`，派生调用点补实参（`BestDetail.tsx:89` → `bestSubsystems(best, decls)`；`BestPanorama.tsx:72` → `aggregateBestCards(best, report, decls)` 且 useMemo 依赖数组补 `decls`；`BestScopePanorama.tsx:54` → `bestScopeGraph(best, target, report, scopeId, decls)` 且依赖数组补 `decls`）。`ext:` 卡写入路径用 `card.external ? card.id.slice(4) : card.id`。
4. **CodegraphPage 穿线**：`:252` `<BestPanorama …>`、`:261` `<BestScopePanorama …>`、`:284` `<BestDetail …>` 三处各加 `decls={data.decls}`。
5. **夹具换底**（机械删行，逐文件）：上述测试文件里一切 `const x: CgBest = {…}` / `resp.best` 字面量的 domains 条目删 `responsibility: '…',` 片段——`besttree.test.ts` :24-28/:71-72/:207-209/:261、`domainpage.test.ts` :87-90/:110/:232-233/:268-270、`BestDetail.test.tsx` :9-12/:62/:75、`BestDomainPage.test.tsx` :34-35、`BestLeafGraph.test.tsx` :10-12、`BestPanorama.test.tsx` :9-11、`BestScopePanorama.test.tsx` :10-13、`CodegraphPage.test.tsx` :45-48。**保留**：一切 decl 夹具的 `responsibility`（`BestDomainPage.test.tsx:42`、`domainpage.test.ts` :95/:168、`CodegraphPage.test.tsx` :252 一带的 decls、`client.test.ts` 全部）。换底后 `rg "responsibility" src/api/types.ts` 仅剩 :36 一处（验收 5）。
6. **跑 TS 绿**：

    ```bash
    cd graph/webui && npm run typecheck && npm test -- src/app/codegraph src/api/client.test.ts
    ```

    typecheck 退出 0（excess property 红全消）；focused vitest 退出 0，T1 全部红灯支转绿、client 回放对保持绿。
7. **日志与注释**：组件渲染位不加日志——DOM data 标记本身就是可观察行为，机内测试直接查询；`declaredResponsibilityOf` 的「为什么」注释第 2 步已给；`useMemo` 依赖补齐是对既有 effect 卫生约定的遵守，无需新日志。纯派生层维持零 console（项目约定）。

### 测试范围声明

只跑 webui typecheck + focused vitest（codegraph 目录 + client.test.ts）。全量 `npm test` 归 T4。

## 5. T4｜全量回归、变异复验与单提交收口

### 文件集

无新增代码文件；操作对象是全仓验证与 git。

### 步骤

1. **双侧全量回归**：

    ```bash
    cd graph && go build ./... && go test ./... -count=1
    cd graph/webui && npm run typecheck && npm test
    git diff --check
    ```

    预期：Go 全包 ok（含 cli/webui/cmd）；webui 全量 ≥21 文件全绿（T1/T3 净增支数为正）；typecheck 0；diff check 干净。
2. **静态刀口复核**：

    ```bash
    grep -n "Responsibility" graph/codegraph/best.go        # 预期无输出
    rg -n "responsibility" graph/webui/src/api/types.ts     # 预期仅 :36 一行（CgDomainDecl）
    rg -n "domains?\[.*\]\.?r|domain\.responsibility" graph/webui/src/app/codegraph/besttree.ts   # 预期无输出
    ```

    第三条是机械判据：knife 后 besttree 里唯一合法的该词读取点是 `declaredResponsibilityOf` 内部对 `decls[domainId].responsibility` 的取值；任何对 best 域对象的同名字段读取（`domain.responsibility` 形态）都是兜底回潮，必须零命中（残余读取同时会被 typecheck 以「属性不存在」拦下，双闸）。
3. **变异钻取一（TS 兜底回填必红，临时副本验后恢复）**：在 `besttree.ts#bestSubsystems` 内把该行临时改为兜底形态 `responsibility: declaredResponsibilityOf(decls, id) ?? domain.label,`（模拟后人「圆场」的第一反应），跑：

    ```bash
    cd graph/webui && npm test -- src/app/codegraph/besttree.test.ts src/app/codegraph/BestPanorama.test.tsx
    ```

    必须 ≥1 支转红（`undefined` 断言与 `[data-declaration-missing]` 断言命中）；恢复后全绿。变异副本不提交。
4. **变异钻取二（Go 金样本回流必红）**：临时在 `BestDomain` 回填 `Responsibility string \`json:"responsibility"\`` 一行，跑 `go test ./codegraph/ -run 'TestBestDomainWireShape|TestBestJSONGolden' -count=1`，两支必红；删除恢复，全绿。变异副本不提交。
5. **越界路径扫描**（交棒欠账未被代做的证据）：`git status --porcelain` 确认改动集 ⊆ §2/§3/§4 文件集 ∪ 台账；无 `internal/agentd`、`codegraph/domains/`、`docs/codegraph-scan-recipe.md` 路径出现。
6. **单提交**：`git add` 本计划列出的全部文件与两个文档（本 plan + ledger），提交信息：

    ```
    codegraph(C12.1): K1 responsibility 同刀迁移——best 删字段、职责正文归 decl 唯一所有

    - BestDomain 删 Responsibility 字段与 ValidateBest 非空检查（§2.2-8/-9）
    - contextVocabulary 领域摘要改读 decls（P4-A），AssembleContext 公开签名不变
    - migrate 去占位符写入与提示行；migrateV2Domain 历史解码器保留
    - TS 镜像删字段、besttree 三函数加 decls 尾参、三组件+装配页穿线 decls
    - 未声明显式态（未声明 + 写入路径），禁兜底回退（§2.2-11）
    - 同刀回归全绿；变异×2 复验（回填 label / 回流金样本必红）
    ```

    不 push。

### 测试范围声明

本 task 只做验证与提交，不再改产品代码（变异钻取的临时副本除外，验后恢复并以 `git diff` 空输出确认）。

## 6. 缺陷族对抗审查（五项检查之一，逐族结论）

| 族 | 结论 |
|---|---|
| 生命周期/状态机中断 | 无，因为纯内存类型/校验/派生改造：不起进程、不开订阅、不建临时资源；唯一持久效果是单次提交，中断重跑无害。 |
| 静默失败/误导报错 | 命中并锁定：删校验（-8/-9）与保校验（-13）分属两支独立测试（TestValidateBestStopsEnforcingResponsibility vs 「container points to leaf」case），防「顺手多删」造成空 best 全绿假象；Summary 无声明为空串而非编造（P4-A 断言）；UI 未声明显式态禁兜底（DOM 契约 + 变异钻取一执法）。 |
| 跨平台假设 | 无新增，因为 JSON tag 变更对所有平台同形，不触路径拼接/进程/权限/webview；TS 侧纯字符串与可选字段。 |
| 假红/假绿测试 | 命中并锁定：序列化断言「必须出现→必须不出现」是行为反转不是删测试（金样本+零值双断言）；-13 保持项用违例正例夹具锁（只有负例会稳定假绿）；夹具换底后跑全量不允许 skip；两条变异钻取证明断言能变红。 |
| 门禁绕过 | 无，因为无新增写/执行入口；ValidateBest 调用点（cli.go:139/:260、migrate.go:157）位置不变，门的位置不动，动的只是门内一条规则的退役。 |
| 序列化边界 | 命中且有穿线回归：Go marshal 钉值（WireShape/Golden）+ 迁移产物级断言 + client.test.ts 真实 Response JSON 回放（缺键⇒undefined、旧键⇒ tolerated，区分缺失与空串）+ 组件端 decls→DOM 断言；「两端各自有测试」的链路缺口由回放对与 CodegraphPage 装配断言补齐。 |
| 枚举新值过白名单 | 无，因为删的是字段不是词表项，不新增任何枚举值。 |
| 承重安全属性有测试锁住 | 无安全属性；承重的「单一所有权」属性由反面断言+变异钻取一锁住（把 best label 回填职责位必转红）。 |

## 7. 序列化边界清单（每一处手写投影都有断言）

1. `BestDomain` → JSON bytes（Go 出口）：Golden + WireShape + RoundTrip property（100 次随机构造 encode∘decode 恒等，既有测试保留并随字段瘦身）。
2. JSON bytes → `Best`（Go 入口，含未知键）：TestLoadBestToleratesLegacyResponsibilityKey（可空容忍：旧键在场不影响结构字段）。
3. `MigrateTarget` 产物 bytes：migrate_test 产物级「不得含键」断言 + 既有逐字节确定性测试自动覆盖新形状。
4. HTTP JSON → `CodegraphResp`（viewer 入口）：client.test.ts 回放对（缺键/旧键两态）。
5. `decls[].responsibility` → besttree 模型可选字段 → DOM 文本：besttree.test.ts（undefined/string 二态）+ 三组件 `[data-declaration-text]`/`[data-declaration-missing]` 断言 + CodegraphPage 装配断言。

## 8. Spec 故事归属与冻结条覆盖

| 冻结条/spec 故事 | 落点 |
|---|---|
| §2.2-8 四者同刀删除（+【释1】补遗） | T1 红灯 + T2/T3 实现，T4 单提交 |
| §2.2-9 职责正文唯一所有者=decl | T2 declSummary 单一取数点；T3 declaredResponsibilityOf 单一取数点 |
| §2.2-10 读数点统一改读 decls、同提交 | T3 步骤 3/4（三组件+六透传）；CodegraphPage 穿线 |
| §2.2-11 未声明显式+写入路径、禁兜底 | DOM 契约 + 组件/装配断言 + 变异钻取一 |
| §2.2-12 迁移不丢字 | 【移】数据在 handoff 仓（contract §6.2-1①），本卡不涉 |
| §2.2-13 容器只挂叶子保持 | 既有「container points to leaf」违例夹具全程保持绿 |
| §4.3 同刀回归口径 | T4 步骤 1 全量双侧 |
| spec 用户故事 4（基本信息 tab 含职责） | 职责位 decl 化是故事 4 的数据正确性前提，落 T3 |
| spec 用户故事 9（缺失显式告知，不做假读数） | 未声明显式态落 T3 |

缝 1/缝 2 相关冻结条（-17～-36）与本卡无交集，归属 K2/K3，此处不认领。

## 9. 真机清单（本卡新增项：无）

K1 全部判据 jsdom/Go 机内可验（wire 字节、校验清单、模型字段、DOM data 标记），无视觉/兼容/性能类新增真机项。C12 整轮真机清单见 breakdown §四，本卡不新增条目。

## 10. 占位符扫描与自审结论

- **占位符扫描**：全文无 TBD/「适当的错误处理」/「同 Task N」式骨架；所有新测试给出完整代码块，所有夹具换底给出逐文件逐行号指令；未使用「复用既有 harness 但形态未知」例外（各 harness 均已实际读过并在文中指名）。
- **接缝双向核对**：测试→缝——T1 每支红灯驱动的入口符号（Marshal/ValidateBest/AssembleContext/MigrateTarget/组件 JSX/fetchCodegraph/CodegraphPage）都在 §1 缝表的某条缝上；缝→测试——四条缝各有 ≥1 支缝级断言（wire：Golden/WireShape/回放对；agent：SummaryComesFromDecls；迁移：Notes+产物断言；组件：三组件+装配页）。缝 1/缝 2 不在本卡缝清单（spec 接缝清单归 K2/K3），无遗漏。
- **内部锁声明**（附加不顶替）：① besttree.test.ts 三支模型断言——理由：从声明缝（DOM）构造不出「undefined 与空串可区分」这条断言，DOM 文本查询无法分辨字段缺席与空渲染，必须钉在模型出口；② Go 单元表驱动用例（parent/type 等既有规则）——既有存量内部锁，本次仅随字面量瘦身，不新增、不顶替任何缝级断言。退路检查：T2/T3 步骤正文无条件退路条款，无未声明内部锁。
- **上下文预算**：T1 九文件、T2 四文件、T3 十四文件、T4 零文件；全集 24 个路径一次圈定，无开放式扩散。
- **类型标注**：本卡无边界型新增外部交互面（DOM 契约即机内形状判据），无新增真机清单条目（§9）。
- **跨卡签名**（对照未来 K2/K6 plan）：`bestSubsystems/aggregateBestCards/bestScopeGraph` 尾部可选 decls 参、三组件可选 decls prop、`CgBestDomain` 三字段形状已在本计划 §1 逐字冻结，供跨卡审计比对。
- **派发前自审**：本计划全部验收步骤均为本地构建/测试/grep/git 命令，无任何需要驱动派发系统自身的步骤，无需标注「由协调者执行」。
- 自审三查：spec/契约覆盖见 §8 逐条可指；占位符扫描见上；跨 task 类型一致性见 §1 Produces 逐字块。未亲自跑到结果的命令为零：§0 全部基线读数本节点亲跑，T1～T4 的预期红/绿均标注「预期」并附判定方法，不以预测冒充结果。
