# C12.3 实现计划：K3 缝 2 实装——deriveFlowPage 行为轴视图模型

> 状态：实现轮执行计划（plan 节点产出物；本计划不含任何留在 src 的代码——全部代码块已按「沙箱副本应用→跑红/跑绿→工作树零改动」探针验证，证据见台账）
> 输入：`docs/specs/2026-08-25-codegraph-viewer-two-axis-spec.md`（已批准）《测试决定（接缝清单）》缝 2；
> `docs/superpowers/specs/c12-contract.md`（已冻结）§2.1-3/-4/-6/-7、§2.4-28～36、§3-4/-5/-6、§4.2/§4.3、§8（修订 R1/R2）；
> `docs/superpowers/specs/c12-breakdown.md`（已拍板）K3 四段式、P1 链式基线约束、【释1-3】。
> 基线：分支 `cards/C12.3-charter` HEAD ee46218（含 K1 同刀迁移 + K2 缝 1 实装 + 契约修订 R1/R2），工作树干净；本卡继续在此分支工作（P1 约束 1，不切分支）。
> 仓界：只改 `graph/webui/src/app/codegraph/{flowpage.ts, flowpage.test.ts(新建), ticket0.passthrough.test.ts(仅删一行断言+头注释)}` 与台账；Go 侧零改动；handoff 侧三项交棒欠账不代做；**ticket0 整文件退役归 K6，本卡不删文件**。
> 台账：`docs/ledgers/2026-08-26-c12.3-plan-ledger.md`（plan 节点亲跑记录；实现轮执行者把每次红/绿原始输出**追加**到同一文件）。

## 0. 基线判据与执行边界（判据先在基线跑，plan 节点已在沙箱亲跑）

以下每一条都在基线 HEAD ee46218 亲跑核对过，原始输出见台账：

- `cd graph/webui && npm ci` 后 `npm run typecheck` 退出码 0；`npm test` → **Test Files 22 passed (22)，Tests 163 passed (163)**（K2 收口读数，对账一致）。验收命令形态 `npm test -- src/app/codegraph/<file>` 的 focused 过滤已在基线验证可用（对 ticket0 试跑 2 支全绿）。
- `flowpage.ts` 现状 = 29 行 Ticket 0 壳：`FlowPageInput` 两字段已按 §2.4-30 钉死（`baseline`、`entryNodeId`）；`FlowPageModel = { entryNodeId, degraded, passthrough: true }`；`deriveFlowPage` 仅做 degraded 判定。**缝地址与输入字段一字不动**（§2.4-29/-30），实装只发生在函数体与模型字段内部。
- **消费面 grep**（pattern `flowpage|deriveFlowPage|FlowPageInput|FlowPageModel|passthrough` over graph/webui/src）：生产与测试合计仅三个文件命中——壳自身 + `ticket0.passthrough.test.ts`（:7 import、:18/:21/:29 断言）+ `scopepage.test.ts:228/:237`（缝 1 自己的 passthrough **退役断言字符串**，与缝 2 无关）。组件层尚无消费方（K5 才接）。
- **K2 跨卡签名交接核对**：`scopepage.ts:33` 导出 `FALLBACK_BUCKET_KINDS = ['函数组', 'TypeScript 函数组'] as const`（C12.2 plan §1 冻结交接「两卡不得各写一份字面量」）——本卡 import 消费，不复制字面量。C12.2 plan §7 的「触达域散度归缝 2」裁决与本卡一致，且契约修订 R1（c12-contract §8）已把归属更正为缝 2 并落在我基线内。
- implements 方向亲证：`graph/codegraph/types.go:153-155` 注释原文「Implements 是接口满足边 [实现, 接口]」——pair[0]=实现符号、pair[1]=接口符号，join 规则据此写死并有专断言。
- FlowStep wire 形状亲证：`graph/codegraph/types.go:113-128`（ID/Order/Kind/To/Cond/Line/Then/Else/Body/Iface）；同文件 :116-117 原文「引用完整性的 Validate 执法随 flows 真数据同批开启」——**派生器必须容忍悬空子干引用并显式标注**（模型 `danglingChildRefs` 字段的依据）。
- 原型副本不在库：`prototypes/codegraph-two-axis/` 不在本工作树也不在 git（base/README.md:34 明言副本删除也不会丢）——入口族切分规则无原型代码可抄，按 spec 文本定规则并用夹具固化假设（breakdown K3 缺陷族表「跨平台假设」行的既定要求）；真实 162 入口分布复核归真机项（breakdown §四.6）。
- `tsconfig.app.json`：`strict` + `noUnusedLocals` + `noUnusedParameters` + `verbatimModuleSyntax` + `erasableSyntaxOnly`——本计划全部代码块按此书写（类型一律 `import type`；无 enum/namespace）。注意 **vitest 不查类型**（esbuild 转译），夹具类型错误只有 `tsc -b` 能抓（本轮探针实测抓到 TS2741×18，见下）。
- **纯派生层零 console 约定亲证**：besttree/domainpage/scopepage 零 console 命中；纯函数层诊断出口是模型显式字段。
- **沙箱探针验证（本节点亲跑，方法=C12.1 先例；工作树零改动）**：`cp -a graph/webui → /root/.handoff/tmp/bf95ec75/opencode/k3probe/webui`，在沙箱应用 T1/T2 全文 + ticket0 单行处置后：
  - 红灯相（壳 + 终版测试）：focused **16 failed (16)**（TypeError 群 + 键集失配，符合 §T1 红灯清单）；
  - 绿灯相：focused **16 passed (16)**、ticket0 **2 passed (2)**、typecheck 退出码 0、全量 **Test Files 23 passed (23)，Tests 179 passed (179)**（163 + 新增 16 = 179，对账分毫不差）；
  - 变异钻取：`degraded` 判定反转 → focused **5 failed | 10 passed**（转红名单见 T3），复原回绿；
  - 探针抓掉两处真缺陷（已修正进本计划）：①夹具领域字面量缺 `CgDomain.kind` 必填字段（TS2741×18，vitest 抓不到、tsc 才能抓）；②族期望值笔误（'flow cmd' 按「去末词」规则族 id 是 'flow'）。
  - 工作树核验：全程 `git status --short` 仅台账 untracked；`flowpage.ts` 经 `git checkout --` 保持 Ticket 0 壳。

### 对 breakdown 有界文件集的机械圈定补遗（自己重做，不照抄）

breakdown K3 有界文件集 = flowpage.ts(+test)。本节点重新机械圈定后**补一处**（编译/测试强制点，随 T2 同一提交处置）：

| 补遗文件 | 证据 |
|---|---|
| `graph/webui/src/app/codegraph/ticket0.passthrough.test.ts` | 删 `FlowPageModel.passthrough` 字段后：(a) :21 `expect(model.passthrough).toBe(true)` 运行时必红；(b) `model.passthrough` 属性在类型上消失，`tsc -b` 必报 TS2339。最小处置＝**只删这一行断言**；其 :20/:29 两支 degraded **值级**断言原样保留至 K6 整文件退役（覆盖平移不丢失，逐支对照表见 T2 步骤 2）。整文件删除是 K6 退役集动作（breakdown K6 有界文件集明列），本卡不越权。 |

除此之外全仓无第四处引用（grep 见上）。breakdown 清单其余部分复核无误。

### 协调者更正留痕

plan 节点首轮曾在工作树 `rm ticket0.passthrough.test.ts` 做探针，被协调者拒绝（两条理由：plan 不改代码；删除归 K6 且覆盖平移未证）。据此改为：沙箱副本验证（C12.1 先例）+ 本计划的值级断言对照表 + ticket0 最小行级处置。另登记：ticket0 里缝 1 的 `model.scopeId === null` 值级断言在新套件无后继一事属 C12.2 已立案缺口，不在本卡范围、本卡不代做，K6 收口时须一并处理。

### 明确不触碰清单（越界即错）

- `scopepage.ts` 及其测试（缝 1 已实装，不重做不改；`FALLBACK_BUCKET_KINDS` 只读 import）。
- `domainpage.ts`、`besttree.ts` 生产代码与其测试（§2.5-37/-38 退役与吸收归 K6）。
- 一切组件与装配层（`CodegraphPage.tsx`、`Best*.tsx`、K5 新组件）——本卡产出模型，不接线。
- Go 侧全部（`graph/codegraph/**`、`graph/cli/**`）：零改动，`go test` 不在本卡判据内。
- 不触 `internal/agentd`、`codegraph/domains/*`、扫描器、宿主传参；charter 根无项目图，不创建 `codegraph/diffs/*.json`。

## 1. 跨 task 接口、依赖与冻结模型

```text
T1（红灯铺设：flowpage.test.ts 全量锁缝断言写完跑红）
   └─> T2（壳内最小实现 + ticket0 删一行 → focused 绿 → 提交 A）
          └─> T3（变异复验 + 全量回归 + 台账收口 → 提交 B）
```

T1 先行点亮全部锁缝断言（对壳 16 支全红——沙箱已亲证）；T2 消费红灯清单做最小实现；T3 只做变异钻取、回归与提交，不再改产品代码。

**最薄路径条自查**：缝今天已可调用（Ticket 0 壳接线真实），测试写下去即红（回声模型缺全部新字段，沙箱红灯相 16 failed 亲证）——属于「写下去就会红的已亮路径」，无需前置竖切卡；红绿模板直接套在 T1/T2 上。

### 缝表（spec《测试决定》接缝清单 × 本卡）

| 缝 | 入口符号 | 缝级断言落点 |
|---|---|---|
| 缝 2：行为轴视图模型派生器 | `deriveFlowPage(input: FlowPageInput): FlowPageModel` | flowpage.test.ts 全部 16 支（唯一入口，逐支经此符号）；ticket0 两支 degraded 值级用例保留至 K6 双保险（contract §4.2 取代关系见 T2 对照表） |

spec 缝 2 覆盖清单六项全部落本卡：入口归属三态、入口注册散度、入口族分组、触达域散度（R1 归缝 2）、递归下钻的下层域入口解析（`targetIsEntry`/`to` + 实现起点）、流程图降级形态的字段与降级标记。

### Produces：冻结模型全量签名（T2 落地，供 K5/K6 plan 逐字对表）

```ts
// —— 既有导出原样保留（缝地址与输入字段不可变，§2.4-29/-30）——
export interface FlowPageInput {
  baseline: CgGraph
  entryNodeId: string
}

// —— 模块私有常量（阈值本地性纪律：不导出可变配置）——
const FLOW_STEP_KINDS = ['call', 'branch', 'loop', 'return'] as const satisfies readonly CgFlowStepKind[]
const HTTP_FAMILY_PATTERN = /^(?:GET|POST|PUT|DELETE|PATCH|HEAD|OPTIONS)\s+\/(.*)$/

// —— 新增模型类型（FlowPageModel 重写，passthrough 字段删除）——

/** 入口归属三态（§2.4-32）：单值 / 多值（全部候选升序）/ 判不出＝无行为。 */
export type EntryOwnership =
  | { state: 'single'; domainId: string }
  | { state: 'multi'; candidates: string[] }
  | { state: 'none' }

export interface RegistrationDispersion {
  domainId: string
  entries: number   // 归属该子系统的入口总数（仅单值归属计入，全图口径）
  files: number     // 这些入口注册文件的去重数
  concentrated: boolean // files===1 ∧ entries>3（§2.4-33 字面边界）
}

export interface InterfaceImplementation {
  nodeId: string    // 实现符号节点 id
  name: string
  entryNodeId: string // 该实现的流程图起点＝实现容器内的程序入口；无则空串如实标注
}

export interface FlowStepView {
  id: string
  order: number
  kind: CgFlowStepKind   // wire 原样透传；词表外值见 unknownKind
  line: number
  to?: string; cond?: string; then?: string[]; else?: string[]; body?: string[]
  // ↑ wire 可选字段缺席即键缺席（不补零值），透传不重排
  targetName: string | null   // to 缺席或悬空为 null
  targetDomain: string        // 目标顶层子系统 id（baseline.domains 口径）；未知空串
  targetIsEntry: boolean      // 紫框 ▸ 可递归下钻的数据依据
  iface?: boolean             // wire 透传
  implementations: InterfaceImplementation[]  // iface=true 时为 implements join；否则恒 []
  unknownKind: boolean        // 词表外 kind 显式标注
}

export interface AgentCallChain {
  nodeIds: string[]   // 机械可达 BFS 序（含入口自身，确定性排序）；仅供 agent tab
  sequenced: false    // 类型级恒 false：无次序语义（§2.4-31 如实标注）
  conditional: false  // 类型级恒 false：无分支语义
}

export interface EntryFamily {
  familyId: string    // cli:<去末词命令族> / http:</路径前两段>
  kind: 'cli' | 'http'
  label: string       // 'CLI <族>' / 'HTTP <路径>'
  members: number     // 全图同族入口数
  reachDomains: number // 触达域散度（R1）：族内成员可达顶层子系统去重数
}

/** 缝 2 输出：顶层九键恒定。 */
export interface FlowPageModel {
  entryNodeId: string
  entryFound: boolean                  // 幽灵入口显式降级
  degraded: boolean                    // §2.4-31：flows 键缺席或该入口缺席
  ownership: EntryOwnership
  registrationDispersion: RegistrationDispersion | null  // multi/none 时 null
  family: EntryFamily | null           // 幽灵入口 null
  steps: FlowStepView[]                // order 升序稳定排序；degraded 恒 []
  callChain: AgentCallChain            // 恒携带；序列永不混入 steps
  danglingChildRefs: string[]          // 子干悬空引用去重升序
}
```

`FlowPageModel` 顶层九键恒为 `entryNodeId, entryFound, degraded, ownership, registrationDispersion, family, steps, callChain, danglingChildRefs`（冻结断言见 T1「九键冻结」支）。模块另导出全部上述类型；两个常量为模块私有（阈值本地性，§2.4-34 族规则不外泄）。

**跨卡签名交接**：

- **K5（流程图页面）**：唯一数据源即上述 `FlowPageModel`。§2.4-35 形态的数据供给逐项——矩形左色条←`steps[].targetDomain`；菱形/卫语句侧甩/子干缩进←`kind`+`then/else/body`（id 引用在 `steps` 内自解析）；紫框 ▸ 与下钻回调参数←`targetIsEntry=true` 的步骤取其 `to` 再次调用本缝；双线框+右栏实现清单+换图起点←`iface`+`implementations[].entryNodeId`（空串=无实现记录，渲染「无实现记录」不得渲染空面板）；蛇形折列坐标是画布关注点归 K5，本缝不输出坐标；degraded 空态与「调用链（给 agent）」tab←`degraded`+`callChain`（tab 必须显示「无次序无分支」标注——`sequenced/conditional` 恒 false 是数据面的同一承诺）；畸形数据标注←`danglingChildRefs`；泳道标题素材←`family.label`。
- **K6（退役收口）**：ticket0 整文件退役时的覆盖后继即本卡 flowpage.test.ts（对照表见 T2 步骤 2）；`rg "passthrough"` 收口断言注意两处非符号残留——`scopepage.test.ts:228/:237`（缝 1 退役断言的命名）与 ticket0 文件名本身，K6 以符号级检查为准或调整模式。
- **K4（结构轴页面）**：无直接接口；channel 分组的数据位已由 K2 的 `ScopeEntryRef.channel` 透传提供（含缺席区分），【释2】「通道未标注」单桶是 K4 渲染语义；行为轴按 §2.4-34 从名字算族，不用 channel。

## 2. T1｜红灯铺设（锁缝断言先行，只写测试）

### 文件集（本 task 只动测试）

- `graph/webui/src/app/codegraph/flowpage.test.ts`（新建）

**注意**：vitest 不做类型检查，T1 阶段对尚不存在的模型字段的断言可以运行并按预期红；typecheck 留到 T2 才有意义。夹具里的领域字面量必须带 `kind` 字段（`CgDomain.kind` 必填——探针 TS2741×18 的教训）；边界期望值（4/3、36/12、reachDomains 4）全部硬编码字面量，不从模块常量推导。

### Consumes / Produces

Consumes：§1 冻结模型签名；Ticket 0 壳现状（红的对象）；vitest harness 形态（照抄 scopepage.test.ts：顶部 `import { describe, expect, it } from 'vitest'` + 类型 `import type` + 顶层 const 夹具工厂）。

Produces：下述完整测试文件（红 → T2 后转绿的锁缝断言）。

### 步骤

1. 新建 `graph/webui/src/app/codegraph/flowpage.test.ts`，全文如下（与沙箱绿灯相逐字一致）。七个夹具世界，每个数字手工可核；所有断言只经 `deriveFlowPage` 一个入口构造：

```ts
import { describe, expect, it } from 'vitest'
import type { CgFlowStep, CgGraph } from '../../api/types'
import { deriveFlowPage } from './flowpage'

// —— 夹具纪律（沿 C12.2 同款）：边界期望值全部硬编码（4/3、36/12、reachDomains 4），
// 不从模块常量推导；所有断言只走 deriveFlowPage 这一个缝入口。——
//
// 缝 2 无组织树输入（§2.4-30 两字段钉死），领域归属一律走 baseline.domains 的
// parent 链 + containers[].domain；夹具用平铺领域（无 parent）即顶层自指。

const meta = { project: 'k3', branch: 'main', commit: 'c', scannedAt: 'now', generator: 'test' }

function blankGraph(): CgGraph {
  return { meta, containers: {}, nodes: {}, edges: [] }
}

// W1 多值世界：e 直连两个异域合格目标（biz/other），外加一个更近的兜底桶噪声
// （noisy 域的函数组容器）——候选恰为 ['biz','other']，噪声不入候选。
function multiWorld(): CgGraph {
  return {
    ...blankGraph(),
    domains: { home: { label: '老巢', kind: 'boundary' }, biz: { label: '业务甲', kind: 'logic' }, other: { label: '业务乙', kind: 'logic' }, noisy: { label: '噪声域', kind: 'logic' } },
    containers: {
      c_home: { label: '入口', kind: '入口', domain: 'home' },
      c_b: { label: 'Biz', kind: '类型方法', domain: 'biz' },
      c_o: { label: 'Other', kind: '类型方法', domain: 'other' },
      c_fb: { label: '杂活', kind: '函数组', domain: 'noisy' },
    },
    nodes: {
      e: { kind: 'entry', container: 'c_home', name: 'dual cmd', file: 'r/e.go', line: 1 },
      b1: { kind: 'func', container: 'c_b', name: 'Biz.Do', file: 'b/x.go', line: 1 },
      o1: { kind: 'func', container: 'c_o', name: 'Other.Do', file: 'o/x.go', line: 1 },
      nz: { kind: 'func', container: 'c_fb', name: 'utilNoise', file: 'n/u.go', line: 1 },
    },
    edges: [
      ['e', 'nz'],
      ['e', 'b1'],
      ['e', 'o1'],
    ],
  }
}

// W2 单值世界：e→helper（同域中转）→svc（biz）；e 还有直达的兜底桶噪声 nz
// （距离更近）和一条悬空边 helper→ghost。最近合格跨域层＝biz → 单值，
// 同时证明：同域中转不计、兜底桶更近也排除、悬空边忽略。
function singleWorld(edgeOrder: 'normal' | 'reversed'): CgGraph {
  const edges: CgGraph['edges'] = [
    ['e', 'nz'],
    ['e', 'helper'],
    ['helper', 'svc'],
    ['helper', 'ghost'],
  ]
  return {
    ...blankGraph(),
    domains: { home: { label: '老巢', kind: 'boundary' }, biz: { label: '业务甲', kind: 'logic' }, noisy: { label: '噪声域', kind: 'logic' } },
    containers: {
      c_home: { label: '入口', kind: '入口', domain: 'home' },
      c_help: { label: 'helpers', kind: '函数组', domain: 'home' },
      c_s: { label: 'Svc', kind: '类型方法', domain: 'biz' },
      c_fb: { label: '杂活', kind: '函数组', domain: 'noisy' },
    },
    nodes: {
      e: { kind: 'entry', container: 'c_home', name: 'solo cmd', file: 'r/e.go', line: 1 },
      helper: { kind: 'func', container: 'c_help', name: 'helperFn', file: 'h/h.go', line: 1 },
      svc: { kind: 'func', container: 'c_s', name: 'Svc.Do', file: 'b/s.go', line: 1 },
      nz: { kind: 'func', container: 'c_fb', name: 'utilNoise', file: 'n/u.go', line: 1 },
    },
    edges: edgeOrder === 'normal' ? edges : [...edges].reverse(),
  }
}

// W3 Cobra 分组命令世界：无出边入口 → 「无行为」，族照常从名字解析。
function cobraWorld(): CgGraph {
  return {
    ...blankGraph(),
    domains: { home: { label: '老巢', kind: 'boundary' } },
    containers: { c_home: { label: '入口', kind: '入口', domain: 'home' } },
    nodes: { e_cg: { kind: 'entry', container: 'c_home', name: 'handoff service', file: 'r/cg.go', line: 1 } },
    edges: [],
  }
}

// W4 散度边界世界：四个被归属子系统各踩判据一侧——red=1 文件 4 入口（红）、
// three=1 文件 3 入口（不红）、spread=36 入口 12 文件（不红）、twofile=4 入口
// 2 文件（不红）。全部入口物理上挤在同一个入口容器，归属只由调用边决定。
function dispWorld(): CgGraph {
  const nodes: CgGraph['nodes'] = {}
  const edges: CgGraph['edges'] = []
  const containers: CgGraph['containers'] = { c_home: { label: '入口', kind: '入口', domain: 'home' } }
  const domains: NonNullable<CgGraph['domains']> = { home: { label: '老巢', kind: 'boundary' } }
  const groups: Array<{ stem: string; count: number; fileOf: (i: number) => string }> = [
    { stem: 'red act', count: 4, fileOf: () => 'reg/red.go' },
    { stem: 'three act', count: 3, fileOf: () => 'reg/three.go' },
    { stem: 'spread act', count: 36, fileOf: (i) => `reg/s${String(Math.ceil(i / 3)).padStart(2, '0')}.go` },
    { stem: 'twofile act', count: 4, fileOf: (i) => (i <= 2 ? 'reg/t_a.go' : 'reg/t_b.go') },
  ]
  for (const g of groups) {
    const top = g.stem.split(' ')[0]
    domains[top] = { label: g.stem, kind: 'logic' }
    containers[`c_${top}`] = { label: g.stem, kind: '类型方法', domain: top }
    for (let i = 1; i <= g.count; i += 1) {
      const eid = `${top}_e${i}`
      const fid = `${top}_f${i}`
      nodes[eid] = { kind: 'entry', container: 'c_home', name: `${g.stem} ${i}`, file: g.fileOf(i), line: i }
      nodes[fid] = { kind: 'func', container: `c_${top}`, name: `${top}.Do${i}`, file: `x/${top}${i}.go`, line: i }
      edges.push([eid, fid])
    }
  }
  return { ...blankGraph(), domains, containers, nodes, edges }
}

// W5 族世界：同名族两成员分处不同容器、不同领域——分组与容器归属无关（验收 4）；
// 触达域散度＝族成员可达顶层去重数（验收 5）：dep run 族 {ha,hc,hb,hd}=4，solo 族=1。
function familyWorld(): CgGraph {
  return {
    ...blankGraph(),
    domains: { ha: { label: '甲', kind: 'logic' }, hb: { label: '乙', kind: 'logic' }, hc: { label: '丙', kind: 'logic' }, hd: { label: '丁', kind: 'logic' } },
    containers: {
      c_a: { label: '容器甲', kind: '类型方法', domain: 'ha' },
      c_b: { label: '容器乙', kind: '入口', domain: 'hb' },
      c_c: { label: '容器丙', kind: '类型方法', domain: 'hc' },
      c_d: { label: '容器丁', kind: '类型方法', domain: 'hd' },
    },
    nodes: {
      x1: { kind: 'entry', container: 'c_a', name: 'dep run one', file: 'p/a.go', line: 1 },
      x2: { kind: 'entry', container: 'c_b', name: 'dep run two', file: 'q/b.go', line: 2 },
      y: { kind: 'entry', container: 'c_a', name: 'solo', file: 'p/y.go', line: 3 },
      t1: { kind: 'func', container: 'c_c', name: 'C.Do', file: 'c/c.go', line: 1 },
      t2: { kind: 'func', container: 'c_d', name: 'D.Do', file: 'd/d.go', line: 2 },
    },
    edges: [
      ['x1', 't1'],
      ['x2', 't2'],
    ],
  }
}

// W6 主干世界：六步覆盖四种合法 kind + 词表外 kind、子干悬空引用、iface join
// （含 flows 里塞实现数组的 §2.1-4 反面夹具）、下层入口目标（紫框 ▸ 数据）。
function flowWorld(): CgGraph {
  const steps: CgFlowStep[] = [
    { id: 's_branch', order: 1, kind: 'branch', line: 10, cond: 'err != nil', then: ['s_call', 's_ghost'], else: ['s_ret'] },
    { id: 's_call', order: 2, kind: 'call', line: 20, to: 'sub_e' },
    { id: 's_ret', order: 3, kind: 'return', line: 30 },
    { id: 's_loop', order: 4, kind: 'loop', line: 40, cond: 'range items', body: ['s_call'] },
    // implementations 不是 wire 字段（§2.1-4 禁止复制）；这里经 as-cast 塞入伪造
    // 实现数组模拟违约数据（运行期 wire 无校验，contract §3-4），断言 join 只认 implements 段。
    {
      id: 's_iface',
      order: 5,
      kind: 'call',
      line: 50,
      to: 'iface.X',
      iface: true,
      implementations: [{ nodeId: 'FAKE', name: '伪造', entryNodeId: '' }],
    } as CgFlowStep,
    // 词表外 kind 同样经 as-cast 模拟未校验 wire。
    { id: 's_bad', order: 6, kind: 'jump' as unknown as CgFlowStep['kind'], line: 60, to: 't' },
  ]
  return {
    ...blankGraph(),
    domains: { home: { label: '老巢', kind: 'boundary' }, biz: { label: '业务', kind: 'logic' } },
    containers: {
      c_home: { label: '入口', kind: '入口', domain: 'home' },
      c_t: { label: 'T', kind: '类型方法', domain: 'biz' },
      c_ia: { label: 'AdapterA', kind: '类型方法', domain: 'biz' },
      c_ib: { label: 'AdapterB', kind: '类型方法', domain: 'biz' },
      c_sub: { label: '下层入口', kind: '入口', domain: 'biz' },
    },
    nodes: {
      e: { kind: 'entry', container: 'c_home', name: 'flow cmd', file: 'r/e.go', line: 1 },
      t: { kind: 'func', container: 'c_t', name: 'T.Do', file: 'b/t.go', line: 5 },
      sub_e: { kind: 'entry', container: 'c_sub', name: 'sub cmd', file: 'b/s.go', line: 2 },
      ia: { kind: 'func', container: 'c_ia', name: 'A.Start', file: 'b/ia.go', line: 3 },
      ib: { kind: 'func', container: 'c_ib', name: 'B.Start', file: 'b/ib.go', line: 4 },
      ea: { kind: 'entry', container: 'c_ia', name: 'A serve', file: 'b/ia.go', line: 1 },
    },
    edges: [['e', 't']],
    implements: [
      ['ia', 'iface.X'],
      ['ib', 'iface.X'],
      ['ix', 'iface.Y'],
    ],
    flows: { e: { steps } },
  }
}

// W7 降级双向基座：flows 缺席 / 键在但该入口缺席 / 命中三态共用。
function degradeBase(flows?: CgGraph['flows']): CgGraph {
  return {
    ...blankGraph(),
    domains: { home: { label: '老巢', kind: 'boundary' }, biz: { label: '业务', kind: 'logic' } },
    containers: {
      c_home: { label: '入口', kind: '入口', domain: 'home' },
      c_b: { label: 'B', kind: '类型方法', domain: 'biz' },
    },
    nodes: {
      n_entry: { kind: 'entry', container: 'c_home', name: 'n cmd', file: 'r/n.go', line: 1 },
      f1: { kind: 'func', container: 'c_b', name: 'B.Do', file: 'b/f.go', line: 2 },
    },
    edges: [['n_entry', 'f1']],
    ...(flows === undefined ? {} : { flows }),
  }
}

describe('C12.3 缝 2：入口归属三态（§2.4-32）', () => {
  it('多值：同一最近前沿的两个异域目标全部升序呈现，兜底桶噪声不入候选', () => {
    const m = deriveFlowPage({ baseline: multiWorld(), entryNodeId: 'e' })
    expect(m.ownership).toEqual({ state: 'multi', candidates: ['biz', 'other'] })
  })

  it('单值：最近合格跨域层判单值；更近的兜底桶与同域中转都不改判；悬空边忽略', () => {
    const m = deriveFlowPage({ baseline: singleWorld('normal'), entryNodeId: 'e' })
    expect(m.ownership).toEqual({ state: 'single', domainId: 'biz' })
  })

  it('稳定性：edges 原序扰动不改变归属结果与可达序列', () => {
    const a = deriveFlowPage({ baseline: singleWorld('normal'), entryNodeId: 'e' })
    const b = deriveFlowPage({ baseline: singleWorld('reversed'), entryNodeId: 'e' })
    expect(b.ownership).toEqual(a.ownership)
    expect(b.callChain.nodeIds).toEqual(a.callChain.nodeIds)
  })

  it('领域链上溯：归属比较在顶层子系统层面进行（同树跨层不算跨域）', () => {
    const w: CgGraph = {
      ...blankGraph(),
      domains: {
        root_a: { label: '顶甲', kind: 'boundary' },
        mid_a: { label: '中甲', kind: 'logic', parent: 'root_a' },
        leaf_a: { label: '叶甲', kind: 'logic', parent: 'mid_a' },
        root_b: { label: '顶乙', kind: 'boundary' },
      },
      containers: {
        c_leaf: { label: '入口容器', kind: '入口', domain: 'leaf_a' },
        c_mid_fn: { label: '中域函数', kind: '类型方法', domain: 'mid_a' },
        c_b: { label: '乙容器', kind: '类型方法', domain: 'root_b' },
      },
      nodes: {
        e: { kind: 'entry', container: 'c_leaf', name: 'deep cmd', file: 'r/d.go', line: 1 },
        mf: { kind: 'func', container: 'c_mid_fn', name: 'Mid.Do', file: 'm/m.go', line: 1 },
        bf: { kind: 'func', container: 'c_b', name: 'B.Do', file: 'b/b.go', line: 1 },
      },
      edges: [
        ['e', 'mf'],
        ['mf', 'bf'],
      ],
    }
    const m = deriveFlowPage({ baseline: w, entryNodeId: 'e' })
    expect(m.ownership).toEqual({ state: 'single', domainId: 'root_b' })
  })

  it('判不出：Cobra 式无出边入口标「无行为」，族照常解析为 CLI handoff', () => {
    const m = deriveFlowPage({ baseline: cobraWorld(), entryNodeId: 'e_cg' })
    expect(m.ownership).toEqual({ state: 'none' })
    expect(m.registrationDispersion).toBeNull()
    expect(m.family).toEqual({ familyId: 'handoff', kind: 'cli', label: 'CLI handoff', members: 1, reachDomains: 1 })
  })
})

describe('C12.3 缝 2：注册散度边界（§2.4-33）', () => {
  it('四侧边界：1 文件 4 入口红；1 文件 3 入口不红；36 入口 12 文件不红；4 入口 2 文件不红', () => {
    const w = dispWorld()
    const q = (id: string) => deriveFlowPage({ baseline: w, entryNodeId: id })
    expect(q('red_e1').ownership).toEqual({ state: 'single', domainId: 'red' })
    expect(q('red_e1').registrationDispersion).toEqual({ domainId: 'red', entries: 4, files: 1, concentrated: true })
    expect(q('three_e1').registrationDispersion).toEqual({ domainId: 'three', entries: 3, files: 1, concentrated: false })
    expect(q('spread_e1').registrationDispersion).toEqual({ domainId: 'spread', entries: 36, files: 12, concentrated: false })
    expect(q('twofile_e4').registrationDispersion).toEqual({ domainId: 'twofile', entries: 4, files: 2, concentrated: false })
  })
})

describe('C12.3 缝 2：族分组与触达域散度（§2.4-34 + R1）', () => {
  it('族从名字算出、跨容器跨域成员照常聚合；触达域散度数值化（4 与 1）', () => {
    const w = familyWorld()
    const dep = deriveFlowPage({ baseline: w, entryNodeId: 'x1' }).family
    expect(dep).toEqual({ familyId: 'dep run', kind: 'cli', label: 'CLI dep run', members: 2, reachDomains: 4 })
    expect(deriveFlowPage({ baseline: w, entryNodeId: 'y' }).family).toEqual({
      familyId: 'solo',
      kind: 'cli',
      label: 'CLI solo',
      members: 1,
      reachDomains: 1,
    })
  })

  it('HTTP 形态按路径前两段成族（方法词剥离）', () => {
    const w: CgGraph = {
      ...familyWorld(),
      nodes: {
        h1: { kind: 'entry', container: 'c_a', name: 'GET /api/tasks/{id}/branches', file: 'p/h.go', line: 1 },
        h2: { kind: 'entry', container: 'c_b', name: 'POST /api/tasks', file: 'q/h.go', line: 2 },
        lone: { kind: 'entry', container: 'c_a', name: 'GET /machines', file: 'p/m.go', line: 3 },
      },
      edges: [],
    }
    expect(deriveFlowPage({ baseline: w, entryNodeId: 'h1' }).family).toMatchObject({
      familyId: '/api/tasks',
      kind: 'http',
      label: 'HTTP /api/tasks',
      members: 2,
    })
    expect(deriveFlowPage({ baseline: w, entryNodeId: 'lone' }).family).toMatchObject({
      familyId: '/machines',
      kind: 'http',
      members: 1,
    })
  })
})

describe('C12.3 缝 2：流程主干模型（§2.4-31/-35 数据面）', () => {
  it('steps 按 order 升序稳定排序；子干引用完整透传；悬空引用显式收集', () => {
    const m = deriveFlowPage({ baseline: flowWorld(), entryNodeId: 'e' })
    expect(m.degraded).toBe(false)
    expect(m.steps.map((s) => s.id)).toEqual(['s_branch', 's_call', 's_ret', 's_loop', 's_iface', 's_bad'])
    const branch = m.steps.find((s) => s.id === 's_branch')
    expect(branch?.cond).toBe('err != nil')
    expect(branch?.then).toEqual(['s_call', 's_ghost'])
    expect(branch?.else).toEqual(['s_ret'])
    expect(m.steps.find((s) => s.id === 's_loop')?.body).toEqual(['s_call'])
    expect(m.danglingChildRefs).toEqual(['s_ghost'])
  })

  it('步骤派生读数：目标名/顶层域/紫框▸标记逐字段可断言；词表外 kind 显式 unknownKind', () => {
    const m = deriveFlowPage({ baseline: flowWorld(), entryNodeId: 'e' })
    const call = m.steps.find((s) => s.id === 's_call')
    expect(call?.targetName).toBe('sub cmd')
    expect(call?.targetDomain).toBe('biz')
    expect(call?.targetIsEntry).toBe(true)
    const bad = m.steps.find((s) => s.id === 's_bad')
    expect(bad?.targetName).toBe('T.Do')
    expect(bad?.targetIsEntry).toBe(false)
    expect(bad?.unknownKind).toBe(true)
    expect(m.steps.filter((s) => !s.unknownKind)).toHaveLength(5)
    const ret = m.steps.find((s) => s.id === 's_ret')
    expect(Object.prototype.hasOwnProperty.call(ret, 'to')).toBe(false)
    expect(ret?.targetName).toBeNull()
    expect(ret?.targetDomain).toBe('')
  })

  it('iface 实现清单＝implements join（[实现,接口] 方向）；flows 里塞的实现数组被忽略（§2.1-4 反面）', () => {
    const m = deriveFlowPage({ baseline: flowWorld(), entryNodeId: 'e' })
    const iface = m.steps.find((s) => s.id === 's_iface')
    expect(iface?.implementations).toEqual([
      { nodeId: 'ia', name: 'A.Start', entryNodeId: 'ea' },
      { nodeId: 'ib', name: 'B.Start', entryNodeId: '' },
    ])
    const plain = m.steps.find((s) => s.id === 's_branch')
    expect(Object.prototype.hasOwnProperty.call(plain, 'iface')).toBe(false)
    expect(plain?.implementations).toEqual([])
  })

  it('机械序列恒住 callChain 不进主干；模型顶层九键冻结；散度与族在命中态照常输出', () => {
    const m = deriveFlowPage({ baseline: flowWorld(), entryNodeId: 'e' })
    expect(m.callChain).toEqual({ nodeIds: ['e', 't'], sequenced: false, conditional: false })
    expect(Object.keys(m).sort()).toEqual([
      'callChain',
      'danglingChildRefs',
      'degraded',
      'entryFound',
      'entryNodeId',
      'family',
      'ownership',
      'registrationDispersion',
      'steps',
    ])
    expect(m.ownership).toEqual({ state: 'single', domainId: 'biz' })
    expect(m.registrationDispersion).toEqual({ domainId: 'biz', entries: 1, files: 1, concentrated: false })
    expect(m.family).toEqual({ familyId: 'flow', kind: 'cli', label: 'CLI flow', members: 1, reachDomains: 2 })
  })
})

describe('C12.3 缝 2：降级双向与幽灵入口（§2.4-31、ticket0 值级断言的后继）', () => {
  it('flows 键整体缺席 → degraded=true；序列只进 callChain、steps 主干恒空（冒充被结构锁死）', () => {
    const m = deriveFlowPage({ baseline: degradeBase(), entryNodeId: 'n_entry' })
    expect(m.entryNodeId).toBe('n_entry')
    expect(m.degraded).toBe(true)
    expect(m.entryFound).toBe(true)
    expect(m.steps).toEqual([])
    expect(m.callChain).toEqual({ nodeIds: ['n_entry', 'f1'], sequenced: false, conditional: false })
    expect(m.danglingChildRefs).toEqual([])
  })

  it('flows 在但该入口缺席 → degraded=true（双向第二侧）', () => {
    const w = degradeBase({
      other: { steps: [{ id: 'z1', order: 1, kind: 'call', line: 1, to: 'f1' }] },
    })
    const m = deriveFlowPage({ baseline: w, entryNodeId: 'n_entry' })
    expect(m.degraded).toBe(true)
    expect(m.steps).toEqual([])
    expect(m.ownership).toEqual({ state: 'single', domainId: 'biz' })
  })

  it('flows 命中该入口 → degraded=false（值级后继：ticket0 :29 断言正式化）', () => {
    const w = degradeBase({
      n_entry: {
        steps: [
          { id: 's1', order: 2, kind: 'call', line: 9, to: 'f1' },
          { id: 's2', order: 1, kind: 'branch', line: 8, cond: 'ok', then: ['s1'] },
        ],
      },
    })
    const m = deriveFlowPage({ baseline: w, entryNodeId: 'n_entry' })
    expect(m.degraded).toBe(false)
    expect(m.steps.map((s) => s.id)).toEqual(['s2', 's1'])
  })

  it('幽灵入口显式降级：entryFound=false、全家字段如实、不崩溃', () => {
    const m = deriveFlowPage({ baseline: blankGraph(), entryNodeId: 'ghost' })
    expect(m.entryFound).toBe(false)
    expect(m.degraded).toBe(true)
    expect(m.ownership).toEqual({ state: 'none' })
    expect(m.registrationDispersion).toBeNull()
    expect(m.family).toBeNull()
    expect(m.steps).toEqual([])
    expect(m.callChain).toEqual({ nodeIds: [], sequenced: false, conditional: false })
    expect(m.danglingChildRefs).toEqual([])
  })
})
```

2. **跑红**：

```bash
cd graph/webui && npm test -- src/app/codegraph/flowpage.test.ts
```

3. **红灯清单（预期 16 failed (16)，非零退出，逐支记录台账原文）**：回声壳模型缺全部新字段——取 `ownership/steps/callChain/family/registrationDispersion/entryFound/danglingChildRefs` 得 undefined（TypeError 或 toEqual 失配）、九键冻结断言失配（壳是 `{entryNodeId, degraded, passthrough}` 三键）、degraded 值断言部分失配。若出现与本清单无关的原始错误（语法错、夹具笔误），贴原文进台账，只修测试书写、不改判据。（沙箱红灯相已亲证 16 failed。）

### 测试范围声明

只跑上面一条 focused vitest 命令。全量 `npm test` 与 typecheck 归 T3；Go 侧不跑（零 Go 改动）。

## 3. T2｜壳内最小实现（缝地址不动，passthrough 同刀退役，ticket0 单行处置）

### 文件集

- `graph/webui/src/app/codegraph/flowpage.ts`（实装）
- `graph/webui/src/app/codegraph/ticket0.passthrough.test.ts`（删一行断言 + 头注释更新——§0 补遗的编译/测试强制点，与字段删除同一提交）

### Consumes / Produces

Consumes：T1 红灯清单；scopepage 只读导出 `FALLBACK_BUCKET_KINDS`（§0 核对的 K2 跨卡交接）；api/types 的 `CgGraph/CgFlowStep/CgFlowStepKind`。

Produces：§1 冻结块的全部导出（类型、`deriveFlowPage` 实装）。

### 步骤

1. 以下方全文**整体替换** `flowpage.ts`。文件头注释即职责+边界声明；导出符号逐一带 doc 注释；非显然逻辑带「为什么」注释（最近前沿归属的理由、邻接排序保确定性的理由、multi/none 不计散度的理由、名字形态识别器的假设固化、iface join 单一数据源、机械序列结构隔离）：

```ts
// flowpage —— C12 行为轴视图模型派生器（缝 2，c12-contract §2.4-28~36）。
//
// 职责：把行为轴的全部读数收敛进这一个纯函数——一条程序入口一张流程图（§2.4-30）：
// 流程主干步骤视图（wire 透传 + 目标派生读数，§2.4-35 的数据面）、入口归属三态
// （§2.4-32）、入口注册散度（§2.4-33）、入口族分组与触达域散度（§2.4-34 +
// 契约修订 R1 把触达域散度归缝 2）、接口实现的 implements join（§2.1-4，禁止在
// flows 里复制实现清单）、degraded 双向与「调用链（给 agent）」机械序列数据位
// （§2.4-31）。
//
// 边界：纯函数层——不请求网络、不访问 DOM、零 console；诊断一律走模型显式字段
// （degraded / entryFound / ownership='none' / unknownKind / danglingChildRefs /
// registrationDispersion=null），与 scopepage 同一约定。兜底桶词表常量复用缝 1
// 导出（两卡不得各写一份字面量，C12.2 plan §1 跨卡签名交接）。导出面仅供
// codegraph 应用模块内组件层消费，不导出到应用外（§2.4-28）。蛇形折列等画布
// 布局是消费侧关注点（K5），本模块不输出坐标。
//
// 判定口径（契约未钉、由本 plan 细化并经夹具钉死的部分）：
// - 归属＝从入口沿调用出边 BFS，「最近一层」上同时出现的全部合格跨域目标的
//   顶层子系统去重（spec 现状读数 122 单值/24 双值/2 三值的成因即同一最近前沿
//   有多个目标；若只看直连边，先调同域 handler 的 HTTP 入口会被整体误判成
//   无行为）。合格＝目标可解析到顶层子系统 ∧ 异于入口自身顶层 ∧ 目标容器
//   kind ∉ 兜底桶。邻接表按 id 排序构造，边序扰动不改结果。
// - 注册散度按「归属单值」在全图聚合入口（multi/none 不计入任何单一子系统，
//   不假装归属）；文件取入口节点的 file 字段；集中注册＝files===1 ∧
//   entries>3（§2.4-33 的字面边界）。
// - 族切分是名字形状识别器（非 wire 词表）：HTTP 形态＝「方法词+空格+/路径」
//   或裸 /路径，族 id＝路径前两段；其余按空白切分、去末段为 CLI 命令族。
//   该假设已由夹具固化；真实 162 入口分布的复核归真机项（breakdown §四.6）。
// - 触达域散度＝族内全部成员入口的机械可达顶层子系统去重数（含各自老家）。
// - 机械可达序列永远单独住在 callChain，其 sequenced/conditional 是类型级恒
//   false 的诚实标注——结构上不可能混入 steps 主干冒充流程图（§2.4-31）。
import type { CgFlowStepKind, CgGraph } from '../../api/types'
import { FALLBACK_BUCKET_KINDS } from './scopepage'

/** 流程步骤 kind 四值的运行时镜像（satisfies 与 api/types 的联合类型钉同步）。 */
const FLOW_STEP_KINDS = ['call', 'branch', 'loop', 'return'] as const satisfies readonly CgFlowStepKind[]

/**
 * HTTP 方法词识别——仅用于入口名的族形态识别（§2.4-34 从名字算族），
 * 不是 wire 受控词表；通道真数据到达后如需改按 channel 分组属契约增量。
 */
const HTTP_FAMILY_PATTERN = /^(?:GET|POST|PUT|DELETE|PATCH|HEAD|OPTIONS)\s+\/(.*)$/

export interface FlowPageInput {
  baseline: CgGraph
  entryNodeId: string
}

/** 入口归属三态（§2.4-32）：单值 / 多值（全部候选升序）/ 判不出＝无行为。 */
export type EntryOwnership =
  | { state: 'single'; domainId: string }
  | { state: 'multi'; candidates: string[] }
  | { state: 'none' }

/**
 * 入口注册散度（§2.4-33）：归属某子系统的全部入口（全图口径，不只当前入口）
 * 的注册文件分布。concentrated＝files===1 ∧ entries>3，判红依据由视图渲染。
 */
export interface RegistrationDispersion {
  domainId: string
  /** 归属该子系统的入口总数（仅单值归属计入）。 */
  entries: number
  /** 这些入口注册文件的去重数。 */
  files: number
  /** 集中注册判红（§2.4-33 字面边界：1 个文件且入口数 >3）。 */
  concentrated: boolean
}

/** 接口调用的一个实现：implements 段 join 结果（§2.1-4 单一数据源）。 */
export interface InterfaceImplementation {
  /** 实现符号节点 id。 */
  nodeId: string
  name: string
  /**
   * 该实现的流程图起点＝实现所在容器内的程序入口（spec：每个实现的入口即其
   * 流程图起点）；实现容器没有入口时空串如实标注，不猜。
   */
  entryNodeId: string
}

/** 流程步骤视图：wire 七类字段透传 + 派生读数。可选字段缺席即键缺席（不补零值）。 */
export interface FlowStepView {
  id: string
  order: number
  /** wire 原样透传；词表外值不崩溃，见 unknownKind。 */
  kind: CgFlowStepKind
  line: number
  /** call 步骤的目标节点/接口 id；wire 缺席即键缺席。 */
  to?: string
  /** branch/loop 的条件表达式；wire 缺席即键缺席。 */
  cond?: string
  /** 子干步骤 id 引用列，原样透传不重排；悬空引用另行汇总在模型 danglingChildRefs。 */
  then?: string[]
  else?: string[]
  body?: string[]
  /** 派生：目标节点名；to 缺席或悬空（不在 nodes 里）时为 null。 */
  targetName: string | null
  /** 派生：目标顶层子系统 id（baseline.domains 口径，左色条数据）；未知为空串。 */
  targetDomain: string
  /** 派生：目标是程序入口（紫框 ▸ 可递归下钻的数据依据）。 */
  targetIsEntry: boolean
  /** wire 原样透传；true 表示动态分派点。 */
  iface?: boolean
  /** iface=true 时为 implements join 结果（可能为空＝无实现记录）；否则恒 []。 */
  implementations: InterfaceImplementation[]
  /** 词表外 kind 显式标注（四值之外的数据不崩溃、不冒充合法步骤）。 */
  unknownKind: boolean
}

/** 「调用链（给 agent）」tab 的机械可达序列数据位（§2.4-31）。 */
export interface AgentCallChain {
  /** 从入口沿调用出边 BFS 的节点 id 序列（含入口自身；邻接按 id 排序保证确定性）。 */
  nodeIds: string[]
  /** 类型级恒 false：机械序列没有次序语义，视图必须如实标注「无次序」。 */
  sequenced: false
  /** 类型级恒 false：机械序列没有分支语义。 */
  conditional: false
}

/** 入口族（§2.4-34）：从节点名算出，不依赖入口容器的服务领域拆分。 */
export interface EntryFamily {
  familyId: string
  kind: 'cli' | 'http'
  /** 展示名（CLI <命令族> / HTTP <资源路径前缀>）。 */
  label: string
  /** 全图同族入口数。 */
  members: number
  /** 触达域散度（契约修订 R1 归缝 2）：族内全部成员可达顶层子系统的去重数。 */
  reachDomains: number
}

/** 缝 2 输出：顶层九键恒定；degraded 双向语义见 §2.4-31。 */
export interface FlowPageModel {
  entryNodeId: string
  /** 入口 id 在 baseline.nodes 中不存在时为 false（幽灵入口显式降级，不静默空页）。 */
  entryFound: boolean
  /** true＝baseline.flows 缺席或该入口无流程数据；界面按降级空态渲染。 */
  degraded: boolean
  ownership: EntryOwnership
  /** 归属非单值（multi/none）时为 null——没有唯一归属就不发散发度读数。 */
  registrationDispersion: RegistrationDispersion | null
  /** 幽灵入口为 null。 */
  family: EntryFamily | null
  /** 按 order 升序（同 order 按 id 稳定排序）；degraded 时恒 []。 */
  steps: FlowStepView[]
  /** 恒携带（与 degraded 无关）：机械可达序列只住这里，永不进 steps 主干。 */
  callChain: AgentCallChain
  /** 子干引用指向不存在步骤 id 的去重清单（升序）——引用完整性执法随扫描侧开启前的诚实标注。 */
  danglingChildRefs: string[]
}

/**
 * 缝 2 主入口（§2.4-29 地址冻结）：组件层对一条程序入口调用一次；输入只读，
 * 返回可渲染模型。flows 缺该入口输出显式 degraded（Ticket 0 判定位的正式化），
 * 机械可达序列单独住 callChain 并带恒假的次序/分支标注——绝不冒充流程图。
 */
export function deriveFlowPage(input: FlowPageInput): FlowPageModel {
  const { baseline } = input
  const nodesById = baseline.nodes
  const containersDef = baseline.containers
  const domains = baseline.domains ?? {}

  // 邻接表：去重 + 按 id 排序——后续一切遍历（BFS 层序、可达序、归属候选序）
  // 都由此获得确定性，edges 原序扰动不改任何输出。
  const adjacency = new Map<string, string[]>()
  for (const [from, to] of baseline.edges) {
    const list = adjacency.get(from)
    if (list) list.push(to)
    else adjacency.set(from, [to])
  }
  for (const [from, list] of adjacency) adjacency.set(from, [...new Set(list)].sort((a, b) => a.localeCompare(b)))

  const isFallbackKind = (kind: string) => (FALLBACK_BUCKET_KINDS as readonly string[]).includes(kind)

  // 顶层子系统解析：node → container.domain（叶）→ domains.parent 链上溯。
  // 断链/环安全终止返回空串；空串＝不可归属，归属判据里直接跳过（数据缺陷
  // 归扫描侧执法，与 scopepage 悬空端点整条忽略同一先例）。
  const topOfNode = (nodeId: string): string => {
    const node = nodesById[nodeId]
    if (!node) return ''
    const leaf = containersDef[node.container]?.domain ?? ''
    if (!leaf) return ''
    let current = leaf
    const seen = new Set<string>()
    while (current && domains[current] !== undefined && !seen.has(current)) {
      seen.add(current)
      const parent = domains[current]?.parent ?? ''
      if (!parent) return current
      current = parent
    }
    return ''
  }

  const reachableFrom = (startId: string): { order: string[]; depth: Map<string, number> } => {
    const order: string[] = [startId]
    const depth = new Map<string, number>([[startId, 0]])
    for (let head = 0; head < order.length; head += 1) {
      const cur = order[head]!
      for (const next of adjacency.get(cur) ?? []) {
        if (depth.has(next)) continue
        depth.set(next, depth.get(cur)! + 1)
        order.push(next)
      }
    }
    return { order, depth }
  }

  const ownershipFor = (entryId: string): EntryOwnership => {
    const entryTop = topOfNode(entryId)
    const { order, depth } = reachableFrom(entryId)
    const candidates = new Set<string>()
    let frontier = -1
    for (const nid of order) {
      const d = depth.get(nid)!
      if (frontier >= 0 && d > frontier) break
      if (nid === entryId) continue
      const node = nodesById[nid]
      if (!node) continue
      const top = topOfNode(nid)
      if (!top || top === entryTop) continue
      if (isFallbackKind(containersDef[node.container]?.kind ?? '')) continue
      frontier = d
      candidates.add(top)
    }
    if (candidates.size === 0) return { state: 'none' }
    const sorted = [...candidates].sort((a, b) => a.localeCompare(b))
    return candidates.size === 1 ? { state: 'single', domainId: sorted[0]! } : { state: 'multi', candidates: sorted }
  }

  // —— 全图入口清单与逐入口派生。成本口径与 scopepage 的复用度逐入口 BFS 同一
  // 先例（入口数×边数）；真实大图性能已在 breakdown §四.4 登记，夹具不外推。——
  const entriesAll = Object.keys(nodesById)
    .filter((id) => nodesById[id]?.kind === 'entry')
    .sort((a, b) => a.localeCompare(b))
  const ownershipByEntry = new Map<string, EntryOwnership>()
  const reachByEntry = new Map<string, { order: string[] }>()
  for (const id of entriesAll) {
    ownershipByEntry.set(id, ownershipFor(id))
    reachByEntry.set(id, { order: reachableFrom(id).order })
  }

  // 注册散度（§2.4-33）：仅单值归属计入——multi/none 不属于任何单一子系统，
  // 计入任何一个都是编造归属。
  const dispAcc = new Map<string, string[]>()
  for (const id of entriesAll) {
    const own = ownershipByEntry.get(id)
    if (own?.state !== 'single') continue
    const list = dispAcc.get(own.domainId)
    if (list) list.push(id)
    else dispAcc.set(own.domainId, [id])
  }
  const dispersionOf = (domainId: string): RegistrationDispersion => {
    const ids = dispAcc.get(domainId) ?? []
    const files = new Set(ids.map((id) => nodesById[id]?.file ?? ''))
    return {
      domainId,
      entries: ids.length,
      files: files.size,
      concentrated: files.size === 1 && ids.length > 3,
    }
  }

  // 入口族（§2.4-34）：名字形状识别器，规则见文件头「判定口径」；假设由夹具固化。
  const classifyFamily = (name: string): { kind: 'cli' | 'http'; familyId: string; label: string } => {
    const trimmed = name.trim()
    const methodMatch = HTTP_FAMILY_PATTERN.exec(trimmed)
    const path = methodMatch ? `/${methodMatch[1]}` : trimmed.startsWith('/') ? trimmed : null
    if (path !== null) {
      const segments = path.split('/').filter(Boolean)
      const familyId = `/${segments.slice(0, 2).join('/')}`
      return { kind: 'http', familyId, label: `HTTP ${familyId}` }
    }
    const words = trimmed.split(/\s+/).filter(Boolean)
    const stem = words.length <= 1 ? words.join(' ') : words.slice(0, -1).join(' ')
    return { kind: 'cli', familyId: stem, label: `CLI ${stem}` }
  }
  const familyMembers = new Map<string, string[]>()
  const familyMeta = new Map<string, { kind: 'cli' | 'http'; label: string }>()
  for (const id of entriesAll) {
    const f = classifyFamily(nodesById[id]?.name ?? '')
    familyMeta.set(f.familyId, { kind: f.kind, label: f.label })
    const list = familyMembers.get(f.familyId)
    if (list) list.push(id)
    else familyMembers.set(f.familyId, [id])
  }
  const familyOf = (familyId: string): EntryFamily => {
    const members = familyMembers.get(familyId) ?? []
    const tops = new Set<string>()
    for (const m of members) {
      for (const nid of reachByEntry.get(m)?.order ?? []) {
        const top = topOfNode(nid)
        if (top) tops.add(top)
      }
    }
    const meta = familyMeta.get(familyId) ?? { kind: 'cli' as const, label: `CLI ${familyId}` }
    return { familyId, kind: meta.kind, label: meta.label, members: members.length, reachDomains: tops.size }
  }

  const node = nodesById[input.entryNodeId]
  const entryFound = node !== undefined
  const flow = baseline.flows?.[input.entryNodeId]
  const rawSteps = flow?.steps ?? []

  // 子干引用完整性：Validate 执法随 flows 真数据同批开启（types.go 注释原文），
  // 开启前悬空引用在此显式收集，不静默丢弃也不崩溃。
  const stepsById = new Map(rawSteps.map((s) => [s.id, s]))
  const dangling = new Set<string>()
  for (const s of rawSteps) {
    for (const ref of [...(s.then ?? []), ...(s.else ?? []), ...(s.body ?? [])]) {
      if (!stepsById.has(ref)) dangling.add(ref)
    }
  }

  // 实现的流程图起点：实现所在容器内的程序入口（spec 用户故事 14）。容器没有
  // 入口就返回空串如实标注——「每个实现必有入口」今天不是数据事实，不硬凑。
  const containerEntryOf = (symbolId: string): string => {
    const cid = nodesById[symbolId]?.container
    if (!cid) return ''
    return entriesAll.find((eid) => nodesById[eid]?.container === cid) ?? ''
  }

  const steps: FlowStepView[] = [...rawSteps]
    .sort((a, b) => a.order - b.order || a.id.localeCompare(b.id))
    .map((s) => {
      const target = s.to !== undefined ? nodesById[s.to] : undefined
      // 实现清单唯一来源是 implements 段（§2.1-4）：flows 步骤里塞进来的实现
      // 数组（扫描侧违约形态）在这里结构性被忽略。
      const implementations =
        s.iface === true && s.to !== undefined
          ? (baseline.implements ?? [])
              .filter(([, ifaceId]) => ifaceId === s.to)
              .map(([implId]) => ({
                nodeId: implId,
                name: nodesById[implId]?.name ?? implId,
                entryNodeId: containerEntryOf(implId),
              }))
          : []
      return {
        id: s.id,
        order: s.order,
        kind: s.kind,
        line: s.line,
        ...(s.to === undefined ? {} : { to: s.to }),
        ...(s.cond === undefined ? {} : { cond: s.cond }),
        ...(s.then === undefined ? {} : { then: s.then }),
        ...(s.else === undefined ? {} : { else: s.else }),
        ...(s.body === undefined ? {} : { body: s.body }),
        targetName: target?.name ?? null,
        targetDomain: s.to !== undefined ? topOfNode(s.to) : '',
        targetIsEntry: target?.kind === 'entry',
        ...(s.iface === undefined ? {} : { iface: s.iface }),
        implementations,
        unknownKind: !(FLOW_STEP_KINDS as readonly string[]).includes(s.kind),
      }
    })

  const ownership: EntryOwnership = entryFound
    ? ownershipByEntry.get(input.entryNodeId) ?? ownershipFor(input.entryNodeId)
    : { state: 'none' }

  return {
    entryNodeId: input.entryNodeId,
    entryFound,
    degraded: flow === undefined,
    ownership,
    registrationDispersion: ownership.state === 'single' ? dispersionOf(ownership.domainId) : null,
    family: entryFound ? familyOf(classifyFamily(node.name).familyId) : null,
    steps,
    callChain: {
      nodeIds: entryFound ? reachableFrom(input.entryNodeId).order : [],
      sequenced: false,
      conditional: false,
    },
    danglingChildRefs: [...dangling].sort((a, b) => a.localeCompare(b)),
  }
}
```

2. **ticket0 单行处置（§0 补遗强制点；整文件保留至 K6）**：

   - 删除 `    expect(model.passthrough).toBe(true)` 一行（原 ：21），并收掉留下的连续空行；
   - 文件头注释第 4 行 `// 缝 1 已由 scopepage.test.ts 正式断言取代（C12.2）；剩余缝 2 用例待 K3 正式化后整文件退役（K6）。` 替换为两行：
     `// 缝 1 已由 scopepage.test.ts 正式断言取代（C12.2）；缝 2 已由 flowpage.test.ts 正式断言取代（C12.3），`
     `// 其 degraded 双向值级断言在本文件保留至 K6 整文件退役（过渡期双保险，contract §4.2「取代」＝覆盖平移）。`
   - **不得删除其余任何内容**——:19-21 的 `entryNodeId`/`degraded` 值级断言与 ：25-31 的命中用例原样保留。

   **ticket0 值级断言 → 正式套件后继对照表**（补充【5】「覆盖不得回退」的逐支确认；「退役」仅在契约明令时发生）：

   | ticket0 断言（现行号） | 断言性质 | K3 后的归宿 |
   |---|---|---|
   | :20 `expect(model.entryNodeId).toBe('n_entry')` | 值级 | **ticket0 原样保留至 K6** ＋ flowpage.test.ts『flows 键整体缺席…』首行同值断言承接（双重在场） |
   | :21 `expect(model.degraded).toBe(true)` | 值级 | **ticket0 原样保留至 K6** ＋ 『flows 键整体缺席…』第二断言承接（双向第一侧） |
   | :21 `expect(model.passthrough).toBe(true)` | 值级 | **设计性退役**（contract §4.2 明令直通标记退场）；反向锁＝『九键冻结』支断言模型键集不含 `passthrough` |
   | :29 `expect(...degraded).toBe(false)` | 值级 | **ticket0 原样保留至 K6** ＋ 『flows 命中该入口 → degraded=false』承接（双向第二侧，同支另证 order 排序） |

   即：K3 之后 degraded 双向值级断言**同时存在于两个文件**，零覆盖间隙；K6 删 ticket0 整文件时以 flowpage.test.ts 为唯一后继（本表即交接凭据）。

3. **跑绿**：

```bash
cd graph/webui && npm test -- src/app/codegraph/flowpage.test.ts && npm test -- src/app/codegraph/ticket0.passthrough.test.ts && npm run typecheck
```

预期：两条 vitest 退出码 0（flowpage.test.ts 16 支全绿；ticket0 保留的两支保持绿）、typecheck 退出码 0（真退出码捕获方式：`npm run typecheck > log 2>&1; echo $?`——管道 tail 会吃掉码，plan 节点已踩过）。任一支红则贴原文进台账修实现，不许改判据。（沙箱绿灯相已亲证三项全过。）

4. **日志与注释核对（纪律块第 3/4 条）**：本模块维持**零 console**——纯派生层的诊断出口是模型显式字段，七个出口逐一列在文件头注释与字段 doc（degraded / entryFound / ownership='none' / registrationDispersion=null / family=null / unknownKind / danglingChildRefs）。结构化日志（`[codegraph]` 前缀）属组件/client 层约定，本缝不引入。「为什么」注释已内嵌于第 1 步代码：最近前沿归属的 spec 读数依据、邻接排序保确定性、multi/none 不计散度的反编造理由、名字识别器的假设固化与真机复核指向、iface join 单一数据源与塞入数组结构性忽略、机械序列与主干的结构隔离、悬空子干显式收集的执法时机依据、实现起点缺失空串不硬凑。实现轮若发现确需日志才能定位的问题，先记台账再向协调者提问，不得擅自引入 console。

5. **提交 A**（T1+T2 产物一并入库，测试与实现同单元评审）：

```bash
git add graph/webui/src/app/codegraph/flowpage.ts graph/webui/src/app/codegraph/flowpage.test.ts graph/webui/src/app/codegraph/ticket0.passthrough.test.ts
git commit -m "codegraph(C12.3): K3 缝 2 实装 deriveFlowPage——归属三态/注册散度/族分组/触达域散度、iface join 与降级双向"
```

不 push。

### 测试范围声明

只跑步骤 3 的三条命令（两条 focused vitest + typecheck）。全量回归归 T3。

## 4. T3｜变异复验、全量回归与收口提交

### 文件集

无产品代码改动；操作对象 = 临时变异副本（验后恢复）+ 台账 + git。

### 步骤

1. **变异（degraded 判定反转必红，contract §4.3）**：临时把实装里 `degraded: flow === undefined,` 改为 `degraded: flow !== undefined,`，跑：

```bash
cd graph/webui && npm test -- src/app/codegraph/flowpage.test.ts
```

预期至少五支转红：「flows 键整体缺席」「flows 在但该入口缺席」「flows 命中该入口」「幽灵入口显式降级」「steps 按 order 升序稳定排序」（末支因断言 degraded=false 而红）——plan 节点沙箱已按 15 支版亲证该名单；16 支终版的「领域链上溯」支只断言 ownership、预期不受影响，以实际输出为准贴台账。恢复原判定重跑回绿；`git diff` 确认空输出。变异副本不提交。

2. **全量回归**：

```bash
cd graph/webui && npm run typecheck && npm test && git diff --check
```

预期：typecheck 退出 0；全量 vitest **23 文件 / ≥179 支全绿**（基线 22 文件 163 支 + 本卡新增 flowpage.test.ts 16 支 = 179；ticket0 保留 2 支不减，沙箱已亲证该计数）；`git diff --check` 干净。原始计数行贴台账。

3. **越界路径扫描**（交棒欠账未被代做的证据）：`git log --oneline -3` 与 `git show --stat HEAD` 确认改动集 ⊆ {flowpage.ts, flowpage.test.ts, ticket0.passthrough.test.ts} ∪ 台账/文档；无 `internal/agentd`、`codegraph/domains/`、Go 路径出现；ticket0 文件仍在库（未被越权删除）。

4. **台账收口并提交 B**：把本 task 全部命令原始输出追加进 `docs/ledgers/2026-08-26-c12.3-plan-ledger.md`（实现轮段），然后：

```bash
git add docs/ledgers/2026-08-26-c12.3-plan-ledger.md
git commit -m "docs(C12.3): K3 变异复验与全量回归证据落台账"
```

不 push。

### 测试范围声明

本 task 只做验证与提交；产品代码仅允许步骤 1 的临时变异（验后必须恢复，以 git diff 空输出确认）。

## 5. 缺陷族对抗审查（五项检查之一，逐族结论；继承 breakdown K3 表并落到本计划的具体锁点）

| 族 | 结论 |
|---|---|
| 生命周期/状态机中断 | 无，因为纯函数无定时器、无订阅、无网络、无临时资源；宿主重启后模型随页面重算，中断重跑无害。 |
| 静默失败/误导报错 | 命中并锁定：degraded 三态（键缺席/键在入口缺席/命中）、幽灵入口 entryFound=false、归属 none、multi 全候选呈现、散度 multi/none=null 不编造归属、词表外 kind unknownKind、悬空子干 danglingChildRefs、实现无入口空串——每条都有专断言（T1 各支），禁止空数组/0/null 伪装有数据；最大危害「拿可达序列冒充流程图」由『序列只进 callChain』支的结构隔离断言＋sequenced/conditional 类型级恒 false 锁死。 |
| 跨平台假设 | 命中并在夹具固化：族切分的名字形状假设（方法词+路径前两段；空白切分去末词）全部写进夹具（『HTTP 形态按路径前两段成族』等支），换环境不变；真实 162 入口分布的复核标真机（breakdown §四.6 继承）。性能（逐入口 BFS × 全边集）未验证，列真机（§四.4 继承，不新增条目）。 |
| 假红/假绿测试 | 命中并锁定：数值断言用手工可验小世界（4/3 入口、36/12 文件、reachDomains 4）；边界期望硬编码不从常量推导；变异钻取证明 degraded 断言能变红；稳定性有边序扰动专支；反面断言三处（flows 塞实现数组被忽略、hasOwnProperty 区分键缺席与值、九键冻结反证无隐藏字段）；测试锁模型输出契约不锁内部 helper 名（无一 helper 被测试直接引用）。 |
| 门禁绕过 | 无，因为纯函数无写/执行路径、无新增请求入口；两个常量模块私有不可从外部覆盖。 |
| 序列化边界 | 命中且有断言：见 §6 清单逐项；「缺席与零值可区分」由 hasOwnProperty 双态断言与可选字段展开模式锁定；wire 端穿线回归归 K6 client 回放（breakdown 既定分工），本卡断言止于模型出口。 |
| 枚举新值过白名单 | 命中：FLOW_STEP_KINDS 四值经 satisfies 与 api/types 联合类型钉同步，词表外值走 unknownKind 显式分支不崩溃（jump 夹具）；族 kind 二值 'cli'\|'http' 由识别器唯一产生；兜底桶二值复用缝 1 词表不加第二份；若需要第三种族形态或第五种 step kind，先退回 contract。 |
| 承重安全属性有测试锁住 | 无安全属性；承重的「序列不冒充流程图」（用户裁决属性）与「实现清单单一数据源」（wire 单源属性）分别由结构隔离断言和 §2.1-4 反面断言锁住，后者有变异可钻（把 join 改读步骤内嵌数组即红——T3 未单列，因 §2.1-4 反面断言已常态在场）。 |

## 6. 序列化边界清单（每一处手写投影都有断言）

本卡不触 wire 字节（输入已是类型化对象，fetchCodegraph 出口不动），序列化边界指派生器内部的手写数据投影，逐项如下：

1. `baseline.edges` 二元组 → 邻接表（去重+排序）：稳定性专支（normal vs reversed 全等）＋ W1/W2 归属结果断言。
2. `domains.parent` 链 → 顶层子系统：领域链上溯专支（三层链 + 同树跨层不算跨域）；断链/环安全终止为代码路径（平铺夹具不触发，真实数据缺陷归扫描侧执法——与 scopepage 悬空端点同一先例，注释留痕）。
3. `flows[].steps` 七类 wire 字段透传：branch/loop 的 cond/then/else/body 逐字段 toEqual；可选键缺席区分由 hasOwnProperty('to'/'iface') 双态断言锁定（return 步骤无 to 键、branch 步骤无 iface 键）。
4. steps 数组 → order 升序视图（同 order id 稳定）：乱序夹具（order 2,1）排序断言两处（W6 主干支 + ticket0 后继支）。
5. `step.kind` 词表外 → unknownKind 显式：jump 夹具专断言＋其余五步 false 计数断言。
6. `implements` `[实现,接口]` 二元组 → 实现清单 join：深比较断言（含 iface.Y 对排除、实现名回填、起点空串态）；§2.1-4 反面断言（步骤内塞实现数组被忽略）。
7. `containers[].domain` + `nodes[].container` → 散度聚合（entries/files/concentrated）：W4 四侧数值断言。
8. 入口 `name` → 族三元组（familyId/kind/label）+ members/reachDomains：CLI 去末词与 HTTP 方法剥离两组夹具，数值硬编码。
9. roundtrip 属性测试不适用本卡：没有 encode∘decode 对偶（纯单向投影），等价保障来自上述逐投影字段级断言＋九键冻结；wire 端 roundtrip 由既有 Go 序列化测试与 K6 回放承担。

## 7. Spec 故事归属与冻结条覆盖

| 冻结条/spec 故事 | 落点 |
|---|---|
| §2.4-28 模块路径与导出面 | 文件头注释 + T2 实装位置不变；导出面限 codegraph 应用模块内（【释3】） |
| §2.4-29 缝地址不可变 | `deriveFlowPage` 签名原样，T1/T2 只动内部 |
| §2.4-30 输入两字段钉死 | 壳既有定义原样保留（§1 Produces） |
| §2.4-31 降级显式、序列只进 agent tab | degraded 三态断言 + callChain 结构隔离断言 + sequenced/conditional 恒 false + 变异钻取 |
| §2.4-32 入口归属三态 | W1 多值 / W2 单值（含噪声排除与距离最小化）/ W3 Cobra 无行为，各有正反夹具 |
| §2.4-33 注册散度判红 | W4 四侧边界断言（1f4e 红；1f3e、36e12f、4e2f 不红） |
| §2.4-34 族分组从名字算 | W5 跨容器跨域聚合断言 + HTTP/CLI 形态夹具；不依赖容器拆分（Out of Scope 2 未做的独立验证） |
| 触达域散度（R1 更正归缝 2） | reachDomains 数值化断言（4 与 1），定义=族成员可达顶层去重数 |
| §2.4-35 流程图形态（数据面） | steps 透传/排序/targetDomain/targetIsEntry/iface+implementations 逐字段断言；蛇形折列坐标归 K5（§1 交接） |
| §2.4-36 术语三分 | K5 渲染义务（breakdown §二 -36 行既定分工）；本卡供 family.label/degraded 数据位，模型不含术语字段 |
| §2.1-3 kind 四值词表 | FLOW_STEP_KINDS satisfies 钉同步 + unknownKind 词表外显式分支 |
| §2.1-4 iface 从 implements join、禁复制 | join 深比较 + flows 塞数组反面断言 |
| §2.1-6 entry.channel 分组 | 结构轴数据位已由 K2 ScopeEntryRef.channel 提供；行为轴按 §2.4-34 名字算族；【释2】通道未标注单桶归 K4/K5 渲染——本卡不新增 channel 字段（输入两字段钉死，撞字段移交装配层，备案 R2 先例） |
| §2.1-7 新键缺席=降级 | degraded 双向三断言（键缺席/入口缺席/命中） |
| §4.2 直通正式化 | passthrough 字段删除 + 九键冻结反向断言 + ticket0 单行处置与对照表（T2 步骤 2） |
| §4.3 变异最低集（缝 2 部分） | T3 变异一条（degraded 反转必红）；复用度阈值与未知 kind 静默化归 K2/K6 各自验证 |
| spec 用户故事 6（集中注册告知） | W4 concentrated 判据全套 |
| spec 用户故事 7（递归下钻） | targetIsEntry + to 作下钻回调参数；实现起点 entryNodeId 支撑故事 14 |
| spec 用户故事 8（复用与抽象的债） | 散度读数 + 兜底桶噪声排除（W1/W2） |
| spec 用户故事 9（缺失显式告知） | 幽灵入口/multi=null 散度/悬空引用三处诚实字段 |
| spec 用户故事 14（接口→实现换图） | InterfaceImplementation 三字段深比较（含空串态） |

**spec 缝 2 覆盖清单六项逐条指认**：归属三态→§2.4-32 行；注册散度→§2.4-33 行；族分组→§2.4-34 行；触达域散度→R1 行；递归下钻的下层域入口解析→故事 7 行；降级形态字段与标记→§2.4-31 行。无遗漏。

## 8. 真机清单（本卡新增项：无）

本卡全部判据机内可验（vitest 小世界夹具 + typecheck + 变异钻取），无视觉/兼容/交互类新增真机项。两项继承既有登记不重复立卡：性能（真实大图、162 入口逐入口 BFS 耗时）= breakdown §四.4；族切分规则与归属三态在真实 162 入口分布上的复现（spec 读数 75% 单值等）= breakdown §四.6（flows 真数据到达后）。

## 9. 占位符扫描与自审结论

- **占位符扫描**：全文无 TBD/「适当的错误处理」/「同 Task N」式骨架；T1 给出完整可运行测试文件（沙箱 16 支亲跑），T2 给出完整实装文件（沙箱绿灯亲跑）与 ticket0 行级指令；未使用「复用既有 harness 但形态未知」例外——harness 形态已实际读过并指名（scopepage.test.ts 房式）。
- **五项检查之接缝双向核对**：测试→缝——flowpage.test.ts 全部 16 支的入口调用符号都是 `deriveFlowPage`，全部落在缝 2 上；标注与入口一致。缝→测试——spec 接缝清单共两条：缝 1 由 scopepage.test.ts 锁定（K2 产物，本卡不动）；缝 2 由本测试文件整体锁定＋ticket0 两支值级双保险。无遗漏缝。
- **内部锁声明（附加不顶替）**：**零内部锁**——所有断言均从声明缝入口构造得出；分类/BFS/join/族切分都可经小世界夹具从缝口构造断言，无「构造不出」的情形。退路检查：T2/T3 步骤正文无条件退路条款，无未声明内部锁。
- **上下文预算检查**：T1 一文件、T2 两文件、T3 零新文件；全集三个路径一次圈定（§0 补遗后），无开放式扩散。
- **类型标注检查**：本卡为 S2 逻辑型，无新增外部交互面；行为验收全部为机内显式清单（命令+期望输出），真机新增为零（§8）。
- **缺陷族对抗审查**：见 §5 八族逐条。
- **序列化边界设问**：见 §6 九项逐条，每处手写投影指到断言。
- **派发前自审**：本计划全部验收步骤均为本地 npm/git/grep 命令，无任何需要驱动派发系统自身的步骤，无需标注「由协调者执行」。
- **自审三查**：①spec/契约覆盖——§7 逐条可指，spec 缝 2 六项覆盖清单逐项指认，40 条冻结断言涉本卡的每条有落点（不涉者属其他子卡，breakdown §二总表已分配）；②占位符扫描——见上；③跨 task 类型/签名一致性——§1 Produces 逐字块是唯一权威（T1 测试、T2 实装、K5/K6 交接都对同一块书写；FALLBACK_BUCKET_KINDS 复用缝 1 导出，两卡无第二份字面量）。亲跑证据边界：基线绿（22/163）、focused 命令形态、沙箱红灯相 16 红、沙箱绿灯相 16 绿+ticket0 2 绿+typecheck 0+全量 23/179、变异反转 5 红——均 plan 节点亲跑留痕于台账；未亲跑的只剩实现轮 T3 在工作树的复跑（变异在 15 支版套件上验证、16 支终版未重钻，T3 已注明以实际输出为准）。
