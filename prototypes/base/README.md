# charter 原型站说明

charter 的真实前端只有代码图查看器（`graph/webui`），单页无路由，参数经 `?project=` 传入。

| 页面 | 对应功能 | 来源路由 | 确认状态 |
|------|---------|---------|---------|
| pages/codegraph.html | 代码图查看器（现状形态镜像） | 单页 `/codegraph/app/?project=` | 未确认 |
| （重设计形态）结构轴：一页按 scope 变 | 代码图查看器 · 结构轴 | 单页 `/codegraph/app/?project=` | **已确认**（C12，2026-08-27 真机验收逐屏对照通过；基准正文见下节，base/pages/ 暂无对应镜像页） |
| （重设计形态）行为轴：入口流程图 | 代码图查看器 · 行为轴 | 同上（同一单页内的第二根轴） | **已确认**（C12，2026-08-27 真机验收逐屏对照通过；同上） |

## 沿革

2026-08-25 建站。此前 charter 无原型站，查看器的形态基准寄居在 handoff 仓
（`handoff/prototypes/`），导致 charter 侧的 review 执行者结构上看不见原型，
C1.10 为此白费两轮（roadmap 第 21 条）。C12 定「原型跟着源码走」，基准站迁来 charter。

## C12 形态确认（2026-08-26）

用户逐屏走查 `prototypes/codegraph-two-axis/`（C12 的 fork 副本）完毕，**这次确认的形态
就是最终验收基准**——真实页面开发完成后对照它验收，不另起标准。走查裁决全文在
`docs/specs/2026-08-25-codegraph-viewer-two-axis-spec.md`，逐轮留痕在卡 C12 的 timeline。

确认到的形态要点：

- **结构轴一个页面按 scope 变**：根（12 个顶层领域 = 子系统）→ 领域 → 容器（叶子领域，
  按包聚成群组）。子系统就是顶层领域，「进领域」不是单独动作。**容器是原子节点，没有详情页。**
- **右栏三 tab**：基本信息 · 对外面 · 状态机；分隔条可拖。
- **行为轴入口流程图**：矩形调用 / 菱形分支 / 卫语句甩到一侧 / 分支体缩进成子干；
  **蛇形折列**（第 1 列自上而下从底部接进第 2 列，第 2 列自下而上从顶部接进第 3 列）；
  紫框 ▸ 是下层入口可递归下钻；**双线框是接口调用——图上展示接口，右栏列全部实现，
  每个实现的入口就是那个实现的流程图起点**。
- **降级一律显式**：数据缺失的格位要说清缺什么，不显示看起来完整的假读数。

fork 副本 `prototypes/codegraph-two-axis/` 不入库，回流合并进 `base/` 由
`charter:finish` 在收尾时执行。确认基准已落在本文件，副本删除也不会丢。

2026-08-27 收尾实录：fork 副本在各 checkout 均已不可查（不入库、未留档），故 base/pages/
无法新增两轴镜像页；好在确认基准正文已在上节，且真实查看器已于 2026-08-27 通过双侧
真机逐屏对照（本机 + linux-01 隔离实例各 14/14 步），上表两行的状态据此由「确认中」
转为「已确认」。将来若需 base/ 镜像页，以真实页面截图重建即可。
